# ⚠️ Проблема с подключением к Redis

**Дата:** 2025-11-08  
**Время:** 18:30 UTC

---

## 🔍 Проблема

**Ошибка:** `ERR DB connection timed-out or wrong username-password given. Please try again`

**Статус Worker:**
- ✅ Worker запущен и работает
- ✅ Health check отвечает (статус: degraded)
- ❌ Redis подключение не работает
- ⏳ Worker пытается переподключиться (retry attempts)

---

## 📋 Текущие настройки

**Переменные окружения в Railway:**
- ✅ `UPSTASH_REDIS_REST_URL` - настроена
- ✅ `UPSTASH_REDIS_REST_TOKEN` - настроена
- ✅ Все остальные переменные настроены (10 Service Variables)

---

## 🔧 Возможные причины

1. **Неправильный токен** - `UPSTASH_REDIS_REST_TOKEN` может быть неверным
2. **Неправильный URL** - `UPSTASH_REDIS_REST_URL` может быть неверным
3. **Upstash Redis недоступен** - сервис может быть заблокирован или недоступен
4. **Сетевые проблемы** - таймауты подключения

---

## ✅ Решение

### Вариант 1: Проверить значения переменных окружения

1. Откройте Railway Dashboard → Variables
2. Проверьте значения:
   - `UPSTASH_REDIS_REST_URL` - должен быть в формате `https://xxx.upstash.io`
   - `UPSTASH_REDIS_REST_TOKEN` - должен быть валидным токеном от Upstash

### Вариант 2: Проверить Upstash Dashboard

1. Откройте https://console.upstash.com/
2. Проверьте что Redis инстанс активен
3. Скопируйте правильные значения:
   - REST URL
   - REST Token

### Вариант 3: Обновить переменные окружения

Если значения неправильные:
1. Откройте Railway Dashboard → Variables
2. Обновите `UPSTASH_REDIS_REST_URL` и `UPSTASH_REDIS_REST_TOKEN`
3. Railway автоматически перезапустит Worker

---

## 📝 Как Worker подключается к Redis

Worker использует:
- **Протокол:** `rediss://` (TLS обязателен для Upstash)
- **Username:** `default` (обязателен для Upstash)
- **Password:** `UPSTASH_REDIS_REST_TOKEN`
- **Host:** извлекается из `UPSTASH_REDIS_REST_URL`
- **Port:** `6379` (для TLS подключения к Upstash)

**Формат URL:** `rediss://default:TOKEN@HOST:6379`

---

## 🚀 После исправления

После обновления переменных окружения:
1. Railway автоматически перезапустит Worker
2. Worker подключится к Redis
3. Health check вернет статус `healthy`
4. Worker начнет обрабатывать задачи

---

## 📊 Текущий статус

- **Worker:** ✅ Работает (uptime: ~20 минут)
- **Health check:** ⚠️ Degraded (Redis не подключен)
- **Redis:** ❌ Не подключен
- **Автоматические переподключения:** ✅ Работают (retry attempts)

---

**Последнее обновление:** 2025-11-08 18:30 UTC

