# ==============================================================================
# GPT AGENT PLATFORM - STAGING ENVIRONMENT TEMPLATE
# ==============================================================================
# Скопируйте этот файл в env.staging и заполните значениями для staging окружения.
# Используйте его при настройке docker-compose.staging.yml или GitHub Environments.
# ==============================================================================

# ------------------------------------------------------------------------------
# 🔐 ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ (Staging)
# ------------------------------------------------------------------------------

# === AUTHENTICATION ===
NEXTAUTH_SECRET=generate-a-32-char-secret
NEXTAUTH_URL=https://staging.your-app.domain
JWT_SECRET=staging-jwt-secret-32-chars

# === SUPABASE ===
SUPABASE_URL=https://your-staging-project.supabase.co
SUPABASE_ANON_KEY=your-staging-supabase-anon
SUPABASE_SERVICE_ROLE_KEY=your-staging-supabase-service-role
NEXT_PUBLIC_SUPABASE_URL=https://your-staging-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-staging-supabase-anon
SUPABASE_DEFAULT_ORGANIZATION_ID=00000000-0000-4000-8000-000000000001

# === REDIS / QUEUES ===
UPSTASH_REDIS_REST_URL=https://your-staging-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-staging-upstash-token
REDIS_URL=redis://redis:6379
JOB_QUEUE_NAME=agent-jobs

# === AI PROVIDERS ===
OPENROUTER_API_KEY=sk-or-v1-your-staging-openrouter-key
ENCRYPTION_KEY=base64-encoded-32-byte-key

# ------------------------------------------------------------------------------
# 🔧 ОПЦИОНАЛЬНЫЕ ПЕРЕМЕННЫЕ
# ------------------------------------------------------------------------------

# BACKEND API
BACKEND_API_URL=https://staging-api.your-app.domain

# KOMMO INTEGRATION
KOMMO_OAUTH_REDIRECT_BASE=https://staging.your-app.domain/integrations/kommo/oauth/callback
KOMMO_WEBHOOK_SECRET=change-me-before-deploy

# EMAIL (SMTP)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=replace-me
FROM_EMAIL=noreply@staging.your-app.domain

# MONITORING
SENTRY_DSN=https://your-staging-dsn@sentry.io/project
NEXT_PUBLIC_SENTRY_DSN=https://your-staging-dsn@sentry.io/project
SENTRY_ENVIRONMENT=staging
OTEL_EXPORTER_OTLP_ENDPOINT=https://otlp.your-provider
OTEL_EXPORTER_OTLP_HEADERS=x-honeycomb-team=token

# FEATURE FLAGS
DEMO_MODE=false
E2E_ONBOARDING_FAKE=false

# ------------------------------------------------------------------------------
# 🐘 POSTGRES (для Docker Compose staging)
# ------------------------------------------------------------------------------
# Database credentials для локального Postgres контейнера
POSTGRES_DB=supabase
POSTGRES_USER=supabase
POSTGRES_PASSWORD=generate-secure-postgres-password

# ------------------------------------------------------------------------------
# 🧪 TEST / DEV TOGGLES (по необходимости)
# ------------------------------------------------------------------------------
KOMMO_TEST_ENABLED=0

# ------------------------------------------------------------------------------
# 📝 ПОДСКАЗКИ
# ------------------------------------------------------------------------------
# - Для staging окружения используйте отдельные Upstash/Supabase проекты.
# - Все переменные, содержащие `change-me` или `your-`, должны быть заменены перед деплоем.
# - Проверьте конфигурацию командой `npm run verify:env`.
# ==============================================================================
