# Railway Worker Setup - Статус

## ✅ Выполнено через браузерную автоматизацию

### 1. ✅ Railway API Token создан и обновлен
- **Токен:** `b2d3...cf26 (замаскировано)` ✅
- **Статус:** Работает и протестирован через GraphQL API
- **Обновлено:** Все скрипты и документация содержат новый токен

### 2. ✅ Railway проект создан
- **Проект ID:** `ee93e450-dfe7-4414-892f-f3c6b83d91d1`
- **Название:** `athletic-unity`
- **Environment:** `production` (worldwideservice)
- **URL:** https://railway.com/project/ee93e450-dfe7-4414-892f-f3c6b83d91d1

### 3. ✅ Основной сервис задеплоен
- **Сервис:** `gpt-agent-platform`
- **Сервис ID:** `2a8d827f-d635-4314-98a8-8c2e5cf77f39`
- **Статус:** ✅ Deployment successful (8 minutes ago via GitHub)
- **Репозиторий:** `worldwideservice/gpt-agent-platform`

---

## ⚠️ Проблема: Worker сервис

Railway не позволяет автоматически создать второй сервис из того же GitHub репозитория через диалог "Create → GitHub Repo". Это ограничение Railway UI.

---

## 🔧 Решения для Worker сервиса

### Вариант 1: Использовать Settings существующего сервиса (НЕ РЕКОМЕНДУЕТСЯ)
❌ Не подходит, так как нужен отдельный Worker сервис для фоновых задач

### Вариант 2: Создать через Docker Image (РЕКОМЕНДУЕТСЯ)
✅ Настроить Docker образ для Worker и деплоить через Railway Docker registry

### Вариант 3: Создать Empty Service + настроить вручную
✅ Создать пустой сервис, затем настроить Root Directory `services/worker` и Dockerfile

### Вариант 4: Использовать Railway CLI
✅ Создать Worker сервис через CLI с указанием Root Directory

---

## 📋 Следующие шаги

### Для завершения настройки Worker нужно:

1. **Определить подход:**
   - Docker Image (если есть Docker registry)
   - Empty Service + ручная настройка Root Directory
   - Railway CLI с конфигурацией

2. **После создания Worker сервиса:**
   - Настроить Root Directory: `services/worker`
   - Добавить переменные окружения
   - Настроить Health Check endpoint (порт 3001)
   - Проверить деплой

---

## 🔗 Полезные ссылки

- **Railway Dashboard:** https://railway.com/project/ee93e450-dfe7-4414-892f-f3c6b83d91d1
- **Railway API Token:** `b2d3...cf26 (замаскировано)`
- **Railway API Docs:** https://docs.railway.com/reference/public-api

---

## 💡 Рекомендация

**Лучший вариант:** Использовать **Railway CLI** для создания Worker сервиса с правильной конфигурацией, так как это позволит указать Root Directory `services/worker` при создании.

**Команда для Railway CLI:**
```bash
railway service create --name worker --directory services/worker
```

Или создать через браузер **Empty Service**, затем в Settings указать:
- Root Directory: `services/worker`
- Build Command: из `services/worker/Dockerfile`

---

**Дата:** 2025-01-26  
**Статус:** ⚠️ Требуется решение для создания Worker сервиса

