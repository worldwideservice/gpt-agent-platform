# 🔧 React Query DevTools - Руководство

## ✅ Что установлено

React Query DevTools теперь активны в приложении!

### Установленный пакет:
```json
"@tanstack/react-query-devtools": "^5.90.6"
```

### Интеграция:
```tsx
// components/providers/QueryClientProvider.tsx
import { ReactQueryDevtools } from '@tanstack/react-query-devtools'

<ReactQueryDevtools initialIsOpen={false} />
```

---

## 📖 Как использовать DevTools

### 1. Откройте приложение в браузере

Перейдите на любую страницу с React Query:
```
http://localhost:3000/manage/[tenantId]/dashboard
```

### 2. Найдите иконку DevTools

В **правом нижнем углу** экрана появится иконка React Query:
```
🟣 (маленькая иконка логотипа)
```

### 3. Откройте панель DevTools

Кликните на иконку - откроется панель снизу экрана.

---

## 🎯 Функции DevTools

### 📊 Просмотр всех Queries

В панели DevTools вы увидите список всех активных queries:

```
Queries (7)
├─ ['dashboard', 'stats', 'tenant-123']          ✅ fresh
├─ ['dashboard', 'monthly-responses', ...]        ✅ fresh
├─ ['dashboard', 'daily-responses', ...]          ✅ fresh
├─ ['knowledge', 'overview', 'tenant-123']        ⏳ loading
├─ ['knowledge', 'stats', 'tenant-123']           ❌ error
├─ ['knowledge', 'categories', 'tenant-123']      🔄 fetching
└─ ['knowledge', 'articles', 'tenant-123', ...]   ⚠️ stale
```

**Статусы:**
- ✅ **fresh** - данные свежие
- ⚠️ **stale** - данные устарели, скоро обновятся
- 🔄 **fetching** - загрузка в процессе
- ⏳ **loading** - первая загрузка
- ❌ **error** - ошибка при загрузке
- 💤 **inactive** - query не используется

### 🔍 Детальная информация о Query

Кликните на любой query чтобы увидеть:

**Data:**
```json
{
  "monthlyResponses": 420,
  "monthlyChange": 12,
  "weeklyResponses": 108,
  "todayResponses": 16,
  "totalAgents": 3
}
```

**Query Details:**
```
Query Key: ['dashboard', 'stats', 'tenant-123']
Status: success
Last Updated: 2 min ago
Stale At: in 3 min
Cache Time: 5 min
Observers: 1
```

**Query Explorer:**
- Data - просмотр возвращенных данных
- Query - детали конфигурации
- Actions - действия над query

### ⚡ Действия над Queries

**Refetch** - принудительно обновить данные
```
Кликните кнопку "Refetch" в правом верхнем углу панели query
```

**Invalidate** - пометить данные как устаревшие
```
Кликните "Invalidate" - query обновится автоматически
```

**Reset** - сбросить query в начальное состояние
```
Кликните "Reset" - очистит данные и состояние
```

**Remove** - удалить query из кэша
```
Кликните "Remove" - полностью удалит из памяти
```

### 📈 Фильтрация Queries

Используйте поле поиска для фильтрации:
```
Введите: "dashboard"     → покажет только dashboard queries
Введите: "knowledge"     → покажет только knowledge queries
Введите: "error"         → покажет только queries с ошибками
```

### 🔄 Сортировка

Сортируйте queries по:
- **Query Key** (алфавитный порядок)
- **Status** (сначала ошибки/загрузка)
- **Last Updated** (сначала новые/старые)

---

## 💡 Практические сценарии

### Отладка медленной загрузки

1. Откройте DevTools
2. Найдите query в статусе "loading"
3. Посмотрите время в "Last Updated"
4. Если слишком долго - проверьте Network tab в браузере

### Проверка кэширования

1. Откройте Dashboard
2. Обновите страницу (F5)
3. В DevTools смотрите:
   - Если данные появились **мгновенно** - взяты из кэша
   - Если статус "fetching" - идет обновление в фоне

### Отладка автообновления

1. Откройте Dashboard
2. В DevTools смотрите "Stale At"
3. Подождите пока время истечет
4. Query должен автоматически перейти в "fetching"

### Проверка ошибок

1. В DevTools найдите queries со статусом "error"
2. Кликните на query
3. Во вкладке "Query" смотрите раздел "Error"
4. Там будет текст ошибки и stack trace

---

## 🎨 Настройки DevTools

### Изменить позицию панели

Кликните на иконку настроек (⚙️) в DevTools:
- **Bottom** (по умолчанию) - снизу
- **Top** - сверху
- **Left** - слева
- **Right** - справа

### Закрепить панель

Toggle кнопка "Pin" - закрепит панель в открытом состоянии

### Изменить размер

Перетащите границу панели чтобы изменить высоту/ширину

---

## 🚀 Продвинутые фичи

### Query Timeline

Смотрите историю всех queries и их статусов во времени

### Mutations (в будущем)

Когда добавим мутации (создание/обновление данных):
- Будут видны в отдельной вкладке "Mutations"
- Можно отследить optimistic updates
- Видно состояние pending/success/error

### Cache Explorer

Просмотр всего кэша React Query:
- Какие queries закэшированы
- Сколько памяти занимают
- Когда истекут

---

## 📝 Горячие клавиши

- **Ctrl/Cmd + K** - открыть/закрыть DevTools
- **Esc** - закрыть DevTools
- **Ctrl/Cmd + F** - фокус на поиске

---

## ⚠️ Production режим

DevTools **автоматически исключаются** из production bundle:
- В production среде DevTools не загружаются
- Размер bundle остается минимальным
- Иконка DevTools не появляется в production

Проверить:
```bash
npm run build
# DevTools не будут включены в production bundle
```

---

## 🐛 Troubleshooting

### DevTools не появляются

1. Убедитесь что находитесь на странице с React Query
2. Проверьте что dev-сервер запущен (`npm run dev`)
3. Откройте консоль браузера - должны видеть логи React Query
4. Проверьте что `NODE_ENV !== 'production'`

### Иконка есть, но панель не открывается

1. Проверьте консоль на ошибки
2. Попробуйте перезагрузить страницу (Cmd/Ctrl + Shift + R)
3. Очистите кэш браузера

### Queries не отображаются

1. Убедитесь что страница использует React Query hooks
2. Проверьте что QueryClientProvider обернут вокруг компонентов
3. Откройте Network tab - должны быть запросы к API

---

## 📚 Дополнительные ресурсы

**Официальная документация:**
https://tanstack.com/query/latest/docs/react/devtools

**Видео-гайд:**
https://www.youtube.com/results?search_query=react+query+devtools

**Примеры использования:**
https://tanstack.com/query/latest/docs/react/examples

---

## ✅ Чек-лист для тестирования

- [ ] Открыть Dashboard в браузере
- [ ] Найти иконку DevTools внизу справа
- [ ] Кликнуть на иконку - панель открывается
- [ ] Видеть список queries (минимум 3)
- [ ] Кликнуть на query - детали открываются
- [ ] Попробовать Refetch - данные обновляются
- [ ] Попробовать Invalidate - query обновляется автоматически
- [ ] Проверить фильтрацию - работает
- [ ] Изменить позицию панели - работает
- [ ] Закрыть панель - иконка остается

---

**Готово! DevTools настроены и готовы к использованию.** 🎉

**Откройте Dashboard и попробуйте:** http://localhost:3000/manage/[tenantId]/dashboard
