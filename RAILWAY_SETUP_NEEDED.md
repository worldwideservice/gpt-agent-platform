# 🚂 Настройка Railway через браузер

**Дата:** 2025-11-08  
**Статус:** ⚠️ **ТРЕБУЕТСЯ НАСТРОЙКА**

---

## 🔍 Проблема

Railway API получает проекты, но не может найти project или service:
- ✅ Railway API работает (токен валидный)
- ✅ Проекты получены
- ❌ Project ID или Service ID не найдены

**Причина:** Railway проект не подключен к GitHub репозиторию или сервис не настроен.

---

## 🚀 Решение: Настройка через браузер

### Шаг 1: Откройте Railway Dashboard

👉 **https://railway.app**

1. Войдите через GitHub
2. Найдите проект или создайте новый

### Шаг 2: Подключите GitHub репозиторий

1. **Settings** → **Source**
2. Выберите **"Connect GitHub"** или **"Deploy from GitHub repo"**
3. Выберите репозиторий: `worldwideservice/gpt-agent-platform`
4. Убедитесь что **Root Directory:** `services/worker`

### Шаг 3: Создайте/настройте Worker сервис

1. Если сервис не существует:
   - **"+ New"** → **"GitHub Repo"**
   - Выберите: `worldwideservice/gpt-agent-platform`
   - **Root Directory:** `services/worker` ⚠️ **КРИТИЧНО!**

2. Если сервис существует:
   - Откройте сервис `worker`
   - **Settings** → **Source**
   - Убедитесь что **Root Directory:** `services/worker`

### Шаг 4: Проверьте переменные окружения

**Settings** → **Variables** → Убедитесь что все переменные настроены:

- `REDIS_URL`
- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `ENCRYPTION_KEY`
- `OPENROUTER_API_KEY`
- `JOB_QUEUE_NAME`
- `JOB_CONCURRENCY`
- `PORT`

### Шаг 5: Запустите деплой

После подключения GitHub репозитория:
- Railway автоматически деплоит при push в `main`
- Или нажмите **"Redeploy"** вручную

---

## ✅ После настройки

Railway будет автоматически деплоить Worker при каждом push в `main` через GitHub integration.

**CI/CD workflow уже настроен и готов!**

---

**Следующий шаг:** Откройте Railway Dashboard и выполните шаги выше.


