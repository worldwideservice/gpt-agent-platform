# Cursor IDE Rules - Автоматизация через MCP Browser Tools

## 🌐 Работа с MCP Browser Tools

### ⚡ КРИТИЧЕСКОЕ ПРАВИЛО: Всегда используй браузерную автоматизацию при упоминании "через браузер" или "в браузере"

**Если пользователь говорит:**
- "сделай это через браузер"
- "настрой через браузер"
- "сделай в браузере"
- "настрой в браузере"
- "через браузер"
- "в браузере"
- "setup via browser"
- "do it in browser"

**ТО ОБЯЗАТЕЛЬНО используй MCP Browser Tools для ЛЮБОГО сервиса и ЛЮБОЙ задачи!**

### Когда использовать браузерную автоматизацию:

1. **ЛЮБАЯ настройка через веб-интерфейс** - Sentry, Railway, Vercel, Supabase, AWS, Google Cloud, Azure, Stripe, Twilio, и т.д.
2. **ЛЮБОЕ создание настроек** - алерты, мониторинг, деплой, переменные окружения, API ключи, и т.д.
3. **ЛЮБОЕ получение данных** - токены, DSN, ключи, настройки, конфигурации, и т.д.
4. **ЛЮБЫЕ задачи**, где пользователь сказал "через браузер" или открыл нужную страницу

### Процесс работы:

#### Шаг 1: Пользователь делает ручную настройку (1-2 минуты)
- Открывает нужную ссылку в браузере
- Входит в систему (Google/GitHub/etc.)
- Оставляет страницу открытой
- **ИЛИ** говорит AI: "сделай это через браузер"

#### Шаг 2: AI автоматизирует все остальное
- Находит открытую страницу через `browser_snapshot` (если страница открыта)
- ИЛИ навигирует к нужному URL через `browser_navigate` (если нужно)
- Делает скриншоты для понимания контекста
- Навигирует по интерфейсу через `browser_navigate`, `browser_click`, `browser_type`
- Выполняет все необходимые настройки автоматически
- Сохраняет результат и обновляет TODO

### Универсальные команды для ЛЮБОГО сервиса:

**Пользователь может сказать:**
- "настрой [ЧТО-УГОДНО] через браузер"
- "сделай [ЧТО-УГОДНО] в браузере"
- "setup [ANYTHING] via browser"
- "настрой [сервис] через браузер"
- "[задача] через браузер"

**Примеры:**
- "настрой Sentry алерты через браузер"
- "задеплой Worker через браузер"
- "настрой переменные в Vercel через браузер"
- "получи API ключ через браузер"
- "создай мониторинг через браузер"
- "настрой бэкапы через браузер"
- И ЛЮБЫЕ другие задачи!

**AI должен для ЛЮБОЙ задачи:**
1. Найти открытую страницу через `browser_snapshot` ИЛИ навигировать к нужному URL
2. Понять контекст через скриншоты и структуру страницы
3. Навигировать к нужному разделу/настройке автоматически
4. Выполнить все необходимые действия (клики, ввод данных, выбор опций)
5. Проверить результат через скриншоты
6. Обновить TODO и документацию

## 🔧 Технические правила

### Использование MCP Browser Tools:

1. **Всегда сначала делать `browser_snapshot`** - чтобы понять текущее состояние страницы
2. **Использовать `browser_wait_for`** после навигации или действий (2-3 секунды)
3. **Делать скриншоты** (`browser_take_screenshot`) для отладки сложных интерфейсов
4. **Использовать правильные селекторы** - проверять через `browser_snapshot` перед кликами
5. **Обрабатывать ошибки** - если элемент не найден, делать скриншот и анализировать

### Порядок действий:

1. `browser_snapshot` - понять текущее состояние
2. `browser_wait_for` - подождать загрузки (если нужно)
3. `browser_click` / `browser_type` / `browser_navigate` - выполнить действие
4. `browser_wait_for` - подождать обновления страницы
5. `browser_snapshot` - проверить результат
6. Повторять до завершения задачи

### Обработка ошибок:

- Если элемент не найден → сделать скриншот для анализа
- Если страница не загрузилась → подождать дольше (`browser_wait_for` с большим временем)
- Если требуется авторизация → сообщить пользователю, что нужно войти в систему

## 📝 Документирование

### После выполнения автоматизации через браузер:

1. **Обновить TODO** - отметить выполненные задачи
2. **Создать/обновить документацию** - сохранить инструкции для команды
3. **Создать отчет** - описать что было сделано и как

### Формат отчетов:

- Создавать файлы с суффиксом `_COMPLETE.md` или `_STATUS.md`
- Включать:
  - Список выполненных действий
  - Ссылки на созданные ресурсы
  - Следующие шаги (если есть)

## 🎯 Приоритеты при работе с браузером

1. **Надежность** - всегда проверять результат через скриншоты
2. **Скорость** - не ждать слишком долго, но давать время на загрузку
3. **Прозрачность** - объяснять пользователю что происходит
4. **Повторяемость** - документировать процесс для будущих задач

## ⚠️ Важные моменты

1. **Браузер должен быть открыт** - AI работает с существующим браузером, не открывает новый
2. **Пользователь должен быть авторизован** - перед началом автоматизации нужно войти в систему
3. **Страница должна быть видимой** - не минимизировать браузер во время автоматизации
4. **Интернет соединение** - требуется стабильное соединение для работы с веб-интерфейсами

## 🔄 Универсальный сценарий для ЛЮБОЙ задачи

### Если пользователь говорит "через браузер" или "в браузере":

```
Пользователь: "[ЛЮБАЯ ЗАДАЧА] через браузер" / "сделай [ЧТО-УГОДНО] в браузере"
AI ОБЯЗАТЕЛЬНО:
1. Проверить открытую страницу через browser_snapshot
   ИЛИ навигировать к нужному URL через browser_navigate
2. browser_wait_for → 3 секунды (подождать загрузки)
3. browser_snapshot → понять структуру и контекст страницы
4. Проанализировать доступные элементы и действия
5. Навигировать к нужному разделу через browser_click
6. Выполнить необходимые действия:
   - browser_click → клик по кнопкам/ссылкам
   - browser_type → ввод текста в поля
   - browser_select_option → выбор опций из dropdown
   - browser_navigate → переход на другие страницы
7. browser_wait_for → подождать обновления после каждого действия
8. browser_snapshot → проверить результат
9. Повторять шаги 5-8 до завершения задачи
10. Обновить TODO и создать отчет о выполненных действиях
```

**Примеры применения:**
- Sentry: настройка алертов, мониторинга, Uptime Monitor
- Railway: деплой сервисов, настройка переменных
- Vercel: настройка проектов, переменных окружения
- Supabase: настройка БД, бэкапов, функций
- AWS/GCP/Azure: настройка любых сервисов
- Stripe: настройка платежей, webhooks
- Twilio: настройка SMS/voice
- **И ЛЮБЫЕ другие сервисы и задачи!**

## 📚 Ссылки на документацию

- `docs/BROWSER_AUTOMATION_FOR_TEAM.md` - Инструкция для команды
- `SENTRY_ALERTS_COMPLETE.md` - Пример успешной автоматизации Sentry
- `docs/AUTOMATION_STATUS.md` - Статус автоматизации задач

---

**Обновлено:** 2025-01-26
**Версия:** 1.0

