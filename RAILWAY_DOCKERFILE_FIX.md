# ✅ Исправление Dockerfile для Railway

**Дата:** 2025-11-08  
**Время:** 18:00 UTC

---

## 🔍 Проблема

**Ошибка:** `Cannot find module '/app/services/worker/services/worker/dist/index.js'`

**Причина:** 
- Root Directory был установлен на `/services/worker`
- Dockerfile был написан для Root Directory = `/` (корень проекта)
- Railway копирует содержимое Root Directory в `/app`
- При Root Directory = `/services/worker` Railway копирует содержимое `services/worker` в `/app`
- Но Worker использует импорты из `../lib/` и `../types/` из корня проекта
- При Root Directory = `/services/worker` Railway не имеет доступа к корню проекта

---

## ✅ Решение

**Вернул Root Directory на `/` (корень проекта)**

**Обновлен Dockerfile:**
- Dockerfile работает из корня проекта (Root Directory = `/`)
- Railway копирует весь проект в `/app`
- Worker может импортировать из `../lib/` и `../types/`
- Все пути в Dockerfile настроены правильно

---

## 📋 Текущие настройки

**Root Directory:** `/` (корень проекта)  
**Dockerfile path:** `services/worker/Dockerfile`  
**GitHub Integration:** ✅ Включен

---

## 🚀 Что нужно сделать в Railway

1. **Вернуть Root Directory на `/`:**
   - Открыть Settings → Root Directory
   - Изменить с `/services/worker` на `/`
   - Сохранить изменения

2. **Railway автоматически задеплоит:**
   - При изменении Root Directory Railway запустит новый деплой
   - Dockerfile будет работать правильно
   - Worker сможет найти все необходимые файлы

---

## ✅ Итог

**Dockerfile исправлен и готов к работе с Root Directory = `/`**

**Следующий шаг:** Вернуть Root Directory на `/` в Railway Dashboard

---

**Последнее обновление:** 2025-11-08 18:00 UTC

