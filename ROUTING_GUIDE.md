# 📍 Руководство по маршрутизации (Routing)

## Где находятся переходы между страницами?

### 1. **`middleware.ts`** - Middleware (проверка ДО загрузки страницы)
```typescript
// Файл: middleware.ts
// Здесь можно проверять авторизацию, делать редиректы и т.д.
// Выполняется ПЕРЕД загрузкой страницы
```

**Что там можно делать:**
- Проверять авторизацию до загрузки страницы
- Делать редиректы
- Проверять cookies, headers
- Блокировать доступ к определенным путям

### 2. **`auth.ts`** - Настройки NextAuth
```typescript
// Файл: auth.ts
export const authConfig = {
  pages: {
    signIn: '/',  // Куда редиректить если не авторизован
  },
  // ...
}
```

**Где указывается куда попадаешь после авторизации:**
- `pages.signIn` - куда редиректить если не авторизован (сейчас: `/`)
- После успешного `signIn()` - переход указывается в компоненте

### 3. **Layout файлы** - Проверки внутри компонентов
```typescript
// Файл: app/(protected)/layout.tsx
const session = await auth();
if (!session?.user) {
  redirect("/");  // Редирект если не авторизован
}
```

**Как работает:**
- `app/(protected)/layout.tsx` - обертка для всех защищенных страниц
- Если пользователь не авторизован → `redirect("/")` на главную
- Если авторизован → показывает children (защищенные страницы)

### 4. **Структура папок `app/`** - Маршруты Next.js App Router

```
app/
  page.tsx              → маршрут: /
  (protected)/
    page.tsx            → маршрут: /platform
    layout.tsx          → обертка для всех страниц в (protected)
```

**Как работают маршруты:**
- `app/page.tsx` = маршрут `/`
- `app/(protected)/page.tsx` = маршрут `/platform` (группа `(protected)` не входит в URL)
- Папки с `[id]` = динамические маршруты (`/users/[id]` → `/users/123`)

## 🔄 Текущий flow авторизации:

1. **Пользователь на `/` (главная)**
   - Видит форму входа/регистрации
   - Заполняет email и пароль
   - Нажимает "Войти"

2. **В компоненте `app/page.tsx`:**
   ```typescript
   const result = await signIn('credentials', {
     email,
     password,
     redirect: false,
   })
   
   if (result?.ok) {
     router.push('/platform')  // ← Здесь указано куда переходить
     router.refresh()
   }
   ```

3. **Переход на `/platform`**
   - Срабатывает `app/(protected)/layout.tsx`
   - Проверяет: есть ли сессия? (`await auth()`)
   - Если нет → `redirect("/")` на главную
   - Если да → показывает `app/(protected)/page.tsx`

## 📝 Где что находится:

| Действие | Где находится |
|----------|---------------|
| **Проверка авторизации** | `middleware.ts` или `layout.tsx` |
| **Редирект после входа** | В компоненте страницы: `router.push('/platform')` |
| **Редирект если не авторизован** | В `layout.tsx`: `redirect("/")` |
| **Настройки NextAuth** | `auth.ts` → `pages.signIn` |
| **Создание новых маршрутов** | Просто создайте файл `app/название/page.tsx` |

## 🎯 Пример: как добавить новую защищенную страницу

1. Создайте файл: `app/(protected)/новое/page.tsx`
2. Маршрут автоматически: `/platform/новое`
3. Авторизация уже работает через `layout.tsx`

## 🎯 Пример: как изменить куда попадаешь после входа

В файле `app/page.tsx` измените:
```typescript
router.push('/platform')  // ← Измените на нужный путь
```




