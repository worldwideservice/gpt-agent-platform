# 🚀 Руководство по деплою проекта

**Дата:** 2025-01-26  
**Проект:** TON 18 Platform (GPT Agent Platform)

---

## 📋 Варианты деплоя

### Вариант 1: Автоматический деплой через GitHub (Рекомендуется)

**Просто сделайте push в main ветку - деплой запустится автоматически!**

1. Убедитесь, что все изменения закоммичены:
```bash
git add .
git commit -m "Deploy to production"
git push origin main
```

2. GitHub Actions автоматически:
   - Запустит тесты
   - Соберет проект
   - Задеплоит на Vercel
   - Проверит health checks

**Требуется:** Настроенные GitHub Secrets:
- `VERCEL_TOKEN`
- `VERCEL_ORG_ID`
- `VERCEL_PROJECT_ID`

---

### Вариант 2: Деплой через Vercel CLI (Локально)

**Быстрый способ для немедленного деплоя:**

#### Шаг 1: Установка Vercel CLI (если не установлен)
```bash
npm install -g vercel
```

#### Шаг 2: Авторизация
```bash
vercel login
```

#### Шаг 3: Настройка переменных окружения (если еще не настроены)
```bash
# Создайте файл env.production из шаблона
cp env.production.example env.production

# Заполните реальными значениями
# Затем добавьте переменные в Vercel:
bash scripts/setup-vercel-env.sh
```

#### Шаг 4: Деплой
```bash
# Вариант A: Использовать готовый скрипт
bash scripts/deploy-to-vercel.sh

# Вариант B: Через npm скрипт
npm run vercel:deploy

# Вариант C: Прямой вызов Vercel CLI
vercel --prod
```

---

### Вариант 3: Деплой через Vercel Dashboard

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект `gpt-agent-kwid`
3. Перейдите в **Deployments**
4. Нажмите **Redeploy** на последнем деплое или создайте новый

**Или:**

1. Подключите GitHub репозиторий в Vercel Dashboard
2. Vercel автоматически задеплоит при каждом push в main

---

## ✅ Проверка перед деплоем

### 1. Проверка переменных окружения

**Vercel (Frontend):**
```bash
# Проверить список переменных
vercel env ls
```

**Обязательные переменные:**
- ✅ `NEXTAUTH_SECRET`
- ✅ `NEXTAUTH_URL`
- ✅ `SUPABASE_URL`
- ✅ `SUPABASE_ANON_KEY`
- ✅ `SUPABASE_SERVICE_ROLE_KEY`
- ✅ `UPSTASH_REDIS_REST_URL`
- ✅ `UPSTASH_REDIS_REST_TOKEN`
- ✅ `OPENROUTER_API_KEY`
- ✅ `ENCRYPTION_KEY`
- ✅ `NODE_ENV=production`

### 2. Проверка кода
```bash
# Проверка типов
npm run type-check

# Линтинг
npm run lint

# Форматирование
npm run format:check

# Сборка (локально)
npm run build
```

### 3. Проверка тестов
```bash
# Unit тесты
npm run test:unit

# E2E тесты (если нужно)
npm run test:e2e
```

---

## 🚀 После деплоя

### Проверка работоспособности

1. **Health Check:**
```bash
curl https://gpt-agent-kwid-1i1j7zlgl-world-wide-services-62780b79.vercel.app/api/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "timestamp": "...",
  "environment": "production"
}
```

2. **Проверка главной страницы:**
- Откройте: https://gpt-agent-kwid-1i1j7zlgl-world-wide-services-62780b79.vercel.app
- Проверьте, что страница загружается

3. **Проверка API:**
```bash
curl https://gpt-agent-kwid-1i1j7zlgl-world-wide-services-62780b79.vercel.app/api/health/ready
```

### Мониторинг

- **Vercel Dashboard:** https://vercel.com/dashboard
- **Sentry:** Проверьте логи ошибок
- **Supabase:** Проверьте метрики БД
- **Upstash Redis:** Проверьте метрики Redis

---

## 🔧 Worker Service (Railway)

Если нужно задеплоить Worker сервис:

1. Следуйте инструкции: [`docs/RAILWAY_DEPLOY_NOW.md`](./docs/RAILWAY_DEPLOY_NOW.md)
2. Или используйте Railway Dashboard:
   - Откройте: https://railway.app
   - Выберите проект
   - Выберите Worker сервис
   - Проверьте переменные окружения
   - Trigger redeploy

---

## ❌ Troubleshooting

### Проблема: "Build failed"

**Решение:**
1. Проверьте логи в Vercel Dashboard
2. Проверьте локально: `npm run build`
3. Убедитесь, что все переменные окружения установлены

### Проблема: "Environment variables missing"

**Решение:**
1. Проверьте: `vercel env ls`
2. Добавьте недостающие переменные через скрипт:
   ```bash
   bash scripts/setup-vercel-env.sh
   ```

### Проблема: "Health check fails"

**Решение:**
1. Проверьте логи: `vercel logs`
2. Убедитесь, что Supabase и Redis доступны
3. Проверьте переменные окружения

---

## 📚 Дополнительная документация

- [`docs/SETUP.md`](./docs/SETUP.md) - Полная настройка проекта
- [`docs/SECRETS.md`](./docs/SECRETS.md) - Управление секретами
- [`docs/RAILWAY_SETUP.md`](./docs/RAILWAY_SETUP.md) - Настройка Worker
- [`PRODUCTION_READINESS_CHECKLIST.md`](./PRODUCTION_READINESS_CHECKLIST.md) - Чеклист готовности

---

## 🎯 Быстрая команда для деплоя

```bash
# Все в одной команде:
npm run type-check && npm run lint && npm run build && npm run vercel:deploy
```

---

**Удачи с деплоем! 🚀**

