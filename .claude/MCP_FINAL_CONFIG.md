# Финальная конфигурация MCP серверов

> **Дата обновления:** 2025-11-15
> **Версия:** 2.0 (оптимизированная)
> **Количество серверов:** 7 (удален дублирующий Filesystem MCP)

---

## 🎯 Финальная конфигурация (7 MCP серверов)

### ✅ Что настроено и работает:

| # | MCP Server | Статус | Требует токен | Версия | Рекомендация |
|---|------------|--------|---------------|--------|--------------|
| 1 | **GitHub** | ✅ Настроен | ✅ Добавлен | 2025 | 🔴 КРИТИЧНО |
| 2 | **Supabase** | ⏳ Не настроен | ⏳ Нужен | 2025 | 🟢 РЕКОМЕНДУЮ |
| 3 | **Sequential Thinking** | ✅ Работает | ❌ Нет | v2025.7.1 | 🟢 РЕКОМЕНДУЮ |
| 4 | **Context7** | ⏳ Не настроен | ⏳ Нужен | Apr 2025 | 🟢 ОЧЕНЬ РЕКОМЕНДУЮ |
| 5 | **Magic** | ⏳ Не настроен | ⏳ Нужен (free) | Beta 2025 | 🟢 РЕКОМЕНДУЮ |
| 6 | **Playwright** | ✅ Работает | ❌ Нет | v0.0.46 (6 дней) | 🟢 РЕКОМЕНДУЮ |
| 7 | **shadcn** | ✅ Работает | ❌ Нет | 2025 | 🟢 ОЧЕНЬ РЕКОМЕНДУЮ |

### ❌ Удаленные серверы:

| # | MCP Server | Причина удаления |
|---|------------|------------------|
| 8 | ~~Filesystem~~ | 90% дублирует встроенные инструменты (Read/Write/Edit/Glob/Grep/Bash), главная фича (edit_file с dryRun) не полностью реализована |

---

## 📋 Статус по категориям

### ✅ Работают БЕЗ настройки (4 сервера):
```json
{
  "sequential-thinking": "Улучшенное мышление",
  "playwright": "Browser automation (Microsoft)",
  "shadcn": "shadcn/ui компоненты"
}
```
+ GitHub (токен уже добавлен)

### ⏳ Требуют токены (3 сервера):
```json
{
  "supabase": "Supabase Access Token + Project Ref",
  "context7": "Context7 API Key (Upstash)",
  "magic": "Magic API Key (21st.dev, бесплатно в бете)"
}
```

---

## 🚀 Текущая активная конфигурация

### `.claude/mcp_config.json` (после оптимизации):

```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "ghp_***"
      }
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp"]
    },
    "shadcn": {
      "command": "npx",
      "args": ["shadcn@latest", "mcp"]
    }
  }
}
```

**Статус:** ✅ 4 из 7 серверов активны и работают

---

## 🔧 Настройка оставшихся 3 серверов

### 1. Supabase MCP

**Зачем:** Прямой доступ к вашей Supabase БД без API

**Как получить токены:**
1. Перейти на https://supabase.com/dashboard/account/tokens
2. Создать новый Access Token
3. Скопировать токен
4. Ваш Project Ref уже известен: `rpzchsgutabxeabbnwas`

**Добавить в конфигурацию:**
```json
"supabase": {
  "command": "npx",
  "args": [
    "-y",
    "@supabase/mcp-server-supabase",
    "--project-ref",
    "rpzchsgutabxeabbnwas"
  ],
  "env": {
    "SUPABASE_ACCESS_TOKEN": "YOUR_SUPABASE_TOKEN",
    "SUPABASE_API_URL": "https://api.supabase.com"
  }
}
```

**Возможности:**
- SQL запросы напрямую
- Создание/удаление таблиц
- Применение миграций
- Доступ к логам

---

### 2. Context7 MCP (Upstash)

**Зачем:** Актуальная документация библиотек (решает проблему устаревших данных LLM)

**Как получить API ключ:**
1. Перейти на https://upstash.com
2. Создать аккаунт (если нет)
3. Получить Context7 API Key

**Добавить в конфигурацию:**
```json
"context7": {
  "command": "npx",
  "args": ["-y", "@upstash/context7-mcp"],
  "env": {
    "CONTEXT7_API_KEY": "YOUR_CONTEXT7_KEY"
  }
}
```

**Пример использования:**
```
Вопрос: "Как использовать новую функцию 'after' в Next.js? use context7"
Результат: Context7 загружает актуальную документацию Next.js 15+
```

**Почему критично:**
- Мои данные обучения: январь 2025
- Next.js, React, Supabase обновляются каждый месяц
- Context7 дает СВЕЖУЮ информацию

---

### 3. Magic MCP (21st.dev)

**Зачем:** AI-генератор UI компонентов (ускоряет frontend разработку в 5-10 раз)

**Как получить API ключ:**
1. Перейти на https://21st.dev/magic/console
2. Зарегистрироваться
3. Получить API Key (БЕСПЛАТНО в Beta)

**Добавить в конфигурацию:**
```json
"magic": {
  "command": "npx",
  "args": ["-y", "@21st-dev/magic"],
  "env": {
    "MAGIC_API_KEY": "YOUR_MAGIC_KEY"
  }
}
```

**Пример использования:**
```
Запрос: "Создай карточку продукта с изображением, заголовком, ценой и кнопкой покупки"
Результат: Готовый TypeScript компонент с красивым дизайном
```

**Возможности:**
- Production-ready код
- TypeScript support
- Real-time preview
- Интеграция с SVGL (брендовые логотипы)

---

## 📊 Сравнение конфигураций

### До оптимизации (8 серверов):
- GitHub ✅
- Supabase ⏳
- Sequential Thinking ✅
- Context7 ⏳
- Magic ⏳
- Playwright ✅
- shadcn ✅
- **Filesystem ❌ (удален)**

### После оптимизации (7 серверов):
- ✅ Убрали дублирование
- ✅ Оставили только полезные
- ✅ Все официальные
- ✅ Все активно поддерживаются

---

## 🎯 Преимущества финальной конфигурации

### 1. Нет дублирования
- Filesystem MCP удален (дублировал Read/Write/Edit/Glob/Grep)
- Каждый сервер решает уникальную задачу

### 2. Все официальные
- ✅ Anthropic (GitHub, Sequential Thinking)
- ✅ Microsoft (Playwright)
- ✅ Supabase (Supabase MCP)
- ✅ Upstash (Context7)
- ✅ 21st.dev (Magic)
- ✅ shadcn community

### 3. Активная поддержка
- ✅ Playwright: обновлен 6 дней назад
- ✅ Context7: релиз апрель 2025
- ✅ Все остальные: 2025 версии

### 4. Быстрая загрузка
- Меньше серверов = быстрее старт сессии
- Только необходимые инструменты

---

## 🔄 Следующие шаги

### Немедленно:
1. ✅ GitHub MCP - уже работает
2. ✅ Sequential Thinking - работает
3. ✅ Playwright - работает
4. ✅ shadcn - работает

### Для полной конфигурации:
5. ⏳ Добавить Supabase токен
6. ⏳ Добавить Context7 API key
7. ⏳ Добавить Magic API key

### После добавления токенов:
8. Перезапустить сессию Claude Code
9. Проверить что все 7 серверов активны

---

## 📖 Документация

### Официальные ссылки:
- [MCP Protocol](https://modelcontextprotocol.io)
- [GitHub MCP](https://github.com/github/github-mcp-server) (новый репо)
- [Supabase MCP](https://supabase.com/features/mcp-server)
- [Context7](https://upstash.com/blog/context7-mcp)
- [Magic](https://21st.dev/magic/console)
- [Playwright MCP](https://github.com/microsoft/playwright-mcp)
- [shadcn MCP](https://ui.shadcn.com/docs/mcp)

### Внутренние файлы:
- `.claude/mcp_config.json` - активная конфигурация
- `.claude/mcp_config.template.json` - шаблон
- `.claude/setup-github-token.sh` - скрипт для GitHub токена
- `.claude/MCP_SETUP.md` - общая документация

---

## ⚠️ Безопасность

1. **НИКОГДА** не коммитьте `.claude/mcp_config.json` (в .gitignore)
2. Храните токены в безопасности
3. Используйте минимальные права для токенов
4. Регулярно обновляйте (rotate) токены
5. Отзывайте старые токены

---

## 🆘 Troubleshooting

### MCP сервер не запускается
```bash
# Проверить переменные окружения
env | grep MCP

# Проверить npx
which npx

# Вручную запустить сервер
npx -y @modelcontextprotocol/server-github
```

### Токены не работают
- Проверьте формат JSON
- Убедитесь что токены не содержат пробелов
- Проверьте что токены не истекли
- Перезапустите сессию после добавления токенов

---

**Автор:** Claude AI Assistant
**Дата:** 2025-11-15
**Версия:** 2.0 (оптимизированная, 7 серверов)
**Статус:** ✅ Production Ready
