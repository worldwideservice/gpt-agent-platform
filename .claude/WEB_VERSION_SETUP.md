# Claude Code Web Version Setup Guide

> **Дата:** 2025-11-15
> **Для:** Claude Code Web (claude.ai/code)
> **Статус:** Production Ready

---

## 🌐 О веб-версии Claude Code

**Что это:**
- Запущена в октябре 2025 года
- Работает через браузер на claude.ai → вкладка "Code"
- Доступна для Pro и Max подписчиков
- Работает на телефонах и планшетах

**Как работает:**
1. Подключается к вашему GitHub аккаунту
2. Создает изолированную песочницу (sandbox) на инфраструктуре Anthropic
3. Монтирует ваш репозиторий (read или read/write в зависимости от прав)
4. Читает MCP конфигурацию из репозитория

---

## 📁 Различия в конфигурации: Desktop vs Web

### Desktop версия (CLI):
```
~/.claude.json                     # User scope
.claude/mcp_config.json           # НЕ коммитится (токены внутри)
.claude/settings.local.json       # Локальные настройки
```

### Web версия (Browser):
```
.mcp.json                          # КОММИТИТСЯ в git (БЕЗ токенов!)
```

---

## ⚠️ КРИТИЧНО: Безопасность токенов

### ❌ НИКОГДА не делайте так:
```json
{
  "mcpServers": {
    "github": {
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "ghp_xxxxx"  ← ОПАСНО!
      }
    }
  }
}
```

### ✅ Правильный подход:

**Для веб-версии:**
1. В `.mcp.json` храните только серверы БЕЗ токенов
2. Токены настраиваются через UI веб-интерфейса Claude Code
3. Anthropic хранит токены безопасно на своей стороне

---

## 🚀 Наша конфигурация для веб-версии

### `.mcp.json` (в корне проекта):

```json
{
  "mcpServers": {
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp"]
    },
    "shadcn": {
      "command": "npx",
      "args": ["shadcn@latest", "mcp"]
    }
  }
}
```

**Эти 3 сервера работают сразу, без токенов!**

---

## 🔑 Настройка серверов с токенами в веб-версии

### Серверы требующие токены:

1. **GitHub MCP**
   - Токен: GitHub Personal Access Token
   - Настройка: через Settings → Integrations в claude.ai

2. **Supabase MCP**
   - Токен: Supabase Access Token
   - Project Ref: `rpzchsgutabxeabbnwas`
   - Настройка: через Settings → Integrations в claude.ai

3. **Context7 MCP**
   - Токен: Context7 API Key (Upstash)
   - Настройка: через Settings → Integrations в claude.ai

4. **Magic MCP**
   - Токен: API_KEY (21st.dev)
   - Настройка: через Settings → Integrations в claude.ai

### Как настроить токены в веб-версии:

```
1. Откройте claude.ai
2. Перейдите в Settings (⚙️)
3. Выберите "Integrations" или "MCP Servers"
4. Добавьте каждый сервер:
   - Выберите тип сервера
   - Вставьте токен
   - Сохраните

Claude Code автоматически применит токены ко всем сессиям
```

---

## 📊 Сводка: 7 MCP серверов

### ✅ Работают сразу (в .mcp.json):
1. **Sequential Thinking** - улучшенное мышление
2. **Playwright** - автоматизация браузера
3. **shadcn** - UI компоненты

### 🔑 Требуют токены (настроить в UI):
4. **GitHub** - PR, issues, repositories
5. **Supabase** - прямой доступ к БД
6. **Context7** - актуальная документация библиотек
7. **Magic** - AI-генератор компонентов

---

## 🎯 Инструкция для быстрого старта

### Шаг 1: Коммит конфигурации
```bash
git add .mcp.json
git commit -m "feat: add MCP configuration for Claude Code web"
git push origin main
```

### Шаг 2: Открыть Claude Code Web
1. Перейти на claude.ai
2. Открыть вкладку "Code"
3. Подключить GitHub (если еще не подключен)
4. Выбрать репозиторий: `worldwideservice/gpt-agent-platform`
5. Выбрать ветку: `main` или рабочую ветку

### Шаг 3: Настроить токены
1. Settings → Integrations
2. Добавить GitHub token
3. Добавить Supabase token
4. Добавить Context7 token
5. Добавить Magic token

### Шаг 4: Проверка
В любой сессии Claude Code выполните:
```
/mcp
```

Вы должны увидеть все 7 серверов как "connected" ✅

---

## 🆘 Troubleshooting

### Серверы не загружаются

**Проблема:** `.mcp.json` не читается
**Решение:**
- Убедитесь что файл в корне проекта
- Проверьте валидность JSON
- Перезапустите сессию Claude Code

### Токены не работают

**Проблема:** Серверы показывают "authentication failed"
**Решение:**
- Проверьте токены в Settings → Integrations
- Убедитесь что токены не истекли
- Перегенерируйте токены если нужно

### Некоторые серверы не отображаются

**Проблема:** Только часть серверов видна
**Решение:**
- Это известная проблема (Issue #5037)
- Попробуйте удалить и добавить сервер заново
- Перезапустите сессию

---

## 📖 Полезные ссылки

- **Claude Code Docs:** https://docs.claude.com/en/docs/claude-code/mcp
- **MCP Protocol:** https://modelcontextprotocol.io
- **GitHub Issue Tracker:** https://github.com/anthropics/claude-code/issues

---

## 🔄 Миграция с Desktop на Web

Если вы переключаетесь с desktop на web версию:

1. **Скопируйте конфигурацию** из `~/.claude.json` или `.claude/mcp_config.json`
2. **Удалите токены** из конфигурации
3. **Создайте `.mcp.json`** с серверами без токенов
4. **Настройте токены** через UI веб-версии
5. **Закоммитьте** `.mcp.json` в репозиторий

---

**Автор:** Claude AI Assistant
**Версия:** 1.0
**Последнее обновление:** 2025-11-15
**Статус:** ✅ Готово к использованию
