# 📋 Инструкция по проверке логов вручную

## 🔍 Браузер MCP не подключен

Браузерная автоматизация недоступна. Нужно проверить логи вручную.

---

## 📍 Где найти логи

### Вариант 1: Если открыт проект `ee93e450-dfe7-4414-892f-f3c6b83d91d1`

1. Найдите сервис **Worker** в списке сервисов
2. Кликните на него
3. Перейдите в **Deployments**
4. Откройте последний **failed deployment** (красный статус)
5. Проверьте **Build Logs** и **Runtime Logs**

### Вариант 2: Если нужно найти правильный проект

**Проект с Worker:** `5cd06a89-c580-450d-958c-6b1553bb1428`  
**Service ID Worker:** `2a8d827f-d635-4314-98a8-8c2e5cf77f39`

**Прямая ссылка:**
```
https://railway.app/project/5cd06a89-c580-450d-958c-6b1553bb1428/service/2a8d827f-d635-4314-98a8-8c2e5cf77f39/deployments
```

---

## 🔍 Что искать в логах

### Build Logs (логи сборки)
- Ошибки при `npm ci`
- Ошибки при `npm run build`
- Ошибки Docker build

### Runtime Logs (логи выполнения)
- Ошибки при запуске Worker
- Ошибки валидации env переменных
- Ошибки импорта модулей
- Ошибки подключения к Redis/Supabase

### Типичные ошибки:

1. **Missing required environment variables**
   ```
   ❌ Missing required environment variables: REDIS_URL, SUPABASE_URL
   ```
   **Решение:** Добавить переменные в Settings → Variables

2. **Module not found**
   ```
   Error: Cannot find module 'xxx'
   ```
   **Решение:** Проверить зависимости в package.json

3. **Import error**
   ```
   Error: Cannot resolve '@/lib/...'
   ```
   **Решение:** Проверить tsconfig.json и пути

---

## 📝 Что делать после проверки

1. **Скопируйте ошибку** из логов
2. **Отправьте мне** текст ошибки
3. **Я исправлю проблему** и закоммичу изменения

---

## 🔧 Альтернатива: Добавить переменные окружения

Если логи показывают, что не хватает переменных окружения:

1. Перейдите в **Worker service**
2. **Settings** → **Variables**
3. Добавьте переменные:
   - `REDIS_URL`
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `ENCRYPTION_KEY`
   - `OPENROUTER_API_KEY`
   - `JOB_QUEUE_NAME` (значение: `agent-jobs`)
   - `JOB_CONCURRENCY` (значение: `5`)
   - `PORT` (значение: `3001`)
4. Railway автоматически перезапустит деплой

---

**Дата:** 2025-01-26  
**Статус:** Ожидание информации о логах


