# 📁 Структура проекта (упрощенная)

## Текущие страницы (только 2):

1. **`app/page.tsx`** → маршрут `/`
   - Главная страница с формой входа/регистрации
   - После входа → переход на `/platform`

2. **`app/(protected)/page.tsx`** → маршрут `/platform`
   - Защищенная страница платформы (только для авторизованных)
   - Если не авторизован → редирект на `/`

## Файлы маршрутизации:

- **`middleware.ts`** - обработка запросов до загрузки страницы
- **`auth.ts`** - настройки авторизации NextAuth
- **`app/(protected)/layout.tsx`** - проверка авторизации для защищенных страниц

## Компоненты:

- `components/ui/*` - базовые UI компоненты (можно использовать в будущем)

## API:

- `app/api/*` - все API эндпоинты сохранены (если понадобятся)
