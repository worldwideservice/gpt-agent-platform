# ⚠️ Railway Worker - Проблема со сборкой

## 🔍 Проблема

Worker не может собраться из-за импортов из `lib/` директории:
- `services/worker/src/tasks/heavy-processing.ts` импортирует `../../../lib/repositories/users`
- `services/worker/src/tasks/index.ts` импортирует `../../../lib/services/webhook-processor`

## ✅ Выполненные исправления

1. ✅ Изменены пути импорта с `../../../lib/` на `../lib/`
2. ✅ Dockerfile обновлен для копирования `lib/` в `services/worker/lib/` перед сборкой
3. ✅ Создан `tsup.config.ts` для конфигурации сборки
4. ✅ Обновлен `tsconfig.json` для включения `lib/` в сборку

## ⚠️ Текущая проблема

tsup/esbuild все еще не может разрешить пути `../lib/` во время сборки, даже когда файлы скопированы.

## 🔧 Возможные решения

1. **Изменить baseUrl в tsconfig** на `./` вместо `./src`
2. **Настроить paths в tsconfig** для правильного разрешения путей
3. **Использовать внешние зависимости** для lib/ и загружать их во время выполнения
4. **Рефакторинг кода** - перенести необходимые функции из lib/ в Worker

## 📋 Следующие шаги

Продолжаю тестирование с измененным baseUrl и paths в tsconfig.

---

**Обновлено:** 2025-01-26


