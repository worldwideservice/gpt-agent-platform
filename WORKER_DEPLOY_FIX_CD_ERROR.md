# 🔧 Исправление ошибки "The executable `cd` could not be found."

## ❌ Проблема

**Ошибка в Railway:** `"The executable `cd` could not be found."`

**Причина:** Railway пытается найти команду `cd`, но в Alpine Linux `cd` - это встроенная команда shell, а не исполняемый файл. Ошибка возникает при попытке выполнить CMD.

## ✅ Решение

Использовать `npm start` вместо прямого вызова `tsx`, чтобы:
1. `tsx` был найден через `node_modules/.bin/tsx`
2. Команда выполнялась через npm, который правильно обрабатывает пути
3. Все зависимости были доступны

### Изменения:

1. **Dockerfile:** Используем `npm start` вместо `CMD ["tsx", "services/worker/dist/index.js"]`
2. **package.json:** Обновлен start скрипт для явного указания tsconfig.json

### Финальный Dockerfile CMD:

```dockerfile
WORKDIR /app/services/worker
CMD ["npm", "start"]
```

### Обновленный package.json start:

```json
"start": "tsx --tsconfig ../../tsconfig.json dist/index.js"
```

## 🎯 Преимущества

- ✅ `npm start` находит `tsx` через `node_modules/.bin/tsx`
- ✅ Явное указание `tsconfig.json` гарантирует правильный резолв path aliases
- ✅ Работает в Alpine Linux без проблем с `cd`

---

**Дата:** 2025-01-26  
**Статус:** ✅ Исправлено

