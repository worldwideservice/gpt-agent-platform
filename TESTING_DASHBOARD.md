# 🧪 Тестирование Dashboard с React Query

## ✅ Что уже работает

- Dev-сервер запущен на **http://localhost:3000**
- API endpoints созданы и отвечают
- React Query интегрирован
- Client components готовы

---

## 📋 Пошаговое тестирование

### 1. Откройте Dashboard в браузере

1. Перейдите на **http://localhost:3000**
2. Войдите в систему (если требуется аутентификация)
3. Откройте Dashboard: **http://localhost:3000/manage/[tenantId]/dashboard**
   - Замените `[tenantId]` на ID вашей организации

### 2. Проверьте загрузку метрик

**Должны увидеть:**
- [ ] **4 карточки метрик:**
  - Ответы за месяц (с процентом изменения)
  - Ответы за неделю
  - Ответы сегодня (с процентом изменения)
  - Активные агенты

- [ ] **Loading состояния:**
  - При первой загрузке - скелетоны/спиннеры
  - Плавное появление данных

- [ ] **Реальные данные:**
  - Цифры отображаются корректно
  - Проценты показываются зеленым (рост) или красным (падение)

### 3. Проверьте графики

**График "Ответы ИИ за этот месяц":**
- [ ] Отображается loading spinner при загрузке
- [ ] Данные появляются после загрузки
- [ ] График интерактивный (hover показывает tooltip)
- [ ] Ось Y автоматически масштабируется

**График "Ответы ИИ за день":**
- [ ] Отображается loading spinner
- [ ] Показывает последние 9 дней
- [ ] График с наклонными подписями дат
- [ ] Интерактивный tooltip

### 4. Откройте DevTools браузера

**F12 → Network → Фильтр: Fetch/XHR**

Должны увидеть запросы:
```
✅ GET /api/manage/[tenantId]/dashboard/stats
✅ GET /api/manage/[tenantId]/dashboard/monthly-responses?months=6
✅ GET /api/manage/[tenantId]/dashboard/daily-responses?days=9
```

**Проверьте:**
- [ ] Все запросы возвращают **200 OK**
- [ ] Response содержит JSON данные
- [ ] Нет ошибок в консоли

### 5. Проверьте автообновление

**Подождите 2-5 минут:**
- [ ] Метрики обновляются автоматически в фоне
- [ ] Нет перезагрузки страницы
- [ ] Данные обновляются плавно (без мерцания)

**В Network tab увидите:**
- Повторные запросы каждые 2-5 минут
- Статус: **304 Not Modified** (если данные не изменились)
- Или **200 OK** с новыми данными

### 6. Проверьте кэширование

**Обновите страницу (F5 или Cmd+R):**
- [ ] Данные появляются **мгновенно** из кэша
- [ ] Затем в фоне проходит refetch для обновления
- [ ] Smooth transition между старыми и новыми данными

---

## 🔍 Расширенное тестирование

### Проверка React Query (если добавлены DevTools)

Откроется панель снизу экрана с информацией о всех queries:

```
Queries:
✅ ['dashboard', 'stats', 'tenant-123'] - fresh (stale in 2min)
✅ ['dashboard', 'monthly-responses', 'tenant-123', 6] - fresh
✅ ['dashboard', 'daily-responses', 'tenant-123', 9] - fresh
```

**Можно:**
- Посмотреть кэшированные данные
- Вручную инвалидировать кэш
- Refetch данные
- Посмотреть время жизни query

### Проверка Knowledge Base

**Перейдите:** http://localhost:3000/manage/[tenantId]/knowledge-base

**Проверьте:**
- [ ] Overview загружается
- [ ] Категории отображаются
- [ ] Статьи показываются
- [ ] Loading states работают

---

## 🐛 Если что-то не работает

### Проверьте консоль браузера (F12 → Console)

**Ищите ошибки:**
```
❌ Failed to fetch dashboard stats
❌ TypeError: Cannot read property...
❌ Network request failed
```

### Проверьте Network tab

**Возможные проблемы:**
- **401 Unauthorized** - проблема с аутентификацией
- **404 Not Found** - неправильный URL
- **500 Internal Server Error** - ошибка на сервере

### Проверьте логи сервера

Смотрите терминал где запущен `npm run dev`:
```bash
# Должны видеть:
GET /api/manage/tenant-123/dashboard/stats 200 in 45ms
GET /api/manage/tenant-123/dashboard/monthly-responses 200 in 32ms
GET /api/manage/tenant-123/dashboard/daily-responses 200 in 28ms
```

---

## 📊 Ожидаемые метрики производительности

| Метрика | Ожидаемое значение |
|---------|-------------------|
| **Initial Load** | < 2 секунд |
| **Cache Hit** | < 100ms |
| **API Response** | < 500ms |
| **Refetch (background)** | Незаметен для пользователя |

---

## ✅ Чек-лист успешного теста

- [ ] Сервер запущен и работает
- [ ] Dashboard страница открывается
- [ ] 4 метрики загружаются
- [ ] 2 графика отображаются
- [ ] Loading states показываются
- [ ] Данные появляются корректно
- [ ] API requests в Network tab
- [ ] Автообновление работает (подождать 2-5 мин)
- [ ] Кэширование работает (F5)
- [ ] Нет ошибок в консоли

---

## 🎯 Дополнительно (опционально)

### Добавить React Query DevTools

Раскомментируйте в `components/providers/QueryClientProvider.tsx`:
```typescript
import { ReactQueryDevtools } from '@tanstack/react-query-devtools'

// В return:
<ReactQueryDevtools initialIsOpen={false} />
```

### Проверить разные сценарии

1. **Оффлайн режим:**
   - DevTools → Network → Offline
   - Должны показываться кэшированные данные

2. **Slow 3G:**
   - DevTools → Network → Slow 3G
   - Loading states должны быть видны дольше

3. **Множественные вкладки:**
   - Откройте Dashboard в 2+ вкладках
   - Изменения должны синхронизироваться

---

## 📸 Скриншоты для проверки

### Dashboard метрики (должны видеть):
```
┌─────────────────┬─────────────────┬─────────────────┬─────────────────┐
│ Ответы за месяц │ Ответы за неделю│ Ответы сегодня  │ Активные агенты │
│      420        │      108        │       16        │        3        │
│    +12% ↑       │   Последние 7   │     +4% ↑       │     Всего       │
└─────────────────┴─────────────────┴─────────────────┴─────────────────┘
```

### Network requests (должны видеть):
```
Name                                          Status  Time
/api/manage/tenant-123/dashboard/stats        200     45ms
/api/manage/tenant-123/dashboard/monthly...   200     32ms
/api/manage/tenant-123/dashboard/daily...     200     28ms
```

---

**Удачного тестирования! 🚀**
