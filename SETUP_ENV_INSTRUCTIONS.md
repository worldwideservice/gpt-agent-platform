# 🚀 Инструкция по настройке .env файла

## ✅ .env файл создан!

Файл `.env` был создан с базовыми настройками для локальной разработки.

---

## 📋 Что нужно настроить

### 🔴 ОБЯЗАТЕЛЬНО (для работы приложения):

#### 1. Supabase (База данных)

1. Перейдите на [supabase.com](https://supabase.com) и создайте проект (или используйте существующий)
2. В Dashboard проекта найдите:
   - **Project URL** → скопируйте в `SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_URL`
   - **anon/public key** → скопируйте в `SUPABASE_ANON_KEY` и `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role key** → скопируйте в `SUPABASE_SERVICE_ROLE_KEY` (⚠️ НЕ используйте на клиенте!)

**Или для локальной разработки:**
```bash
# Установите Supabase CLI
npm install -g supabase

# Инициализируйте локальный проект
supabase init

# Запустите локальный Supabase
supabase start

# Ключи будут показаны в выводе команды
```

#### 2. NextAuth Secret

Замените `NEXTAUTH_SECRET` на безопасный случайный ключ:

```bash
# Сгенерируйте новый секрет
openssl rand -base64 32
```

Скопируйте результат в `.env`:
```
NEXTAUTH_SECRET=ваш_сгенерированный_ключ
```

#### 3. OpenRouter API Key (для AI функционала)

1. Перейдите на [openrouter.ai](https://openrouter.ai)
2. Зарегистрируйтесь или войдите
3. Перейдите в [Keys](https://openrouter.ai/keys)
4. Создайте новый API ключ
5. Скопируйте в `.env`:
   ```
   OPENROUTER_API_KEY=sk-or-v1-ваш_ключ
   ```

---

### 🟡 РЕКОМЕНДУЕТСЯ:

#### Redis (для кэширования и очередей)

**Локальная установка:**
```bash
# macOS
brew install redis
brew services start redis

# Или используйте Docker
docker run -d -p 6379:6379 redis:alpine
```

В `.env` уже указано:
```
REDIS_URL=redis://localhost:6379
```

**Или используйте Upstash (облачный Redis):**
1. Создайте проект на [upstash.com](https://upstash.com)
2. Скопируйте URL и токен в `.env`

---

### 🟢 ОПЦИОНАЛЬНО:

#### Stripe (платежи)
- Создайте аккаунт на [stripe.com](https://stripe.com)
- Получите тестовые ключи в Dashboard
- Замените в `.env`

#### Email (SMTP)
- Настройте Gmail App Password или другой SMTP сервер
- Замените в `.env`

#### Sentry (мониторинг ошибок)
- Создайте проект на [sentry.io](https://sentry.io)
- Скопируйте DSN в `.env`

---

## ✅ Быстрая проверка

После настройки проверьте:

```bash
# Проверка переменных окружения
npm run check:env

# Проверка Redis
npm run check:redis

# Проверка Supabase
npm run setup:supabase
```

---

## 🔧 Скрипты для автоматической настройки

```bash
# Автоматическая настройка всех сервисов
npm run setup:env

# Настройка только Supabase
npm run setup:supabase

# Настройка только Redis
npm run setup:redis

# Настройка только OpenRouter
npm run setup:openrouter
```

---

## 🚀 После настройки

1. **Запустите миграции БД:**
   ```bash
   npm run db:migrate
   ```

2. **Запустите приложение:**
   ```bash
   npm run dev
   ```

3. **Откройте в браузере:**
   ```
   http://localhost:3000
   ```

---

## 📝 Важные замечания

1. **НЕ коммитьте `.env` файл в Git!** (он уже в `.gitignore`)
2. Для продакшена используйте переменные окружения на хостинге (Vercel, Railway, etc.)
3. `SUPABASE_SERVICE_ROLE_KEY` - это секретный ключ, НИКОГДА не используйте его на клиенте!
4. Периодически обновляйте секретные ключи для безопасности

---

## 🆘 Проблемы?

### "Supabase connection error"
- Проверьте что URL и ключи правильные
- Убедитесь что проект активен в Supabase Dashboard

### "OpenRouter API error"
- Проверьте что API ключ начинается с `sk-or-v1-`
- Убедитесь что на балансе есть средства

### "Redis connection error"
- Убедитесь что Redis запущен: `redis-cli ping`
- Или оставьте `REDIS_URL` пустым - система будет работать без Redis

---

## 📚 Дополнительная документация

- [Supabase Setup](docs/SETUP.md)
- [OpenRouter Setup](docs/OPENROUTER_SETUP.md)
- [Database Migrations](docs/DATABASE.md)

