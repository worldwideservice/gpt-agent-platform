# 🔍 Настройка Sentry

Sentry установлен и настроен для мониторинга ошибок в продакшене.

## 📋 Что уже сделано:

1. ✅ Пакет `@sentry/nextjs` установлен
2. ✅ Базовая конфигурация в `instrumentation.ts`
3. ✅ Поддержка переменной окружения `SENTRY_DSN`

## 🚀 Как использовать:

### 1. Получите DSN из Sentry Dashboard

1. Зарегистрируйтесь на [sentry.io](https://sentry.io)
2. Создайте новый проект (выберите Next.js)
3. Скопируйте **DSN** (Data Source Name)

### 2. Добавьте переменную окружения

В `.env.local` добавьте:

```bash
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
```

Для продакшена (Vercel):
- Добавьте переменную `SENTRY_DSN` в настройках проекта Vercel

### 3. Поведение по умолчанию:

- **Development** (`NODE_ENV=development`): 
  - Sentry **отключен** по умолчанию (логирует в консоль)
  - Чтобы включить: `ENABLE_SENTRY=true`
  
- **Production**:
  - Sentry **включен** автоматически (если `SENTRY_DSN` установлен)
  - Отслеживает 10% транзакций для производительности
  - Отправляет все ошибки в Sentry

## 🎯 Что отслеживается:

- ✅ JavaScript ошибки
- ✅ Unhandled promise rejections
- ✅ Ошибки API роутов
- ✅ Ошибки серверных компонентов
- ✅ Медленные запросы (10% в продакшене)

## 🔧 Дополнительные настройки:

### Отслеживание ошибок вручную:

```typescript
import * as Sentry from '@sentry/nextjs'

try {
  // ваш код
} catch (error) {
  Sentry.captureException(error)
}
```

### Добавление контекста:

```typescript
Sentry.setUser({ email: 'user@example.com', id: '123' })
Sentry.setContext('custom', { key: 'value' })
```

### Отслеживание производительности:

```typescript
const transaction = Sentry.startTransaction({
  name: 'slow-operation',
  op: 'task'
})

// ваш код

transaction.finish()
```

## 📊 Просмотр ошибок:

Все ошибки будут доступны в [Sentry Dashboard](https://sentry.io)

## ⚙️ Настройки в `instrumentation.ts`:

- `tracesSampleRate`: процент транзакций для отслеживания производительности
- `ignoreErrors`: список ошибок, которые игнорируются
- `beforeSend`: функция для фильтрации/обогащения событий перед отправкой



