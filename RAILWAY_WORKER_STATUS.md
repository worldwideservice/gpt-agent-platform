# ✅ Railway Worker - Статус и исправления

## 🔍 Проверка статуса деплоя

**Последний деплой:**
- **Deployment ID:** `f3c9c612-f62c-49e7-891a-6fa664fc0991`
- **Статус:** ❌ FAILED
- **Дата:** 2025-11-03T17:30:39.308Z

## ✅ Исправления

### Dockerfile Worker исправлен

**Проблема:**
- Dockerfile предполагал запуск из `services/worker`, но Railway запускает его из корня (`Root Directory = /`)
- Worker не мог найти импорты из `../../../lib/` после сборки

**Решение:**
- ✅ Исправлен Dockerfile для работы из корня проекта
- ✅ Копирует правильный `package.json` из `services/worker/`
- ✅ Копирует весь проект для доступа к `lib/` директории
- ✅ Собирает Worker из правильной директории
- ✅ Копирует `lib/` в production stage для импортов
- ✅ Запускает Worker из корня через `node services/worker/dist/index.js`

## 🚀 Следующий шаг

**Нужно запустить новый деплой через Railway Dashboard:**
1. Открыть страницу сервиса: https://railway.com/project/ee93e450-dfe7-4414-892f-f3c6b83d91d1/service/2a8d827f-d635-4314-98a8-8c2e5cf77f39
2. Нажать кнопку "Deploy" или "Redeploy"
3. Дождаться завершения деплоя
4. После успешного деплоя добавить переменные окружения для Worker

## 📋 Переменные окружения для Worker

После успешного деплоя нужно добавить:
- `REDIS_URL` (обязательная)
- `SUPABASE_URL` (обязательная)
- `SUPABASE_SERVICE_ROLE_KEY` (обязательная)
- `ENCRYPTION_KEY` (обязательная, минимум 32 символа)
- `OPENROUTER_API_KEY` (опциональная)
- `JOB_QUEUE_NAME` (опциональная, по умолчанию 'agent-jobs')
- `JOB_CONCURRENCY` (опциональная, по умолчанию 5)

---

**Обновлено:** 2025-01-26


