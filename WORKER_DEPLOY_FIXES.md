# 🔧 Исправления для Worker деплоя

## ✅ Последние изменения (commit: `502228c0`)

### 1. Добавлена зависимость `bcryptjs`

**Проблема:** `lib/repositories/users.ts` использует `bcryptjs`, но его не было в dependencies Worker.

**Решение:**
- Добавлен `bcryptjs: "^3.0.2"` в `services/worker/package.json`

---

### 2. Регистрация tsx для динамических импортов

**Проблема:** `tsImport` из `tsx/esm/api` может не работать без предварительной регистрации tsx.

**Решение:**
- Добавлен импорт `import 'tsx/esm'` в `services/worker/src/index.ts` перед использованием `tsImport`

---

### 3. Улучшена обработка ошибок валидации env

**Проблема:** При отсутствии переменных окружения Worker падает с непонятной ошибкой Zod.

**Решение:**
- Добавлена обработка ошибок в `services/worker/src/lib/env.ts`
- Понятные сообщения об ошибках с указанием отсутствующих переменных
- Инструкция добавить переменные в Railway Dashboard

---

## 📋 Переменные окружения для Worker

После успешного деплоя нужно добавить следующие переменные в Railway:

```
REDIS_URL=redis://default:AYcUASQgZjI2MTM5NzYtYzU2ZS00YjFkLTk3MmQtMWIyODAzYjY3ODg5OGE3ODAzNDUwMzQ5NGE0Yjk5NzEwZDFiNWE4ZTg0MDU=@usw1-merry-term-40416.upstash.io:6379
SUPABASE_URL=https://rpzchsgutabxeabbnwas.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJwemNoc2d1dGFieGVhYmJud2FzIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc1OTU2MzMzOCwiZXhwIjoyMDc1MTM5MzM4fQ.UIkX-rUGGTbMGfd5YoF41Dx3QBuLH13nO-R3BXdbx2I
ENCRYPTION_KEY=HxXQ5WCMJ3TrFZehEHJUyMVgVX5fdGsSWy/2rixkVwE=
OPENROUTER_API_KEY=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
JOB_QUEUE_NAME=agent-jobs
JOB_CONCURRENCY=5
PORT=3001
```

**Как добавить:**
1. Откройте Railway Dashboard
2. Перейдите в сервис Worker
3. Settings → Variables → "+ New Variable"
4. Добавьте каждую переменную

Или используйте скрипт: `scripts/add-worker-env-vars.sh`

---

## 🔍 Что было исправлено

### Зависимости
- ✅ `bcryptjs` добавлен в dependencies

### Динамические импорты
- ✅ Импорт `tsx/esm` для регистрации tsx
- ✅ `tsImport` с опциями `{ parentURL, tsconfig }` для резолва path aliases

### Обработка ошибок
- ✅ Понятные сообщения об ошибках валидации env
- ✅ Инструкции для добавления переменных

---

## 📊 Статус деплоя

Railway автоматически обнаружил изменения и запустил новый деплой.

**Следующие шаги:**
1. Дождаться успешного деплоя
2. Добавить переменные окружения
3. Проверить health check
4. Протестировать обработку jobs

---

**Дата:** 2025-01-26  
**Последний commit:** `502228c0`

