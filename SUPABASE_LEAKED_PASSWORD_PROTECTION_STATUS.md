# ⚠️ Статус настройки Supabase Leaked Password Protection

**Дата:** 2025-01-26  
**Статус:** ⚠️ Требуется дополнительная настройка

---

## 📋 Выполненные действия

### 1. ✅ Проверка страницы Attack Protection

**URL:** https://supabase.com/dashboard/project/rpzchsgutabxeabbnwas/auth/protection

**Наблюдения:**
- Страница открыта и доступна
- "Enable Captcha protection" - переключатель доступен
- "Prevent use of leaked passwords" - описание есть, но нет отдельного переключателя
- "Email provider" - показывает "Disabled"

---

## 🔍 Анализ

### Текущая ситуация:
- Функция "Prevent use of leaked passwords" описана как "Rejects the use of known or easy to guess passwords on sign up or password change"
- В UI нет отдельного переключателя для этой функции
- Email provider не настроен (показывает "Disabled")

### Возможные причины:
1. **Требуется настройка Email Provider** - функция может быть доступна только после настройки email провайдера
2. **Требуется платный план** - функция может быть доступна только на Team плане или выше
3. **Включается автоматически** - функция может включаться при определенных условиях (например, при включении Captcha)
4. **Настройка через API** - функция может быть доступна только через Supabase Management API

---

## 🚀 Рекомендации

### Вариант 1: Настройка Email Provider (рекомендуется)

1. Откройте: https://supabase.com/dashboard/project/rpzchsgutabxeabbnwas/auth/providers?provider=Email
2. Настройте SMTP провайдера (SendGrid, Mailgun, AWS SES, или другой)
3. После настройки email provider, проверьте, появилась ли возможность включить "Prevent use of leaked passwords"

### Вариант 2: Проверка через Supabase Management API

```bash
# Проверить текущие настройки Attack Protection
curl -X GET \
  'https://api.supabase.com/v1/projects/rpzchsgutabxeabbnwas/auth/config' \
  -H 'Authorization: Bearer YOUR_ACCESS_TOKEN' \
  -H 'apikey: YOUR_API_KEY'

# Обновить настройки (если API поддерживает)
curl -X PUT \
  'https://api.supabase.com/v1/projects/rpzchsgutabxeabbnwas/auth/config' \
  -H 'Authorization: Bearer YOUR_ACCESS_TOKEN' \
  -H 'apikey: YOUR_API_KEY' \
  -H 'Content-Type: application/json' \
  -d '{
    "security": {
      "prevent_leaked_passwords": true
    }
  }'
```

### Вариант 3: Проверка документации Supabase

1. Изучить официальную документацию: https://supabase.com/docs/guides/auth/attack-protection
2. Проверить, требуется ли платный план для этой функции
3. Связаться с поддержкой Supabase для уточнения способа включения

---

## 📝 Следующие шаги

1. **Настроить Email Provider** (если еще не настроен):
   - Выбрать SMTP провайдера (SendGrid, Mailgun, AWS SES)
   - Настроить в Supabase Dashboard
   - Проверить, появилась ли возможность включить "Prevent use of leaked passwords"

2. **Проверить план подписки**:
   - Убедиться, что текущий план поддерживает эту функцию
   - Если нет - рассмотреть upgrade до Team плана

3. **Проверить через API**:
   - Использовать Supabase Management API для проверки и настройки

---

## ✅ Итог

Функция "Prevent use of leaked passwords" не может быть включена через UI без дополнительной настройки. Рекомендуется:

1. Настроить Email Provider в Supabase
2. Проверить документацию Supabase
3. При необходимости связаться с поддержкой

**Важно:** Эта функция повышает безопасность, но не критична для запуска проекта. Можно отложить настройку до настройки email провайдера.

---

**Последнее обновление:** 2025-01-26

