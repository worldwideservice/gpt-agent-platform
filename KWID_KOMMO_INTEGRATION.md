# 🔗 KWID ИНТЕГРАЦИЯ С KOMMO CRM - ПОЛНЫЙ АНАЛИЗ

## 📊 ЧТО ИЗВЕСТНО О РАБОТЕ KWID С KOMMO:

### ✅ СТРУКТУРА ИНТЕГРАЦИИ (из анализа Kwid):

#### 1. **Страница интеграций в Kwid:**
**URL:** `/manage/{tenant-id}/ai-agents/{agent-id}/edit/available-integrations`

**Таблица интеграций:**
- **Коммо** отображается как **установленная и активная** интеграция
- Статус: ✅ Installed, ✅ Active
- Действие: "Settings" (настройки)
- Ссылка: `/integrations/1/edit`

#### 2. **Вкладка "Lead & Contact" в редактировании агента:**

##### Data Access Settings:
- **"Sync CRM Settings"** кнопка - синхронизация данных из Kommo
- **Lead Data** - выбор полей сделок для чтения агентом
  - Примеры полей: Lead Name, Responsible User, Lead Stage, Тип услуги, Email
- **Contact Data** - выбор полей контактов для чтения
  - Примеры полей: Contact Name, Responsible User, Created Date, Tags, Email, Страна

##### Data Entry Settings:
- **Автоматическое заполнение полей CRM** на основе контекста разговора
- Правила для Lead полей (например: `cf_564832` - "Тип услуги")
- Правила для Contact полей
- Порядок применения правил (order)

#### 3. **Pipeline Settings (Воронки продаж):**
- Кнопка **"Sync CRM Settings"** - синхронизация воронок из Kommo
- Выбор воронок (pipelines) для работы агента
- Выбор этапов (stages/statuses) внутри воронок
- Настройка инструкций для каждого этапа

**Пример структуры:**
```json
{
  "pipeline_id": 11689563,
  "name": "GENERATION LEAD",
  "is_active": true,
  "all_statuses": false,
  "statuses": [89860455, 89860459, 89860463],
  "stage_settings": {
    "89860463": {
      "prompt": "Инструкции для стадии..."
    }
  }
}
```

#### 4. **Channels (Каналы связи):**
- Кнопка **"Sync CRM Settings"** - синхронизация каналов из Kommo
- Toggle "All Channels" - доступ ко всем каналам
- Выбор конкретных каналов для работы агента

---

## 🔧 КАК KWID РАБОТАЕТ С KOMMO (РЕЗУЛЬТАТЫ АНАЛИЗА):

### ✅ ИЗВЕСТНО:

#### 1. **Синхронизация настроек:**
- ✅ **"Sync CRM Settings"** кнопки присутствуют на всех связанных страницах
- ✅ Синхронизация воронок (pipelines) и этапов (stages)
- ✅ Синхронизация полей (lead_fields, contact_fields)
- ✅ Синхронизация каналов связи (channels)

#### 2. **Настройка агента:**
- ✅ Выбор воронок продаж для работы агента
- ✅ Выбор этапов внутри воронок
- ✅ Настройка инструкций для каждого этапа
- ✅ Выбор полей для чтения (Data Access)
- ✅ Настройка правил автоматического заполнения (Data Entry)

#### 3. **Структура данных:**
```json
{
  "pipeline_settings": {
    "11689563": {
      "is_active": true,
      "all_statuses": false,
      "statuses": [89860455, 89860459, 89860463],
      "stage_settings": {
        "89860463": {
          "prompt": "..."
        }
      }
    }
  },
  "lead_fields": ["name", "responsible_user_id", "status_id", "cf_564832"],
  "contact_fields": ["name", "responsible_user_id", "created_at", "tags"],
  "data_entry_rules": {
    "lead": [
      {
        "field_id": "cf_564832",
        "field_name": "Тип услуги",
        "order": 1,
        "settings": {}
      }
    ]
  }
}
```

### ⚠️ ЧТО НЕ ИЗВЛЕЧЕНО ПРЯМО ИЗ KWID:

#### 1. **OAuth Flow:**
- ❌ Детали OAuth авторизации не видны в UI
- ✅ Но структура понятна из анализа форм

#### 2. **API Endpoints:**
- ❌ Конкретные endpoints не видны в Network (требуется авторизация)
- ✅ Но понятно что используется синхронизация данных

#### 3. **Webhooks:**
- ❌ Структура webhooks не видна из frontend
- ✅ Но логично что они используются для получения событий

#### 4. **Автоматические действия:**
- ❌ Детали автоматических действий не видны
- ✅ Но есть "Data Entry Settings" для авто-заполнения полей

---

## 📋 ЧТО ЕСТЬ В ВАШЕМ ПРОЕКТЕ (ДЛЯ СРАВНЕНИЯ):

### ✅ РЕАЛИЗОВАНО:

#### 1. **OAuth 2.0 Flow:** ✅
- `POST /api/integrations/kommo/oauth/start` - начало авторизации
- `GET /api/integrations/kommo/oauth/callback` - callback обработка
- Сохранение токенов в `crm_connections` (зашифровано)
- Поддержка refresh token

#### 2. **Kommo API класс:** ✅
- Полная реализация в `lib/crm/kommo.ts`
- Методы:
  - `getPipelines()` - получение воронок
  - `getUsers()` - получение пользователей
  - `getLead()`, `createLead()`, `updateLead()` - работа со сделками
  - `getContact()`, `createContact()`, `updateContact()` - работа с контактами
  - `createTask()` - создание задач
  - `createNote()` - добавление заметок
  - `sendEmailFromLead()` - отправка email
  - `getCustomFields()` - получение кастомных полей

#### 3. **Webhooks:** ✅
- `POST /api/crm/webhook` - endpoint для получения событий
- `KommoAPI.parseWebhook()` - парсинг событий
- Обработчики для leads, contacts, customers

#### 4. **Синхронизация данных:** ✅
- `POST /api/integrations/kommo/sync/pipelines` - синхронизация воронок
- Класс `KommoAPI` с методами синхронизации
- Сохранение в БД (таблицы: `agent_pipelines`, `agent_pipeline_stages`)

#### 5. **Email функционал:** ✅
- `sendEmailFromLead()` - отправка email через Kommo
- Интерфейс `KommoEmailMessage`
- Поддержка attachments, cc, bcc

---

## 🎯 СРАВНЕНИЕ: KWID vs ВАШ ПРОЕКТ

### ✅ ЧТО ЕСТЬ В KWID, ЧТО ЕСТЬ У ВАС:

| Функция | Kwid | Ваш проект | Статус |
|---------|------|------------|--------|
| OAuth авторизация | ✅ Есть | ✅ Есть | ✅ Полностью |
| Синхронизация воронок | ✅ Есть | ✅ Есть | ✅ Полностью |
| Выбор полей для чтения | ✅ Есть | ⚠️ Частично | ⚠️ Нужно добавить UI |
| Авто-заполнение полей | ✅ Есть | ⚠️ Частично | ⚠️ Нужно реализовать |
| Настройка этапов | ✅ Есть | ✅ Есть | ✅ Полностью |
| Инструкции для этапов | ✅ Есть | ⚠️ Частично | ⚠️ Нужно добавить |
| Синхронизация каналов | ✅ Есть | ❌ Нет | ❌ Нужно реализовать |
| Webhooks | ✅ (предположительно) | ✅ Есть | ✅ Полностью |
| Email функционал | ❌ Не видно | ✅ Есть | ✅ Больше чем в Kwid |

### ⚠️ ЧТО ЕСТЬ В KWID, ЧЕГО НЕТ У ВАС:

1. **"Sync CRM Settings" кнопки** - автоматическая синхронизация
2. **Data Entry Settings UI** - визуальный редактор правил авто-заполнения
3. **Синхронизация каналов связи** - выбор каналов из Kommo
4. **Lead Stage Instructions** - инструкции для каждого этапа воронки
5. **Интеграции таблица** - управление всеми интеграциями в одном месте

### ✅ ЧТО ЕСТЬ У ВАС, ЧЕГО НЕТ В KWID:

1. **Email функционал** - отправка/чтение/ответ на письма (требование)
2. **Отдельная регистрация** - вход на платформу (требование)
3. **Более детальный API класс** - полная реализация методов Kommo

---

## 🔍 ДЕТАЛИ ИНТЕГРАЦИИ KWID (из анализа):

### 1. Структура страницы интеграций:
- Таблица со всеми интеграциями
- Фильтрация и поиск
- Статус установки (Installed/Not Installed)
- Статус активности (Active/Inactive)
- Действия: Settings, Install, Uninstall

### 2. Настройки агента для Kommo:

#### General вкладка:
- **Pipeline Settings** - выбор воронок и этапов
- **Channels** - выбор каналов связи
- **"Sync CRM Settings"** - синхронизация данных

#### Lead & Contact вкладка:
- **Data Access Settings** - выбор полей для чтения
- **Data Entry Settings** - правила авто-заполнения
- **"Sync CRM Settings"** - синхронизация полей

### 3. Структура синхронизации:
```
"Sync CRM Settings" кнопка
  ↓
API запрос к Kommo
  ↓
Получение данных (pipelines, fields, channels)
  ↓
Отображение в UI
  ↓
Сохранение выбранных настроек
```

---

## 📊 ВЫВОДЫ:

### ✅ ЧТО ПОНЯТНО О РАБОТЕ KWID С KOMMO:

1. **Синхронизация:** ✅
   - Kwid использует кнопки "Sync CRM Settings" для синхронизации
   - Синхронизируются: воронки, этапы, поля, каналы
   - Данные сохраняются в структуре агента

2. **Настройка агента:** ✅
   - Выбор воронок и этапов для работы агента
   - Выбор полей для чтения данных
   - Настройка правил авто-заполнения
   - Инструкции для каждого этапа

3. **Структура данных:** ✅
   - `pipeline_settings` - настройки воронок
   - `lead_fields`, `contact_fields` - выбранные поля
   - `data_entry_rules` - правила авто-заполнения
   - `stage_settings` - инструкции для этапов

### ⚠️ ЧТО НЕ ПОНЯТНО ДО КОНЦА:

1. **Детали OAuth:** ⚠️
   - Как именно происходит авторизация (UI не показывает)
   - Где хранятся токены (предположительно на сервере)

2. **Webhooks структура:** ⚠️
   - Какие события обрабатываются
   - Как обрабатываются события

3. **Автоматические действия:** ⚠️
   - Когда и как выполняются авто-действия
   - Логика работы "Data Entry Settings"

### 🎯 РЕКОМЕНДАЦИИ ДЛЯ РЕАЛИЗАЦИИ:

1. ✅ **Добавить "Sync CRM Settings" кнопки** - как в Kwid
2. ✅ **Реализовать Data Entry Settings UI** - визуальный редактор правил
3. ✅ **Добавить синхронизацию каналов** - выбор каналов из Kommo
4. ✅ **Добавить Lead Stage Instructions** - инструкции для этапов
5. ✅ **Создать страницу управления интеграциями** - таблица как в Kwid

---

**Статус понимания:** ~85-90% работы Kwid с Kommo понятно
**Что осталось:** детали реализации автоматических действий и webhooks логики

