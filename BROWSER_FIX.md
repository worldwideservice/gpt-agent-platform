# 🔧 Проблема с MCP Browser

**Проблема:** MCP Browser сервер не подключен к Cursor.

## ❌ Ошибка
```
{"error":"Not connected"}
```

## ✅ Решение

### Вариант 1: Перезапустить Cursor
1. Закройте Cursor полностью
2. Откройте Cursor заново
3. MCP Browser сервер должен подключиться автоматически

### Вариант 2: Настроить Railway вручную (5 минут)

1. **Откройте Railway Dashboard:**
   - https://railway.app
   - Войдите через GitHub

2. **Подключите репозиторий:**
   - **New Project** → **Deploy from GitHub repo**
   - Выберите: `worldwideservice/gpt-agent-platform`

3. **Настройте Worker сервис:**
   - Если сервис не существует: **"+ New"** → **"GitHub Repo"**
   - **Root Directory:** `services/worker` ⚠️ **КРИТИЧНО!**
   - **Settings** → **Variables** → Добавьте все переменные окружения

4. **Проверьте деплой:**
   - Railway автоматически деплоит при push в `main`
   - Или нажмите **"Redeploy"** вручную

---

**После настройки Railway будет автоматически деплоить Worker при каждом push!** 🚀


