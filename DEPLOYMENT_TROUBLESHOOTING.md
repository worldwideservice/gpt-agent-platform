# 🔍 Troubleshooting Worker Deployment

## Проблема: Деплой продолжает проваливаться

### Текущие исправления

1. ✅ Добавлен `bcryptjs` в dependencies
2. ✅ Используется `tsImport` с опциями `{ parentURL, tsconfig }`
3. ✅ Улучшена обработка ошибок env валидации
4. ✅ Убран лишний `import 'tsx/esm'` (tsx уже зарегистрирован при запуске через `tsx dist/index.js`)

### Возможные причины провала

1. **Отсутствие переменных окружения**
   - Worker падает при валидации env
   - Решение: добавить переменные после успешного деплоя

2. **Проблемы с динамическими импортами**
   - `tsImport` не может резолвить path aliases
   - Решение: проверить, что `tsconfig.json` скопирован в production image

3. **Проблемы с зависимостями**
   - Не хватает каких-то npm пакетов
   - Решение: проверить все зависимости в `lib/` файлах

### Следующие шаги

1. **Проверить логи через Railway Dashboard**
   - Открыть браузер: https://railway.app
   - Перейти в проект → Worker service → Deployments → [failed deployment] → Logs
   - Найти причину ошибки

2. **Если логи недоступны через API**
   - Использовать браузерную автоматизацию для получения логов
   - Или проверить локально запуск Worker

3. **Локальная проверка**
   ```bash
   cd services/worker
   npm run build
   # Установить переменные окружения
   npm start
   ```

---

**Дата:** 2025-01-26  
**Статус:** Проблема требует проверки логов через Dashboard


