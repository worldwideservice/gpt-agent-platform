import { describe, it, expect } from 'vitest'

// TODO: Rewrite tests for Paddle webhooks
describe.skip('API: /api/billing/webhook', () => {
  it('should be rewritten for Paddle', () => {
    expect(true).toBe(true)
  })
})
