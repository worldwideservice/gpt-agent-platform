# 📁 Проверка структуры файлов и использования компонентов

## ✅ Текущая структура

### Компоненты (используются всеми страницами):
```
app/(protected)/agents/[id]/_components/
  ├── AgentEditForm.tsx          ✅ Используется в: /manage/[tenantId]/ai-agents/[id]/edit
  ├── AgentSequencesManager.tsx  ✅ Используется в: /manage/[tenantId]/ai-agents/[id]/sequences
  ├── AdvancedSettingsClient.tsx ✅ Используется в: /manage/[tenantId]/ai-agents/[id]/advanced
  └── ...
```

### Страницы (новая структура Kwid):
```
app/(protected)/manage/[tenantId]/ai-agents/
  ├── page.tsx                    ✅ Список агентов
  ├── create/page.tsx             ✅ Создание (использует AgentEditForm)
  └── [id]/
      ├── edit/page.tsx           ✅ Редактирование (использует AgentEditForm)
      ├── sequences/page.tsx      ✅ Цепочки (использует AgentSequencesManager)
      ├── triggers/page.tsx       ✅ Триггеры
      ├── leads-contacts/page.tsx ✅ Сделки и контакты
      ├── available-integrations/page.tsx ✅ Интеграции
      └── advanced/page.tsx       ✅ Дополнительно (использует AdvancedSettingsClient)
```

### Старая структура (может конфликтовать):
```
app/(protected)/agents/
  ├── [id]/edit/page.tsx          ⚠️ РЕДИРЕКТ на новую структуру
  └── ...
```

## 🔍 Проверка импортов

Все страницы `/manage/[tenantId]/` импортируют компоненты из:
- `@/app/(protected)/agents/[id]/_components/AgentSequencesManager`
- `@/app/(protected)/agents/[id]/_components/AgentEditForm`
- `@/app/(protected)/agents/[id]/_components/AdvancedSettingsClient`

✅ Это правильно - компоненты не дублируются, используются общие.

## ⚠️ Потенциальные проблемы

1. **Конфликт роутинга**: Старая структура `app/(protected)/agents/` все еще существует
2. **Ошибка RovingFocusGroupItem**: Происходит при создании агента - нужно исправить
3. **Проверка на деплое**: Нужно убедиться, что правильные файлы используются

## 🎯 Что нужно сделать

1. Исправить ошибку `RovingFocusGroupItem` в `AgentEditForm.tsx`
2. Проверить на деплое, что страницы используют правильные компоненты
3. Возможно, удалить или закомментировать старую структуру `app/(protected)/agents/[id]/` чтобы избежать конфликтов


