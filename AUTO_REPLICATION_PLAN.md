# 🔄 План автоматической репликации Kwid

## Проблема:
Текущий подход ручной репликации не дает 100% точности.

## Решение: Автоматическое извлечение через MCP Browser

### Шаги:

1. **Открыть каждую страницу Kwid через MCP browser**
2. **Извлечь точную HTML структуру** с помощью `page.content()`
3. **Извлечь все computed CSS стили** для каждого элемента
4. **Сгенерировать React компоненты** с точными стилями

### Страницы для репликации:

- [ ] Dashboard (`/manage/{tenantId}`)
- [ ] AI Agents List (`/ai-agents`)
- [ ] Agent Edit (`/ai-agents/{id}/edit`)
- [ ] Sequences (`/ai-agents/{id}/sequences`)
- [ ] Triggers (`/ai-agents/{id}/triggers`)
- [ ] Leads & Contacts (`/ai-agents/{id}/leads-contacts`)
- [ ] Integrations (`/ai-agents/{id}/available-integrations`)
- [ ] Advanced Settings (`/ai-agents/{id}/advanced`)
- [ ] Knowledge Categories (`/knowledge-categories`)
- [ ] Knowledge Items (`/knowledge-items`)

### Инструменты:

✅ **MCP Browser Tools** - уже доступны, могу использовать прямо сейчас
⚠️ **v0.dev API** - нужно проверить доступность
⚠️ **Custom script** - создам автоматический конвертер HTML→React

## Начинаю автоматическую репликацию СЕЙЧАС!


