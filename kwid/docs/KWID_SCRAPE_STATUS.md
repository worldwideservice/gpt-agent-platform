# 📦 Статус сбора данных KWID (реплика для World Wide Services)

> Обновлено: 2025‑01‑31  
> Источник: https://aai.widgets.wearekwid.com/manage/1000373-worldwideservices  
> CRM-интеграция: https://worldwideservices.kommo.com/settings/widgets/aia_chatgpt_leadsbot/

Документ фиксирует, какие части сервиса уже скопированы в локальный репозиторий (`../raw/scrape/`, `./`) и какие фрагменты ещё нужно доснять для полной реплики. Формат — по разделам интерфейса.

---

## 1. Агенты ИИ

- **Собрано**
  - Полная страница списка (`../raw/scrape/ai-agents.html|png|txt|tables.json`).
  - Snapshot Filament таблицы (`../raw/scrape/ai-agents.snapshot.json`).
  - Livewire‑пэйлоады:
    - Создание агента (`../raw/scrape/actions/ai_agent_create*.json` + `parsed/`).
    - Удаление агента (`../raw/scrape/actions/ai_agent_delete_*.json` + `parsed/`).
    - Триггеры «вся база знаний» и fallback (`../raw/scrape/actions/agent_toggle_*`, `knowledge_not_found_task.*`, `all_knowledge_base.*`).
    - Сохранение профиля (`../raw/scrape/actions/save_original.*`, `save_modified.*`).
  - Формы создания/редактирования (`../raw/scrape/forms/ai-agent-*.html|png|fields.json|snapshot.json`).
- **Что ещё нужно**
  1. **Тоггл “Проверять перед отправкой” (`data.manual_generation`)** — собрать Livewire `save` запрос на включение/выключение и обратный откат.
  2. **Блок “Создать задачу, если ответ не найден”** — сохранить пэйлоад с активным `knowledge_not_found_task` (уже есть enable/disable), но требуется `task` конфигурация (поле резерва задачи, если оно появляется).
  3. **Блок “Указать ссылку…”** — в UI появляется при включении fallback‑сообщения; нужно зафиксировать запрос с пользовательским текстом + URL (если доступно).
  4. **Табличные действия “Копировать” (`tableAction('copy')`) и пагинация** — пока нет лога `mountTableAction`, нужно поймать запросы для реплики таблицы.

---

## 2. База знаний

- **Собрано**
  - Листинги категорий и статей (html/png/txt/tables.json).
  - Формы создания категории/статьи (`../raw/scrape/forms/knowledge-*.html|png|fields.json`).
  - Запросы `knowledge_category_create`, `knowledge_item_create` (с ошибкой на отсутствие категории).
- **Что ещё нужно**
  1. **Успешное создание статьи**: заполнить категорию `582`, получить `knowledge_item_create.response.json` и `parsed` с `record`.
  2. **Удаление статьи** (`tableAction('delete')`) — аналитика для зеркального поведения.
  3. **Создание/удаление категории** — текущий response пустой; требуется лог успешного ответа и обратное удаление, чтобы поймать `tableAction`.
  4. **Bulk‑действия** (если есть чекбоксы) — проверить наличие Livewire событий для массового удаления.

---

## 3. Тестовый чат

- **Собрано**
  - Страница тестового чата (`../raw/scrape/test-chat.*`).
  - Пэйлоад `sendMessage` (request/response + parsed).
  - Инициализация чата (`../raw/scrape/actions/test_chat_select.*`).
- **Что ещё нужно**
  1. **`newChat`** — зафиксировать Livewire запрос на создание нового чата (пока есть только `sendMessage` внутри существующего).
  2. **Проверка успешного ответа** — нужно поймать `messages` с готовым ответом ассистента (response Livewire после генерации).
  3. **Скринкаст/лог websocket** (если ассистент отвечает стримингом) — пригодится при воссоздании UX.

---

## 4. Навигация и глобальные страницы

- **Собрано**
  - Dashboard, Pricing, Account Settings, Knowledge таблицы (HTML+PNG).
  - Документация по структуре (`./KWID_ARCHITECTURE*.md`, `./KWID_ROUTING_ANALYSIS.md`).
- **Что ещё нужно**
  1. **Уведомления** — требуют скрин/DOM состояния выпадающего списка + API `notifications/read`.
  2. **Темы интерфейса** — подтвердить Livewire событие при переключении “Светлая/Тёмная/Системная”.
  3. **Breadcrumbs и URL‑переходы** — записать `filament.navigate` события при смене вкладок агента.

---

## 5. Kommo → виджет GPT Agent

- **Собрано**
  - Скрипт `scripts/kwid-login.ts`: авторизация в Коммо и скриншот виджета (см. `../raw/scrape/screenshots/widget-settings-*.png`).
  - Актуальный UI‑снимок с лимитами аккаунта.
- **Что ещё нужно**
  1. **Снять полную страницу настроек GPT‑виджета**: скрин/DOM модулей “Интеграция”, “Webhook”, “Поля”.
  2. **Экспортировать JSON настроек** — если доступен API `widgets/settings`, застабить payload.
  3. **Проверить синхронизацию** (кнопки “Сохранить”, “Проверить подключение”) — нужен лог сетевых запросов.

---

## 6. Дополнительные действия перед сбором реплики

1. **Обновить magic link**. В памяти предыдущего агента: используется редирект через `https://gpt-agent-crm.d-novation.com/magiclink/...`. Нужно сгенерировать свежий линк перед Playwright‑сессией.
2. **Очистка временных сущностей**. После тестов удалять:
   - Временных агентов (`Temp Agent Snapshot ...`),
   - Статьи/категории snapshot, чтобы Production не замусорить.
3. **Консолидировать Livewire схемы**. Скрипт для автоматического парсинга (`../raw/scrape/actions/parsed`) должен пополняться новыми кейсами (см. TODO выше).

---

## 7. Следующие шаги

1. Запустить новый Playwright‑скрипт `scripts/kwid-scrape.ts` (добавлен) и расширить его сценариями:
   - `livewire:agent_manual_generation` — фиксация enable/disable с парсингом в `actions/parsed/`.
   - `livewire:knowledge_task_configure` — сбор полного payload конфигурации задачи.
   - `livewire:fallback_message_with_url` — сохранение текста + URL.
   - `tables:ai-agent-actions` — `copy` и пагинация для списка агентов.
   - `knowledge-base:items-success` и `knowledge-base:bulk-actions` — успешные CRUD/массовые действия.
   - `test-chat:new-session` — `newChat`, streaming и полный ответ ассистента.
   - `global:notifications`, `global:theme-toggle`, `global:search` — навигация и глобальные элементы.
2. Дополнить скрипт функциями сохранения логики (`behavior/*.json`) и маппинга (`mapping/*.json`), затем пройтись по Kommo‑виджету: сохранить HTML/JSON настроек, описать поля и действия.
3. Обновить архитектурный отчёт — внести логику триггеров, fallback‑сообщений и сценариев задач CRM на основе собранных payloads.

После выполнения пунктов выше реплика будет содержать 100% интерактивных сценариев и сможет воспроизводить поведение оригинального сервиса без доступа к продакшену.
