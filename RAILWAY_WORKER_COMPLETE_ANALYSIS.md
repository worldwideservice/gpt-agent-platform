# 🔍 Полный анализ проблемы Worker деплоя

## Основные проблемы

### 1. Path Aliases в lib/ не резолвятся в runtime

**Проблема:**
- Файлы в `lib/` используют path aliases: `@/lib/...`, `@/types/...`
- Эти aliases определены в корневом `tsconfig.json` (`"@/*": ["./*"]`)
- `tsx` не резолвит TypeScript path aliases автоматически в runtime
- Когда Worker динамически импортирует файлы из `lib/`, они пытаются импортировать через `@/lib/`, но эти пути не резолвятся

**Примеры проблемных импортов:**
```typescript
// lib/services/webhook-processor.ts
import { getSupabaseServiceRoleClient } from '@/lib/supabase/admin'
import { addJobToQueue } from '@/lib/queue'
import { getCrmConnectionData } from '@/lib/repositories/crm-connection'
import { KommoAPI } from '@/lib/crm/kommo'

// lib/repositories/users.ts
import { getSupabaseServiceRoleClient } from await import('@/lib/supabase/admin')
import type { User, DatabaseUser, Subscription, Plan } from '@/types/user'
```

### 2. Структура проекта

```
/app
  /lib                     # Общая библиотека (TypeScript файлы)
    /services
      webhook-processor.ts # Использует @/lib/... импорты
    /repositories
      users.ts             # Использует @/lib/... импорты
  /services
    /worker
      /src
        /tasks
          index.ts         # Динамически импортирует lib/services/webhook-processor
          heavy-processing.ts # Динамически импортирует lib/repositories/users
      /dist
        index.js           # Скомпилированный Worker
```

### 3. Как это работает в Next.js

- Next.js использует Webpack/SWC, который резолвит path aliases во время сборки
- Все импорты транслируются в реальные пути
- Path aliases не нужны в runtime

### 4. Как это должно работать в Worker

**Варианты решения:**

**A. Использовать tsconfig-paths для резолва path aliases:**
- Добавить `tsconfig-paths` в dependencies
- Регистрировать перед импортами: `import 'tsconfig-paths/register'`
- Работает для Node.js ESM

**B. Изменить импорты в lib/ на относительные:**
- Заменить все `@/lib/...` на относительные пути
- Требует большого рефакторинга

**C. Компилировать lib/ в JS с резолвом path aliases:**
- Использовать tsc для компиляции lib/
- Пути будут резолвлены в реальные пути
- Более production-ready подход

## Рекомендуемое решение

**Использовать tsconfig-paths** - самое простое и правильное решение для текущей архитектуры.

### Шаги:

1. Добавить `tsconfig-paths` в dependencies Worker
2. Создать tsconfig.json в корне проекта специально для tsx (или использовать существующий)
3. Зарегистрировать tsconfig-paths в entry point Worker
4. Убедиться, что tsx использует правильный tsconfig.json

---

**Дата анализа:** 2025-01-26

