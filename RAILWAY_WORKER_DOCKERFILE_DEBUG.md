# 🔍 Railway Worker - Отладка Dockerfile

## ✅ Добавлена проверка копирования lib/

### Проблема
Деплой продолжает проваливаться, возможно из-за того, что `lib/` не копируется правильно или файлы не доступны во время выполнения.

### Решение
Добавлена проверка и логирование в Dockerfile:

```dockerfile
RUN mkdir -p ./services/worker/lib && \
    if [ -d ./lib ] && [ "$(ls -A ./lib 2>/dev/null)" ]; then \
      cp -r ./lib/* ./services/worker/lib/ && \
      echo "✅ lib/ скопирован в services/worker/lib/"; \
    else \
      echo "⚠️ lib/ директория пуста или не существует"; \
    fi && \
    ls -la ./lib/services/ 2>/dev/null || echo "lib/services/ не найден" && \
    ls -la ./services/worker/lib/services/ 2>/dev/null || echo "services/worker/lib/services/ не найден"
```

### Проверка локально
- ✅ `lib/services/webhook-processor.ts` существует
- ✅ `lib/services/` содержит файлы

## 📋 Изменения

**Commit:** `d7998ef4`
- Добавлена проверка существования `lib/` перед копированием
- Добавлено логирование для отладки путей импорта
- Проверка наличия `lib/services/` и `services/worker/lib/services/`

## 🚀 Ожидание

После успешного деплоя логи покажут:
1. Была ли скопирована `lib/` директория
2. Какие файлы доступны в `lib/services/`
3. Какие файлы доступны в `services/worker/lib/services/`

Это поможет понять, правильно ли работает копирование и доступны ли файлы для динамических импортов.

---

**Время:** 2025-01-26  
**Статус:** ⏳ Ожидание нового деплоя с отладочными логами


