# 📘 Runbooks - Процедуры реагирования на инциденты

> Пошаговые инструкции для команды DevOps при возникновении проблем в production

## 🚨 Критические инциденты

### 1. Health Check не проходит

**Симптомы:**
- `/api/health` возвращает ошибку
- Статус код не 200
- Логи показывают проблемы с подключением

**Действия:**

1. **Проверить статус сервисов:**
   ```bash
   # Vercel Dashboard
   # Проверить последний деплой
   # Проверить функции и логи
   ```

2. **Проверить подключения:**
   - ✅ Supabase доступен: https://status.supabase.com
   - ✅ Upstash Redis доступен: https://status.upstash.com
   - ✅ Worker доступен (если задеплоен): проверьте Railway/Render

3. **Проверить логи:**
   - Vercel → Project → Functions → Logs
   - Sentry Dashboard → Errors

4. **Быстрый фикс:**
   - Если проблема с БД → проверить connection string
   - Если проблема с Redis → проверить REDIS_URL
   - Если проблема с Worker → перезапустить worker

5. **Если не помогло:**
   - Перезапустить деплой на Vercel
   - Откатить на предыдущую версию

---

### 2. Деплой упал / Build Failed

**Симптомы:**
- GitHub Actions workflow failed
- Vercel build failed
- Локальный build работает

**Действия:**

1. **Проверить логи сборки:**
   - GitHub Actions → Failed workflow → View logs
   - Vercel → Deployments → Failed deployment → Logs

2. **Частые причины:**
   - TypeScript ошибки → проверить `npm run type-check`
   - ESLint ошибки → проверить `npm run lint`
   - Ошибки зависимостей → проверить `package.json`
   - Environment variables missing

3. **Исправить и повторить:**
   ```bash
   git add .
   git commit -m "fix: resolve build errors"
   git push
   ```

4. **Откат (если критично):**
   - Vercel → Deployments → Previous deployment → "Promote to Production"

---

### 3. Ошибки в API / 500 Internal Server Error

**Симптомы:**
- Пользователи получают 500 ошибки
- Sentry показывает новые ошибки
- Логи показывают exceptions

**Действия:**

1. **Проверить Sentry Dashboard:**
   - Открыть: https://sentry.io
   - Проверить новые ошибки
   - Посмотреть stack trace

2. **Проверить логи:**
   - Vercel → Functions → Logs
   - Фильтр по ошибкам: `ERROR`, `Exception`

3. **Типичные проблемы:**

   **Проблема: "Database connection failed"**
   - Проверить SUPABASE_URL и SUPABASE_SERVICE_ROLE_KEY
   - Проверить статус Supabase
   - Решение: перезапустить сервис или проверить credentials

   **Проблема: "Redis connection failed"**
   - Проверить REDIS_URL
   - Проверить Upstash status
   - Решение: обновить REDIS_URL или перезапустить

   **Проблема: "OpenRouter API error"**
   - Проверить OPENROUTER_API_KEY
   - Проверить баланс/лимиты OpenRouter
   - Решение: обновить ключ или проверить billing

4. **Временный workaround:**
   - Отключить проблемную функцию через feature flag (если есть)
   - Добавить обработку ошибок для graceful degradation

5. **Hotfix:**
   ```bash
   # Создать hotfix branch
   git checkout -b hotfix/api-error-fix
   # Исправить проблему
   git commit -m "hotfix: fix API error"
   git push
   # Создать PR и срочно мержить
   ```

---

### 4. Worker не обрабатывает задачи

**Симптомы:**
- Файлы остаются в статусе "pending"
- Очередь переполняется
- Задачи не выполняются

**Действия:**

1. **Проверить статус Worker:**
   ```bash
   # Railway
   railway status
   railway logs
   
   # Или через Dashboard
   # Railway → Project → Service → Logs
   ```

2. **Проверить health check:**
   ```bash
   curl https://your-worker-url.up.railway.app/health
   ```

3. **Проверить подключения:**
   - Redis connection (REDIS_URL)
   - Supabase connection
   - Environment variables

4. **Перезапустить Worker:**
   ```bash
   # Railway CLI
   railway restart
   
   # Или через Dashboard
   # Railway → Service → Restart
   ```

5. **Проверить очередь:**
   - Проверить что задачи добавляются в очередь
   - Проверить логи на ошибки обработки

6. **Если не помогло:**
   - Проверить переменные окружения в Railway
   - Проверить что Worker подключен к правильной очереди
   - Проверить что BullMQ worker запущен

---

### 5. База данных недоступна

**Симптомы:**
- Все запросы к БД падают
- Supabase Dashboard показывает ошибки
- Connection timeout errors

**Действия:**

1. **Проверить статус Supabase:**
   - https://status.supabase.com
   - Проверить регион вашего проекта

2. **Проверить credentials:**
   - SUPABASE_URL правильный
   - SUPABASE_SERVICE_ROLE_KEY не истек

3. **Проверить лимиты:**
   - Supabase Dashboard → Settings → Usage
   - Проверить не превышены ли лимиты (если free tier)

4. **Временные меры:**
   - Кэшировать критические данные
   - Отключить функции зависимые от БД
   - Показать maintenance message

5. **Если проблема с Supabase:**
   - Дождаться восстановления (обычно быстро)
   - Или переключиться на backup (если настроен)

---

## ⚠️ Важные инциденты

### 6. Медленные запросы

**Симптомы:**
- Response time > 5 секунд
- Timeout errors
- Пользователи жалуются на медленную работу

**Действия:**

1. **Проверить Sentry Performance:**
   - Sentry → Performance
   - Найти медленные транзакции
   - Анализировать bottleneck

2. **Проверить медленные запросы БД:**
   - Supabase Dashboard → Database → Query Performance
   - Найти медленные queries
   - Оптимизировать или добавить индексы

3. **Проверить внешние API:**
   - OpenRouter latency
   - Kommo API latency
   - Redis latency

4. **Оптимизация:**
   - Добавить кэширование
   - Оптимизировать запросы
   - Увеличить timeout для внешних API

---

### 7. Превышение лимитов API

**Симптомы:**
- OpenRouter rate limit errors
- Upstash quota exceeded
- Supabase rate limit

**Действия:**

1. **OpenRouter:**
   - Проверить usage в Dashboard
   - Обновить план если нужно
   - Добавить rate limiting на нашей стороне

2. **Upstash:**
   - Проверить usage в Dashboard
   - Обновить план
   - Оптимизировать использование Redis

3. **Supabase:**
   - Проверить usage в Dashboard
   - Обновить план
   - Оптимизировать запросы

---

## 📞 Контакты и эскалация

### Команда

**DevOps Lead:**
- Email: [ваш email]
- Slack: @devops-lead

**Backend Lead:**
- Email: [backend email]
- Slack: @backend-lead

**On-Call:**
- Primary: @devops-lead
- Secondary: @backend-lead

### Эскалация

1. **Уровень 1:** Попытка исправить самостоятельно (15 минут)
2. **Уровень 2:** Привлечь команду в Slack (30 минут)
3. **Уровень 3:** Эскалировать менеджменту (1 час)

---

## 🔧 Полезные команды

### Проверка статуса

```bash
# Health check
curl https://your-app.vercel.app/api/health

# Worker health
curl https://your-worker.railway.app/health

# Redis
redis-cli -u $REDIS_URL ping

# Проверка переменных окружения в Vercel
vercel env ls
```

### Логи

```bash
# Vercel logs (через CLI)
vercel logs [deployment-url]

# Railway logs
railway logs

# GitHub Actions logs
gh run view [run-id]
```

### Откат

```bash
# Vercel
vercel rollback [deployment-url]

# Railway
railway rollback
```

---

## 📋 Чеклист при инциденте

- [ ] Определить масштаб проблемы (кто затронут?)
- [ ] Проверить статус всех сервисов
- [ ] Проверить логи и ошибки
- [ ] Попытаться воспроизвести проблему
- [ ] Применить быстрый фикс если возможно
- [ ] Уведомить команду о проблеме
- [ ] Задокументировать инцидент
- [ ] Провести post-mortem после решения

---

**Последнее обновление:** 2025-01-XX  
**Ответственный:** DevOps Team

