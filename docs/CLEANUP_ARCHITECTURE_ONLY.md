# Очистка ТОЛЬКО архитектуры платформы

> **Дата создания:** 2025-11-20  
> **ВАЖНО:** Удаляем ТОЛЬКО архитектурные компоненты платформы, НЕ трогаем название проекта, брендинг, описание!

---

## ✅ ЧТО НЕ ТРОГАЕМ (брендинг и описание проекта)

- ❌ **README.md** - НЕ ТРОГАТЬ (это описание проекта)
- ❌ **package.json name** - НЕ ТРОГАТЬ (это название проекта)
- ❌ **app/layout.tsx метаданные** - НЕ ТРОГАТЬ (это брендинг)
- ❌ **Документация про проект** - НЕ ТРОГАТЬ
- ❌ **Упоминания названия проекта** - НЕ ТРОГАТЬ

---

## 🎯 ЧТО УДАЛЯЕМ (только архитектура платформы)

### 1. Компоненты агентов (архитектура)

**Удалить:**
- `components/features/agents/` - все компоненты агентов
- `app/manage/[tenantId]/ai-agents/` - страницы агентов
- `app/api/agents/` - API endpoints агентов

**Почему:** Это архитектурные компоненты платформы, которые не нужны в новом сервисе

---

### 2. Репозитории агентов (архитектура)

**Удалить:**
- `lib/repositories/agents.ts`
- `lib/repositories/agent-assets.ts`
- `lib/repositories/agent-sequences.ts`
- `lib/repositories/agent-pipeline-settings.ts`

**Почему:** Это архитектурные репозитории для работы с агентами

---

### 3. Сервисы агентов (архитектура)

**Удалить:**
- `lib/services/agents.ts`
- `lib/services/agent-actions.ts`
- `lib/services/agent-context-builder.ts`
- `lib/services/agent-email-handler.ts`
- `lib/services/agent-memory.ts`

**Почему:** Это архитектурные сервисы для бизнес-логики агентов

---

### 4. Валидация агентов (архитектура)

**Удалить:**
- `lib/validation/schemas/agent.ts`

**Почему:** Это схемы валидации для агентов

---

### 5. GraphQL схема (архитектура)

**Удалить или очистить:**
- `lib/graphql/schema.ts` - если GraphQL не нужен

**Почему:** Описывает старую архитектуру (Agent, Knowledge Base)

---

### 6. Комментарии про архитектурные задачи (архитектура)

**Очистить комментарии "Задача X.X" из:**
- `lib/repositories/agents.ts` - комментарии про фильтры агентов
- `lib/services/agents.ts` - комментарии про агентов
- `lib/repositories/knowledge-base.ts` - если не нужна база знаний
- `lib/services/webhook-processor.ts` - комментарии про задачи (если не нужны)
- `lib/services/sequences.ts` - если не нужны последовательности
- `lib/services/rule-engine.ts` - если не нужен rule engine
- `lib/repositories/triggers.ts` - если не нужны триггеры
- `lib/repositories/agent-sequences.ts` - удалить вместе с репозиторием

**Почему:** Это комментарии про архитектурные задачи, которые описывают старую логику

---

### 7. Таблицы БД агентов (архитектура)

**Удалить из миграций/схемы:**
- `agents` - таблица агентов
- `agent_assets` - ресурсы агентов
- `agent_sequences` - последовательности
- `agent_sequence_steps` - шаги последовательностей
- `agent_channels` - каналы
- `agent_pipeline_settings` - настройки пайплайнов
- `agent_conversations` - разговоры
- `agent_memory` - память агентов

**Почему:** Это архитектурные таблицы для агентов

---

### 8. API endpoints агентов (архитектура)

**Удалить:**
- `app/api/agents/` - все endpoints агентов

**Почему:** Это архитектурные API endpoints

---

## 📋 Чеклист очистки архитектуры

### Компоненты и UI
- [ ] Удалить `components/features/agents/`
- [ ] Удалить `app/manage/[tenantId]/ai-agents/`

### Репозитории
- [ ] Удалить `lib/repositories/agents.ts`
- [ ] Удалить `lib/repositories/agent-assets.ts`
- [ ] Удалить `lib/repositories/agent-sequences.ts`
- [ ] Удалить `lib/repositories/agent-pipeline-settings.ts`

### Сервисы
- [ ] Удалить `lib/services/agents.ts`
- [ ] Удалить `lib/services/agent-actions.ts`
- [ ] Удалить `lib/services/agent-context-builder.ts`
- [ ] Удалить `lib/services/agent-email-handler.ts`
- [ ] Удалить `lib/services/agent-memory.ts`

### Валидация
- [ ] Удалить `lib/validation/schemas/agent.ts`

### API
- [ ] Удалить `app/api/agents/`

### GraphQL
- [ ] Удалить или очистить `lib/graphql/schema.ts` (если не нужен)

### Комментарии
- [ ] Удалить комментарии "Задача X.X" из кода (только архитектурные)

### База данных
- [ ] Удалить таблицы агентов из миграций (если не нужны)

---

## ⚠️ ВАЖНО: Что НЕ удаляем

- ✅ README.md - описание проекта
- ✅ package.json name - название проекта
- ✅ app/layout.tsx - метаданные и брендинг
- ✅ Документация про проект
- ✅ Упоминания названия проекта в коде
- ✅ Компоненты UI (Button, Input и т.д.) - они переиспользуемые
- ✅ Инфраструктура (auth, security, rate-limit) - она переиспользуемая
- ✅ Базовые репозитории (users, organizations) - они переиспользуемые

---

**Дата создания:** 2025-11-20  
**Версия:** 1.0

