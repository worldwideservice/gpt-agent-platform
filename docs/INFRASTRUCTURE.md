# 🏗️ Инфраструктура проекта

> Документ описывает архитектуру и инфраструктуру GPT Agent Platform

## 📊 Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                      GitHub Repository                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │          CI/CD Pipeline (GitHub Actions)            │   │
│  │  • Lint & Type Check                                 │   │
│  │  • E2E Tests                                        │   │
│  │  • Security Scan                                    │   │
│  │  • Build Check                                      │   │
│  └─────────────────────────────────────────────────────┘   │
└───────────────────────┬─────────────────────────────────────┘
                         │
        ┌────────────────┴────────────────┐
        │                                  │
        ▼                                  ▼
┌──────────────┐                  ┌──────────────┐
│   Vercel     │                  │   Railway    │
│  (Frontend)  │                  │   (Worker)   │
│              │                  │              │
│ Next.js App  │                  │ BullMQ       │
│ API Routes   │                  │ Worker      │
└──────┬───────┘                  └──────┬───────┘
       │                                  │
       └──────────────┬──────────────────┘
                      ▼
         ┌─────────────────────┐
         │      Supabase        │
         │  ┌────────────────┐ │
         │  │  PostgreSQL    │ │
         │  │  + pgvector    │ │
         │  │  + RLS         │ │
         │  └────────────────┘ │
         │  ┌────────────────┐ │
         │  │   Storage     │ │
         │  └────────────────┘ │
         │  ┌────────────────┐ │
         │  │   Auth        │ │
         │  └────────────────┘ │
         └─────────────────────┘
                      │
         ┌────────────┴────────────┐
         │                         │
         ▼                         ▼
┌──────────────┐          ┌──────────────┐
│   Upstash    │          │    Sentry    │
│   (Redis)    │          │ (Monitoring) │
└──────────────┘          └──────────────┘
         │
         ▼
┌──────────────┐
│  OpenRouter  │
│   (LLM API)  │
└──────────────┘
```

---

## 🛠️ Компоненты инфраструктуры

### 1. Frontend & API (Vercel)

**Платформа:** Vercel  
**URL Production:** https://gpt-agent-kwid-1i1j7zlgl-world-wide-services-62780b79.vercel.app

**Технологии:**
- Next.js 14 (App Router)
- React 18
- TypeScript
- Server Components
- API Routes

**Настройки:**
- Region: `fra1` (Frankfurt)
- Build Command: `npm run build`
- Install Command: `npm ci`
- Node Version: 20.x

**Переменные окружения:**
- Управляются через Vercel Dashboard
- См. `docs/SECRETS.md` для деталей

**Мониторинг:**
- Vercel Analytics
- Health Check: `/api/health`
- Sentry для ошибок

---

### 2. Worker Service (BullMQ)

**Статус:** 🚧 Требует деплоя

**Планируемая платформа:** Railway / Render / Fly.io

**Технологии:**
- Node.js 20
- BullMQ
- TypeScript

**Задачи:**
- Обработка файлов для базы знаний
- Генерация embeddings
- Синхронизация с CRM
- Обработка webhooks

**Требования:**
- Подключение к Redis (Upstash)
- Подключение к Supabase
- Доступ к OpenRouter API

**Мониторинг:**
- Health checks для worker процессов
- Логирование в Sentry
- Метрики очередей

---

### 3. База данных (Supabase)

**Платформа:** Supabase Cloud  
**URL:** https://rpzchsgutabxeabbnwas.supabase.co

**Компоненты:**

#### PostgreSQL
- Версия: PostgreSQL 15
- Регион: Определяется в Supabase Dashboard

#### Extensions:
- `pgvector` - векторные операции для RAG
- `uuid-ossp` - генерация UUID
- `pg_trgm` - полнотекстовый поиск

#### Row Level Security (RLS)
- Все таблицы защищены RLS политиками
- Пользователи видят только данные своей организации

#### Storage
- Bucket для файлов базы знаний
- Автоматическая обработка загруженных файлов

#### Auth
- Email/Password авторизация
- NextAuth интеграция
- Мультиарендность (organizations)

**Бэкапы:**
- Автоматические ежедневные бэкапы
- Point-in-time recovery (если включено)

**Мониторинг:**
- Supabase Dashboard
- Database logs через Supabase Dashboard

---

### 4. Redis (Upstash)

**Платформа:** Upstash Cloud  
**URL:** https://usw1-merry-term-40416.upstash.io

**Использование:**
- Очереди задач (BullMQ)
- Rate limiting
- Кэширование
- Session storage

**Настройки:**
- Region: `usw1` (US West)
- Type: Serverless Redis

**Мониторинг:**
- Upstash Dashboard
- Метрики использования через API

---

### 5. AI Provider (OpenRouter)

**Платформа:** OpenRouter  
**URL:** https://openrouter.ai

**Использование:**
- LLM модели (100+ моделей)
- Embeddings (опционально)
- Токенизация

**Модели:**
- GPT-4, GPT-4 Turbo
- Claude (Anthropic)
- Llama 2/3
- И другие модели через OpenRouter

**Мониторинг:**
- OpenRouter Dashboard для usage
- Логирование токенов в БД

---

### 6. Мониторинг (Sentry)

**Платформа:** Sentry Cloud

**Отслеживание:**
- JavaScript ошибки
- Server-side ошибки
- Performance monitoring
- Transaction tracking
- Replay sessions (10% ошибок)

**Настройки:**
- Sample rate: 10% в production
- Replay: только при ошибках

---

## 🔗 Зависимости между сервисами

### Frontend → Supabase
- Авторизация пользователей
- Хранение данных (agents, knowledge base)
- RLS для безопасности

### Frontend → Upstash Redis
- Rate limiting
- Кэширование

### Frontend → OpenRouter
- Генерация ответов агентов
- Embeddings (опционально)

### Worker → Supabase
- Чтение задач из очереди
- Запись результатов
- Обработка файлов

### Worker → Upstash Redis
- Очереди задач (BullMQ)
- Rate limiting

### Worker → OpenRouter
- Генерация embeddings
- LLM вызовы

---

## 🌐 Порты и Endpoints

### Frontend (Vercel)
- Production: `https://gpt-agent-kwid-1i1j7zlgl-world-wide-services-62780b79.vercel.app`
- Health Check: `/api/health`
- API Routes: `/api/*`
- Swagger Docs: `/api-docs`

### Worker (планируется)
- Port: определяется платформой
- Health Check: `/health`

### Supabase
- REST API: `https://rpzchsgutabxeabbnwas.supabase.co/rest/v1`
- Realtime: `wss://rpzchsgutabxeabbnwas.supabase.co/realtime/v1`
- Storage: `https://rpzchsgutabxeabbnwas.supabase.co/storage/v1`

### Upstash Redis
- REST URL: `https://usw1-merry-term-40416.upstash.io`
- Redis Port: определяется платформой

---

## 🔒 Безопасность

### Аутентификация
- NextAuth для сессий
- Supabase Auth для пользователей
- JWT токены

### Шифрование
- CRM токены шифруются (AES-256)
- `ENCRYPTION_KEY` в секретах

### Защита данных
- Row Level Security (RLS) в Supabase
- Rate limiting для API
- Security headers в Next.js

### Секреты
- Хранятся в Vercel Secrets
- НЕ коммитятся в репозиторий
- См. `docs/SECRETS.md`

---

## 📊 Мониторинг и метрики

### Application Metrics
- Error rate (Sentry)
- Response times (Vercel Analytics)
- Request count
- User sessions

### Infrastructure Metrics
- Database performance (Supabase)
- Redis usage (Upstash)
- Worker queue depth
- API rate limit hits

### Business Metrics
- Token usage (OpenRouter)
- Agent responses count
- Knowledge base size
- Active users

---

## 🚀 Деплоймент

### Frontend
- Автоматический через GitHub Actions
- Deploy на каждое обновление `main`
- Preview деплои для PR

### Worker
- Ручной деплой (пока)
- Планируется автоматизация

### Database Migrations
- Через Supabase Dashboard
- Или через Supabase CLI
- Версионирование через `schema_migrations`

---

## 🔄 Disaster Recovery

### База данных
- Автоматические бэкапы Supabase
- Point-in-time recovery (если включено)
- Экспорт данных через Supabase Dashboard

### Код
- Git репозиторий как основной backup
- GitHub Actions артефакты

### Секреты
- Хранятся в Vercel Secrets
- Резервные копии в password manager

---

## 📝 Следующие шаги

1. Настроить автоматический деплой Worker
2. Настроить Infrastructure as Code (Terraform)
3. Улучшить мониторинг с дашбордами
4. Настроить автоматические алерты

---

**Последнее обновление:** 2025-01-XX  
**Версия инфраструктуры:** 1.0

