# Этап 4: Архитектура и база данных

> **Дата анализа:** 2025-11-20  
> **Статус:** ✅ Полный анализ завершен

---

## 📋 Содержание

1. [Структура проекта](#структура-проекта)
2. [Архитектура приложения](#архитектура-приложения)
3. [База данных](#база-данных)
4. [Репозитории](#репозитории)
5. [Сервисы](#сервисы)
6. [Типы и интерфейсы](#типы-и-интерфейсы)
7. [Индексы и оптимизация](#индексы-и-оптимизация)
8. [Расширения PostgreSQL](#расширения-postgresql)
9. [Рекомендации по переиспользованию](#рекомендации-по-переиспользованию)

---

## Структура проекта

### Основные директории

```
.
├── app/                    # Next.js App Router
│   ├── (auth)/            # Группа маршрутов аутентификации
│   ├── api/               # API routes
│   ├── manage/            # Защищенные страницы управления
│   ├── docs/              # Публичная документация
│   └── ...
├── components/            # React компоненты
│   ├── ui/                # Базовые UI компоненты (shadcn/ui)
│   ├── features/          # Feature-специфичные компоненты
│   ├── layout/            # Layout компоненты
│   └── ...
├── lib/                   # Утилиты и сервисы
│   ├── repositories/      # Репозитории для работы с БД
│   ├── services/          # Бизнес-логика и сервисы
│   ├── supabase/          # Supabase клиенты
│   ├── auth/              # Аутентификация
│   ├── crypto/            # Шифрование
│   ├── security/          # Безопасность (CSRF)
│   ├── rate-limit/        # Rate limiting
│   └── ...
├── types/                 # TypeScript типы
│   ├── supabase.ts        # Автогенерированные типы БД
│   ├── user.ts            # Типы пользователей
│   └── ...
├── supabase/              # База данных
│   ├── migrations/        # SQL миграции
│   ├── schema.sql         # Основная схема
│   └── config.toml        # Конфигурация Supabase
├── services/              # Background сервисы
│   ├── api/               # Fastify API сервис
│   └── worker/            # BullMQ Worker
└── tests/                 # Тесты
```

### Архитектурные паттерны

**1. Repository Pattern**
- Все операции с БД через репозитории в `lib/repositories/`
- Изоляция логики доступа к данным
- Легкое тестирование и мокирование

**2. Service Layer**
- Бизнес-логика в `lib/services/`
- Репозитории используются сервисами
- Разделение ответственности

**3. Next.js App Router**
- Server Components по умолчанию
- Client Components только при необходимости
- API Routes для backend endpoints

**4. Type Safety**
- Автогенерированные типы из Supabase
- Строгая типизация TypeScript
- Валидация через Zod

### Что переиспользовать
✅ **Переиспользовать:**
- Структуру папок (app/, lib/, types/)
- Repository Pattern
- Service Layer
- Разделение на features/components/layout

⚠️ **Адаптировать:**
- API routes под новый проект
- Компоненты под новую логику

---

## Архитектура приложения

### Next.js App Router

**Структура маршрутов:**
- `app/(auth)/` - Группа аутентификации (login, register, reset-password)
- `app/api/` - API endpoints
- `app/manage/[tenantId]/` - Защищенные страницы управления
- `app/docs/` - Публичная документация
- `app/pricing/` - Страница тарифов

**Особенности:**
- Server Components по умолчанию
- Client Components с `'use client'`
- Layout компоненты для общих оберток
- Loading и Error boundaries

### API Routes

**Структура:**
```
app/api/
├── auth/              # Аутентификация
├── agents/            # Управление агентами
├── manage/[tenantId]/ # Tenant-specific endpoints
├── integrations/      # CRM интеграции
├── knowledge-base/    # База знаний
└── ...
```

**Паттерны:**
- RESTful API
- Валидация через Zod
- Обработка ошибок
- Rate limiting через middleware

### Что переиспользовать
✅ **Переиспользовать:**
- Структуру App Router
- Паттерны API routes
- Server/Client Components разделение

⚠️ **Адаптировать:**
- Маршруты под новый проект
- API endpoints под новую логику

---

## База данных

### Supabase (PostgreSQL)

**Основные таблицы:**

**1. Организации и пользователи:**
- `organizations` - Организации (multi-tenant)
- `users` - Пользователи
- `organization_members` - Членство в организациях
- `organization_invites` - Приглашения
- `password_resets` - Сброс паролей

**2. Агенты:**
- `agents` - AI агенты
- `agent_assets` - Ресурсы агентов
- `agent_sequences` - Последовательности действий
- `agent_sequence_steps` - Шаги последовательностей
- `agent_channels` - Каналы коммуникации
- `agent_pipeline_settings` - Настройки пайплайнов
- `agent_conversations` - Разговоры с агентами
- `agent_memory` - Память агентов

**3. База знаний:**
- `knowledge_base_categories` - Категории
- `knowledge_base_articles` - Статьи
- `knowledge_chunks` - Chunks с embeddings
- `documents` - Загруженные документы
- `document_chunks` - Chunks документов с embeddings

**4. CRM интеграции:**
- `crm_connections` - Подключения к CRM
- `crm_credentials` - Учетные данные (зашифрованы)
- `crm_pipelines` - Воронки продаж
- `crm_pipeline_stages` - Этапы воронок
- `oauth_states` - OAuth состояния

**5. Подписки и биллинг:**
- `subscriptions` - Подписки организаций
- `billing_history` - История платежей
- `usage_daily` - Ежедневное использование

**6. Другие:**
- `notifications` - Уведомления
- `conversations` - Разговоры
- `webhook_events` - События webhooks
- `automation_rules` - Правила автоматизации
- `rule_executions` - Выполнения правил

### Миграции

**Всего миграций:** 30+ файлов

**Ключевые миграции:**
- `000_init_migration_system.sql` - Система миграций
- `20250117000000_create_knowledge_base.sql` - База знаний
- `20250119_paddle_integration.sql` - Интеграция Paddle
- `20251116090000_create_notifications.sql` - Уведомления
- `20251116140000_add_performance_indexes.sql` - Индексы производительности
- `enable_rls_policies.sql` - RLS политики

**Паттерн миграций:**
- Версионирование через дату: `YYYYMMDDHHMMSS_description.sql`
- Идемпотентность (IF NOT EXISTS)
- Откат через отдельные миграции

### Что переиспользовать
✅ **Переиспользовать:**
- Структуру таблиц (organizations, users, organization_members)
- Подход к миграциям
- RLS политики
- Триггеры для updated_at

⚠️ **Адаптировать:**
- Таблицы агентов (если не нужны)
- CRM таблицы (если другая интеграция)
- Таблицы подписок (если другая модель)

---

## Репозитории

### Паттерн Repository

**Расположение:** `lib/repositories/`

**Основные репозитории:**

1. **users.ts** - UserRepository
   - Поиск пользователей
   - Создание пользователей
   - Обновление паролей
   - Определение тарифа

2. **organizations.ts** - Организации
   - Создание организаций
   - Получение организаций пользователя
   - Установка дефолтной организации

3. **agents.ts** - Агенты
   - CRUD операции
   - Фильтрация и поиск
   - Статистика активности
   - Кэширование

4. **knowledge-base.ts** - База знаний
   - Категории и статьи
   - CRUD операции
   - Поиск

5. **knowledge-search.ts** - Поиск по базе знаний
   - Векторный поиск
   - Semantic search
   - Фильтрация

6. **document-storage.ts** - Документы
   - Загрузка документов
   - Chunking
   - Embeddings

7. **crm-connection.ts** - CRM подключения
   - OAuth flow
   - Управление токенами
   - Шифрование credentials

8. **conversations.ts** - Разговоры
   - Создание разговоров
   - Сообщения
   - История

9. **notifications.ts** - Уведомления
   - Создание уведомлений
   - Отметка прочитанными
   - Получение списка

10. **subscriptions.ts** - Подписки
    - Получение подписки
    - Обновление статуса
    - Проверка валидности

### Паттерны реализации

**1. Использование Supabase Service Role Client:**
```typescript
import { getSupabaseServiceRoleClient } from '@/lib/supabase/admin'

const supabase = getSupabaseServiceRoleClient()
```

**2. Кэширование:**
- Использование Redis/Upstash для кэширования
- Инвалидация кэша при обновлениях

**3. Обработка ошибок:**
- Логирование через logger
- Типизированные ошибки
- Graceful degradation

**4. Валидация:**
- Zod схемы для валидации входных данных
- TypeScript для типизации

### Что переиспользовать
✅ **Переиспользовать:**
- Паттерн Repository
- Структуру репозиториев
- Использование Supabase клиента
- Подход к кэшированию
- Обработку ошибок

⚠️ **Адаптировать:**
- Методы под новую логику
- Таблицы под новую схему

---

## Сервисы

### Расположение: `lib/services/`

**Основные сервисы:**

1. **ai/** - AI интеграции
   - `openrouter.client.ts` - OpenRouter клиент
   - `openrouter-resolver.ts` - Резолвер клиента
   - `configuration-resolver.ts` - Резолвер конфигурации

2. **llm.ts** - LLM сервис
   - Генерация ответов
   - Построение system prompt
   - Интеграция с базой знаний

3. **embeddings.ts** - Embeddings
   - Генерация embeddings
   - Chunking документов
   - Batch обработка

4. **agents.ts** - Сервис агентов
   - Бизнес-логика агентов
   - Обработка действий

5. **knowledge.ts** - База знаний
   - Обработка документов
   - Векторный поиск
   - Интеграция с агентами

6. **integrations.ts** - Интеграции
   - Управление интеграциями
   - Синхронизация данных

7. **billing.ts** - Биллинг
   - Проверка подписок
   - Обработка платежей

8. **webhook-processor.ts** - Webhooks
   - Обработка webhook событий
   - Валидация подписей

9. **rule-engine.ts** - Правила автоматизации
   - Выполнение правил
   - Обработка триггеров

10. **sequences.ts** - Последовательности
    - Выполнение последовательностей
    - Управление шагами

### Паттерны реализации

**1. Разделение ответственности:**
- Сервисы содержат бизнес-логику
- Репозитории - доступ к данным
- API routes - HTTP обработка

**2. Dependency Injection:**
- Сервисы получают зависимости через параметры
- Легкое тестирование

**3. Error Handling:**
- Типизированные ошибки
- Логирование
- Graceful degradation

### Что переиспользовать
✅ **Переиспользовать:**
- Структуру сервисов
- Паттерны разделения ответственности
- Подход к обработке ошибок

⚠️ **Адаптировать:**
- Бизнес-логику под новый проект
- Интеграции под новые сервисы

---

## Типы и интерфейсы

### Расположение: `types/`

**Основные файлы:**

1. **supabase.ts** - Автогенерированные типы
   - Типы всех таблиц
   - Row, Insert, Update типы
   - Relationships

2. **user.ts** - Типы пользователей
   - User, DatabaseUser
   - Subscription, Plan
   - UserTier

3. **crm.ts** - Типы CRM
   - CRMConnection
   - Pipeline, Stage
   - WebhookEvent

4. **kommo.ts** - Типы Kommo
   - Специфичные типы для Kommo API

5. **next-auth.d.ts** - Расширения NextAuth
   - Session с orgId
   - JWT с orgId

### Паттерны типизации

**1. Автогенерация:**
- Типы Supabase генерируются автоматически
- Синхронизация с БД

**2. Расширение типов:**
- Модульное расширение через `declare module`
- Сохранение типов библиотек

**3. Валидация:**
- Zod схемы для runtime валидации
- TypeScript для compile-time проверки

### Что переиспользовать
✅ **Переиспользовать:**
- Подход к типизации
- Расширение типов NextAuth
- Использование автогенерированных типов

⚠️ **Адаптировать:**
- Типы под новую схему БД
- Типы под новую логику

---

## Индексы и оптимизация

### Индексы производительности

**Всего индексов:** 189+ (по grep)

**Ключевые индексы:**

**1. Agents:**
- `idx_agents_org_status_created` - Composite индекс (org_id, status, created_at)
- `idx_agents_org_model` - Фильтрация по модели
- `idx_agents_name_trgm` - Full-text search (GIN)
- `idx_agents_org_active` - Partial индекс для активных
- `idx_agents_org_activity` - Сортировка по активности

**2. Knowledge Base:**
- `idx_documents_org_id` - Поиск по организации
- `idx_documents_status` - Фильтрация по статусу
- `idx_documents_tags` - GIN индекс для массивов
- `idx_document_chunks_embedding` - HNSW для векторного поиска

**3. Activity Metrics:**
- `idx_activity_metrics_org_date_desc` - Агрегация метрик
- `idx_activity_metrics_agent_date` - Фильтрация по агенту

**4. Другие:**
- `idx_org_members_user` - Поиск организаций пользователя
- `idx_conversations_org_created` - Разговоры по организации
- `idx_webhook_events_status` - Обработка pending events

### Оптимизации

**1. Vector Search:**
- HNSW индекс для embeddings
- Функция `match_knowledge_chunks` для поиска
- Cosine similarity

**2. Full-text Search:**
- pg_trgm расширение
- GIN индексы для триграмм

**3. Partial Indexes:**
- Индексы только для активных записей
- Уменьшение размера индексов

**4. Composite Indexes:**
- Оптимизация запросов с несколькими фильтрами
- Покрытие сортировки

### Что переиспользовать
✅ **Переиспользовать:**
- Подход к индексации
- Composite индексы для частых запросов
- Partial индексы для фильтрации
- HNSW для векторного поиска

⚠️ **Адаптировать:**
- Индексы под новые запросы
- Оптимизации под новую схему

---

## Расширения PostgreSQL

### Используемые расширения

**1. uuid-ossp:**
- Генерация UUID
- `gen_random_uuid()`

**2. pgcrypto:**
- Криптографические функции
- Хеширование

**3. vector (pgvector):**
- Векторные типы данных
- Векторный поиск
- Embeddings storage

**4. pg_trgm:**
- Триграммный поиск
- Full-text search
- GIN индексы

### Функции БД

**1. match_knowledge_chunks:**
- Векторный поиск по embeddings
- Фильтрация по org_id, agent_id, article_id
- Cosine similarity

**2. trigger_set_timestamp:**
- Автоматическое обновление `updated_at`
- Используется в триггерах

**3. update_documents_updated_at:**
- Специфичная функция для documents
- Триггер для updated_at

### Что переиспользовать
✅ **Переиспользовать:**
- Расширения (uuid-ossp, pgcrypto, vector)
- Подход к функциям БД
- Триггеры для updated_at

⚠️ **Адаптировать:**
- Функции под новую логику
- Триггеры под новые таблицы

---

## Рекомендации по переиспользованию

### ✅ Обязательно переиспользовать

1. **Структура проекта**
   - Разделение на app/, lib/, types/
   - Repository Pattern
   - Service Layer

2. **База данных**
   - Структура organizations/users/organization_members
   - RLS политики
   - Миграции
   - Триггеры для updated_at

3. **Репозитории**
   - Паттерн Repository
   - Использование Supabase клиента
   - Кэширование
   - Обработка ошибок

4. **Типизация**
   - Автогенерированные типы Supabase
   - Расширение типов NextAuth
   - Zod валидация

5. **Индексы**
   - Подход к индексации
   - Composite индексы
   - Partial индексы
   - HNSW для векторов

6. **Расширения PostgreSQL**
   - uuid-ossp, pgcrypto, vector
   - pg_trgm для поиска

### ⚠️ Адаптировать под новый проект

1. **Таблицы БД**
   - Удалить таблицы агентов (если не нужны)
   - Адаптировать CRM таблицы
   - Упростить подписки (если другая модель)

2. **Репозитории**
   - Удалить репозитории агентов
   - Адаптировать под новую схему
   - Добавить новые репозитории

3. **Сервисы**
   - Удалить сервисы агентов
   - Адаптировать под новую логику
   - Добавить новые сервисы

4. **API Routes**
   - Удалить endpoints агентов
   - Адаптировать под новый проект
   - Добавить новые endpoints

5. **Компоненты**
   - Удалить компоненты агентов
   - Адаптировать под новую логику
   - Добавить новые компоненты

### ❌ Можно удалить

1. **Таблицы агентов:**
   - `agents`, `agent_assets`, `agent_sequences`
   - `agent_conversations`, `agent_memory`
   - Если не нужны AI агенты

2. **CRM таблицы:**
   - `crm_connections`, `crm_pipelines`
   - Если другая CRM интеграция

3. **База знаний:**
   - `knowledge_base_*`, `knowledge_chunks`
   - Если не нужна векторная база знаний

4. **Правила автоматизации:**
   - `automation_rules`, `rule_executions`
   - Если не нужна автоматизация

---

## Итоговая оценка

### Готовность к переиспользованию: **90%**

**Сильные стороны:**
- ✅ Отличная структура проекта
- ✅ Четкое разделение ответственности
- ✅ Production-ready решения
- ✅ Хорошая типизация
- ✅ Оптимизированная БД

**Что нужно адаптировать:**
- ⚠️ Удалить/адаптировать таблицы агентов
- ⚠️ Адаптировать CRM интеграцию
- ⚠️ Упростить подписки (если нужно)
- ⚠️ Адаптировать компоненты

**Рекомендации:**
1. Начать с базовой структуры (organizations, users)
2. Постепенно добавлять нужные таблицы
3. Переиспользовать репозитории и сервисы
4. Адаптировать под новую логику

---

**Дата создания:** 2025-11-20  
**Версия:** 1.0

