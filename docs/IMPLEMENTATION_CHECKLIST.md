# Чеклист реализации Kwid → Refine + Shadcn UI

## 📦 Установка зависимостей

### 1. Refine пакеты (ОБЯЗАТЕЛЬНО)
```bash
npm install @refinedev/core @refinedev/nextjs-router @refinedev/shadcn-ui
```

**Что это дает:**
- `@refinedev/core` - основной функционал Refine (CRUD, хуки, провайдеры)
- `@refinedev/nextjs-router` - интеграция с Next.js App Router
- `@refinedev/shadcn-ui` - готовые компоненты на базе Shadcn UI

### 2. Дополнительные зависимости для Refine
```bash
npm install react-hook-form @hookform/resolvers zod
```
**Для:** валидации форм (Refine использует react-hook-form)

### 3. Недостающие Shadcn UI компоненты

**Критически важные:**
```bash
npx shadcn@latest add accordion      # FAQ секции на странице Pricing
npx shadcn@latest add checkbox      # Массовые действия в таблицах
npx shadcn@latest add combobox      # Выбор полей сделок/контактов (50+ опций)
npx shadcn@latest add command       # Поиск в combobox
npx shadcn@latest add form          # Валидация форм
npx shadcn@latest add separator      # Визуальное разделение секций
npx shadcn@latest add sheet         # Фильтры (drawer/sidebar)
```

**Опциональные (но полезные):**
```bash
npx shadcn@latest add alert         # Уведомления об ошибках
npx shadcn@latest add skeleton      # Loading состояния
npx shadcn@latest add toast         # Toast уведомления (возможно уже есть)
```

---

## 🏗️ Структура файлов

### Создать следующие файлы:

1. **`lib/providers/data-provider.ts`** - Custom data provider для ваших API endpoints
2. **`lib/providers/auth-provider.ts`** - Интеграция с NextAuth
3. **`components/refine/resources.ts`** - Определение всех Refine resources
4. **`components/refine/providers.tsx`** - Конфигурация Refine providers
5. **`app/manage/[tenantId]/layout.tsx`** - Обновить для добавления Refine Provider

---

## 📋 По приоритетам реализации

### Приоритет 1 (Основной функционал)
1. ✅ Установить Refine + зависимости
2. ✅ Установить недостающие Shadcn компоненты
3. ✅ Создать data-provider.ts (подключение к существующим API)
4. ✅ Создать auth-provider.ts (NextAuth интеграция)
5. ✅ Создать resources.ts (определение ресурсов)
6. ✅ Настроить Refine Provider в layout

### Приоритет 2 (Базовые страницы)
7. ✅ Dashboard - виджеты метрик (кастомная страница, без Refine)
8. ✅ AI Agents List - таблица со списком агентов
9. ✅ AI Agents Create - форма создания
10. ✅ Knowledge Base Articles - List и Create

### Приоритет 3 (Сложные страницы)
11. ✅ AI Agents Edit - страница с 6 вкладками
12. ✅ Knowledge Base Categories - с иерархией
13. ✅ Test Chat - кастомная страница

### Приоритет 4 (Дополнительные страницы)
14. ✅ Account Settings
15. ✅ Pricing
16. ✅ Кастомные действия (copy, sync CRM)

---

## 🔍 Проверка текущих компонентов

### ✅ Уже есть:
- Button, Card, Input, Textarea, Badge, Select
- Table, Tabs, Dialog, Popover
- Switch, Progress, Scroll Area

### ❌ Отсутствуют (нужно добавить):
- **Accordion** - для FAQ на Pricing
- **Checkbox** - для массовых действий
- **Combobox** - для выбора полей (50+ опций)
- **Command** - поиск в combobox
- **Form** - валидация форм
- **Separator** - разделители
- **Sheet** - боковые панели (фильтры)

---

## 📝 Следующие шаги

1. **Установить зависимости** (npm install)
2. **Добавить Shadcn компоненты** (npx shadcn add ...)
3. **Создать базовую структуру Refine** (providers, resources)
4. **Реализовать первую страницу** (AI Agents List) как proof of concept
5. **Постепенно мигрировать остальные страницы**

---

## 💡 Важные замечания

1. **API Endpoints** - уже существуют, нужно просто подключить через data-provider
2. **Backend логика** - НЕ ТРОГАЕМ, только фронтенд
3. **Shadcn стили** - уже настроены в `components.json`
4. **Мультитенантность** - `[tenantId]` уже работает в роутинге

---

## 🎯 Цель

Минимальный код с максимальной функциональностью благодаря Refine:
- Автоматический CRUD
- Встроенные таблицы, формы, фильтры
- TypeScript типизация
- Shadcn UI компоненты для красивого дизайна





