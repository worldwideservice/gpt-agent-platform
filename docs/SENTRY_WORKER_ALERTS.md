# 🔔 Настройка алертов Sentry для Worker

**Дата создания:** 2025-01-26  
**Версия:** 1.0

---

## 📋 Обзор

Эта документация описывает, как настроить алерты в Sentry для мониторинга Worker сервиса.

---

## 🎯 Рекомендуемые алерты

### 1. Uptime Monitor для Worker Health Check

**Цель:** Мониторинг доступности Worker через health check endpoint.

**Предварительные требования:**

✅ **Worker имеет публичный URL:** `https://gpt-agent-platform-production.up.railway.app`

**Создано:** 2025-01-26  
**Статус:** ✅ Готово к настройке Uptime Monitor

**Настройка Uptime Monitor:**

1. Откройте: https://world-wide-services.sentry.io/issues/alerts/rules/
2. Нажмите "Create Alert"
3. Выберите "Uptime Monitor"
4. Нажмите "Set Conditions"

**Конфигурация:**

- **Project:** `javascript-nextjs` (или выберите подходящий проект)
- **Environment:** `production` (или выберите нужное окружение)
- **URL:** `https://gpt-agent-platform-production.up.railway.app/health`
  - ✅ **URL Worker:** `https://gpt-agent-platform-production.up.railway.app`
- **Method:** `GET`
- **Interval:** `Every 1 minute` (рекомендуется)
- **Timeout:** `10.00s` (рекомендуется)
- **Headers:** Не требуется (опционально)
- **Failure Tolerance:** `3` (создать issue после 3 последовательных неудач)
- **Recovery Tolerance:** `1` (решить issue после 1 успешной проверки)
- **Uptime rule name:** `Worker Health Check`
- **Owner:** Выберите команду или участника

**Ожидаемый ответ (успех):**
```json
{
  "status": "ok",
  "service": "worker",
  "timestamp": "2025-01-26T12:00:00.000Z",
  "uptime": 3600.5,
  "redis": {
    "connected": true,
    "error": null
  },
  "worker": {
    "concurrency": 5,
    "queueName": "agent-jobs",
    "jobsProcessing": 2
  }
}
```

**Ожидаемый ответ (Redis недоступен):**
```json
{
  "status": "degraded",
  "service": "worker",
  "redis": {
    "connected": false,
    "error": "Connection timeout"
  }
}
```

---

### 2. Alert для высокого процента ошибок Worker

**Цель:** Уведомление при высоком проценте ошибок обработки jobs.

**Настройка:**

1. Откройте: https://world-wide-services.sentry.io/issues/alerts/rules/
2. Нажмите "Create Alert"
3. Выберите "Number of Errors"
4. Нажмите "Set Conditions"

**Конфигурация:**

- **Project:** `javascript-nextjs` (или выберите подходящий проект)
- **Environment:** `production`
- **Condition:** 
  - When the number of errors in `javascript-nextjs` is **more than** `10` in `1 minute`
  - ИЛИ: When the number of errors in `javascript-nextjs` is **more than** `50` in `5 minutes`
- **Filter conditions:**
  - `logger` equals `worker` (если Worker отправляет ошибки с logger=worker)
  - ИЛИ: `tags[service]` equals `worker` (если Worker отправляет ошибки с тегом service=worker)

**Рекомендации:**
- Настройте порог в зависимости от ожидаемой нагрузки
- Используйте процент от общего количества jobs вместо абсолютного числа (если возможно)

---

### 3. Alert для проблем с производительностью Worker

**Цель:** Уведомление при деградации производительности обработки jobs.

**Настройка:**

1. Откройте: https://world-wide-services.sentry.io/issues/alerts/rules/
2. Нажмите "Create Alert"
3. Выберите "Performance" → "Duration"
4. Нажмите "Set Conditions"

**Конфигурация:**

- **Project:** `javascript-nextjs`
- **Environment:** `production`
- **Condition:**
  - When the p95 duration for transactions in `javascript-nextjs` is **more than** `5000ms` in `5 minutes`
  - ИЛИ: When the avg duration for transactions in `javascript-nextjs` is **more than** `3000ms` in `5 minutes`
- **Filter conditions:**
  - `transaction` contains `worker` (если Worker отправляет транзакции с именем, содержащим "worker")

---

## 🔍 Получение URL Worker из Railway

### Способ 1: Через Railway Dashboard

1. Откройте: https://railway.app
2. Выберите проект с Worker сервисом
3. Откройте Worker сервис
4. Перейдите в раздел "Settings" → "Networking"
5. Найдите "Public Domain" или создайте новый
6. Скопируйте URL (например: `worker-production.up.railway.app`)

### Способ 2: Через Railway CLI

```bash
# Установите Railway CLI (если еще не установлен)
# npm install -g @railway/cli

# Войдите в Railway
railway login

# Перейдите в проект
cd services/worker
railway link

# Получите URL
railway domain
```

### Способ 3: Из переменных окружения

Если Worker имеет публичный домен, он может быть доступен через переменную окружения или логи Railway.

---

## 📊 Интеграция метрик Worker с Sentry

### Вариант 1: Отправка метрик через Worker endpoints

Worker уже предоставляет метрики через `/metrics` endpoint. Можно настроить внешний мониторинг (например, Prometheus) для сбора этих метрик и отправки алертов.

### Вариант 2: Отправка событий в Sentry из Worker

Для более детального мониторинга можно интегрировать Sentry SDK в Worker:

1. Установить `@sentry/node` в Worker:
   ```bash
   cd services/worker
   npm install @sentry/node
   ```

2. Инициализировать Sentry в Worker:
   ```typescript
   import * as Sentry from '@sentry/node'
   
   Sentry.init({
     dsn: process.env.SENTRY_DSN,
     environment: process.env.NODE_ENV || 'production',
     tracesSampleRate: 1.0,
     beforeSend(event) {
       // Добавить теги для Worker
       event.tags = {
         ...event.tags,
         service: 'worker',
       }
       return event
     },
   })
   ```

3. Отправлять метрики в Sentry:
   ```typescript
   // При ошибке обработки job
   Sentry.captureException(error, {
     tags: {
       jobName: job.name,
       jobId: job.id,
     },
     contexts: {
       job: {
         name: job.name,
         id: job.id,
         data: job.data,
       },
     },
   })
   
   // При проблемах с Redis
   Sentry.captureMessage('Redis connection failed', {
     level: 'error',
     tags: {
       service: 'worker',
       component: 'redis',
     },
   })
   ```

---

## 🔔 Настройка уведомлений

### Email уведомления

1. Откройте настройки алерта в Sentry
2. Перейдите в раздел "Actions"
3. Добавьте "Send a notification via email"
4. Выберите получателей

### Slack уведомления

1. Интегрируйте Slack с Sentry:
   - Откройте: https://world-wide-services.sentry.io/settings/integrations/
   - Найдите "Slack" и настройте интеграцию
2. В настройках алерта добавьте "Send a Slack notification"
3. Выберите канал Slack

### Webhook уведомления

1. В настройках алерта добавьте "Send a webhook"
2. Укажите URL webhook endpoint
3. Настройте формат payload

---

## 📝 Примеры алертов

### Пример 1: Worker недоступен

**Триггер:** Uptime Monitor обнаружил 3 последовательных неудачных проверки health check.

**Действие:**
- Отправить email администраторам
- Отправить Slack уведомление в канал #alerts
- Создать issue в Sentry

### Пример 2: Высокий процент ошибок

**Триггер:** Более 10% jobs завершились с ошибкой за последние 5 минут.

**Действие:**
- Отправить email команде разработки
- Отправить Slack уведомление в канал #worker-alerts

### Пример 3: Redis недоступен

**Триггер:** Health check возвращает `status: "degraded"` и `redis.connected: false`.

**Действие:**
- Отправить критичное уведомление
- Создать issue в Sentry с high priority

---

## 🧪 Тестирование алертов

### Тест Uptime Monitor

1. Временно остановите Worker в Railway
2. Подождите 3 минуты (или настроенное время Failure Tolerance)
3. Проверьте, что алерт сработал
4. Запустите Worker обратно
5. Проверьте, что issue автоматически решился

### Тест алерта на ошибки

1. Добавьте тестовую задачу, которая всегда падает
2. Подождите, пока накопится достаточное количество ошибок
3. Проверьте, что алерт сработал
4. Удалите тестовую задачу

---

## 📚 Дополнительные ресурсы

- **Sentry Alerts:** https://world-wide-services.sentry.io/issues/alerts/rules/
- **Sentry Documentation:** https://docs.sentry.io/product/alerts/
- **Railway Dashboard:** https://railway.app
- **Worker Monitoring:** `docs/WORKER_MONITORING.md`

---

## ⚠️ Важные замечания

1. **URL Worker может меняться** при передеплое на Railway. Убедитесь, что обновляете Uptime Monitor при изменении URL.

2. **Настройка окружения:** Убедитесь, что выбрано правильное окружение (production/staging) для алертов.

3. **Пороги алертов:** Настройте пороги в зависимости от ожидаемой нагрузки. Слишком чувствительные алерты могут привести к "alert fatigue".

4. **Тестирование:** Всегда тестируйте алерты после настройки, чтобы убедиться, что они работают корректно.

---

**Последнее обновление:** 2025-01-26

