# 📧 Типы Email в проекте - Полное объяснение

**Дата:** 2025-01-26  
**Статус:** ✅ Актуальная информация

---

## 🎯 Два типа Email в проекте

### 1. ✅ Email через Kommo API (РЕАЛИЗОВАНО)
**Назначение:** Отправка писем клиентам **внутри сделки** через AI-агента

**Как работает:**
- Агент отправляет письмо клиенту через Kommo API
- Создается note типа `outgoing_email` или `mail_message` в сделке
- Kommo **РЕАЛЬНО отправляет письмо** клиенту через свою email-интеграцию
- Письмо доставляется клиенту на его email адрес
- Вся переписка сохраняется в истории сделки

**Где используется:**
- AI-агент отвечает на входящие письма клиента
- Отправка шаблонов писем при переходе сделки на этап
- Автоматические ответы на вопросы клиентов

**Реализация:**
```typescript
// lib/crm/kommo.ts
async sendEmailFromLead(leadId: number, emailData: {
  to: string[]
  subject: string
  html: string
  text?: string
}): Promise<unknown> {
  // Создает note типа mail_message
  // Kommo отправляет реальное письмо через свою email-интеграцию
}

// services/worker/src/tasks/index.ts
const sendKommoMessage = async (payload: {
  channel: 'email' | 'chat'
  message: { subject?: string; body: string }
}) => {
  // Использует note_type: 'outgoing_email'
  // Kommo отправляет реальное письмо клиенту
}
```

**Пример из реальной сделки:**
- Сделка #16049640
- Исходящие письма: "04.11.2025 15:17 Исходящее письмо от: Irakli Shukakidze кому: Akintola Oluwatosin"
- Входящие письма: "04.11.2025 15:20 Входящее письмо от: Akintola oluwatosin"
- Письма реально отправляются и приходят!

**Требования:**
- В Kommo аккаунте должна быть настроена email-интеграция (SMTP/email сервис)
- Компания настраивает это в настройках Kommo
- После настройки агент может отправлять письма через API

---

### 2. ⏳ Email для авторизации (НЕ НАСТРОЕНО)
**Назначение:** Системные письма для пользователей платформы

**Для чего нужен:**
- Восстановление пароля
- Подтверждение email при регистрации
- Уведомления о безопасности (новая авторизация, смена пароля)
- Двухфакторная аутентификация (если будет)

**Текущий статус:**
- ⚠️ Код есть только заглушки (`console.log`)
- ⚠️ SMTP сервис не настроен
- ⚠️ Переменные окружения не заполнены

**Где используется:**
```typescript
// lib/services/sequences.ts
const executeSendEmailStep = async (...) => {
  // ⚠️ ЗАГЛУШКА - только console.log
  console.log('Sequence: Sending email', {
    recipient: step.recipient,
    template: step.template,
  })
  return true
}

// lib/services/rule-engine.ts
const executeSendEmail = async (...) => {
  // ⚠️ ЗАГЛУШКА - только console.log
  console.log('Sending email:', {
    recipient: action.recipient,
    template: action.template,
  })
  return true
}
```

**Что нужно для реализации:**
1. Выбрать email-сервис:
   - **Resend** (рекомендуется) - простой, быстрый API
   - **SendGrid** - мощный, много функций
   - **SMTP** (Gmail, Outlook, etc.) - требует настройки SMTP

2. Установить зависимости:
```bash
npm install resend
# или
npm install @sendgrid/mail
# или
npm install nodemailer
```

3. Настроить переменные окружения:
```env
# Для Resend
RESEND_API_KEY=re_xxxxxxxxxxxxx

# Для SendGrid
SENDGRID_API_KEY=SG.xxxxxxxxxxxxx
FROM_EMAIL=noreply@yourdomain.com

# Для SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
```

4. Создать сервис отправки email:
```typescript
// lib/services/email.ts
import { Resend } from 'resend'

const resend = new Resend(process.env.RESEND_API_KEY)

export const sendEmail = async (to: string, subject: string, html: string) => {
  return await resend.emails.send({
    from: process.env.FROM_EMAIL || 'noreply@yourdomain.com',
    to,
    subject,
    html,
  })
}
```

5. Обновить заглушки:
```typescript
// lib/services/sequences.ts
const executeSendEmailStep = async (...) => {
  if (!step.template || !step.recipient) return false
  
  // ✅ РЕАЛЬНАЯ ОТПРАВКА
  await sendEmail(
    step.recipient,
    step.template.subject,
    step.template.html
  )
  
  return true
}
```

---

## 📊 Сравнительная таблица

| Параметр | Email через Kommo API | Email для авторизации |
|----------|----------------------|----------------------|
| **Назначение** | Письма клиентам в сделках | Системные письма пользователям |
| **Статус** | ✅ Реализовано и работает | ⏳ Не настроено (заглушки) |
| **Куда отправляется** | Клиентам через Kommo | Пользователям платформы |
| **Требует настройки** | Email-интеграция в Kommo | SMTP/Email сервис в проекте |
| **Кто настраивает** | Компания в Kommo | Разработчики проекта |
| **Примеры** | Ответы агента, шаблоны писем | Восстановление пароля, подтверждение email |

---

## 🔄 Синхронизация данных через Kommo API

**Когда пользователь подключает Kommo:**

1. **OAuth авторизация:**
   - Пользователь авторизуется через Kommo OAuth
   - Получаем access_token и refresh_token
   - Сохраняем в `crm_connections` таблицу

2. **Автоматическая синхронизация:**
   - После OAuth создается задача `crm:sync-pipelines`
   - Worker синхронизирует все данные из Kommo:
     - ✅ Воронки продаж (pipelines)
     - ✅ Этапы воронок (stages)
     - ✅ Поля сделок (custom fields)
     - ✅ Контакты
     - ✅ Сделки (частично)

3. **Что синхронизируется:**
```typescript
// services/worker/src/tasks/index.ts
const handleCrmSyncPipelines = async (payload: CrmSyncPipelinesJob) => {
  // Получаем все воронки
  const pipelines = await kommoApi.getPipelines()
  
  // Синхронизируем каждую воронку с этапами
  for (const pipeline of pipelines) {
    await syncPipelineToDatabase(pipeline)
  }
}
```

4. **Где хранятся данные:**
   - `crm_pipelines` - воронки
   - `crm_pipeline_stages` - этапы воронок
   - `crm_connections` - подключения к Kommo

---

## 📝 Примеры использования

### Пример 1: Агент отправляет письмо клиенту
```typescript
// lib/services/agent-email-handler.ts
await kommoApi.sendEmailFromLead(leadId, {
  to: [emailData.from],
  subject: responseSubject,
  html: formatEmailResponse(llmResponse.content),
  text: llmResponse.content,
})
// ✅ Письмо реально отправляется клиенту через Kommo
```

### Пример 2: Отправка системного письма (НЕ РАБОТАЕТ)
```typescript
// lib/services/sequences.ts
const executeSendEmailStep = async (...) => {
  // ⚠️ Только console.log, письмо НЕ отправляется
  console.log('Sequence: Sending email', {
    recipient: step.recipient,
  })
  return true
}
```

---

## ✅ Итог

1. **Email через Kommo API** - ✅ РАБОТАЕТ
   - Агент отправляет письма клиентам
   - Письма реально доставляются
   - Вся переписка в истории сделки

2. **Email для авторизации** - ⏳ НЕ НАСТРОЕНО
   - Нужен SMTP/Email сервис
   - Нужно реализовать отправку
   - Заменить заглушки на реальный код

3. **Синхронизация Kommo** - ✅ РАБОТАЕТ
   - Автоматически после OAuth
   - Подтягиваются воронки, этапы, поля
   - Данные сохраняются в БД

---

**Последнее обновление:** 2025-01-26  
**Автор:** AI Agent (после изучения реальной сделки #16049640)

