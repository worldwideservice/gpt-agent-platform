# 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА: Sidebar ссылки без tenant-id

**Дата:** 2025-01-26  
**Статус:** 🔴 **КРИТИЧНО - ТРЕБУЕТ НЕМЕДЛЕННОГО ИСПРАВЛЕНИЯ**

---

## Проблема

Все ссылки в sidebar генерируются БЕЗ tenant-id:
- `/ai-agents` вместо `/manage/44-makysm-holovatyi/ai-agents`
- `/test-chat` вместо `/manage/44-makysm-holovatyi/test-chat`
- `/knowledge-categories` вместо `/manage/44-makysm-holovatyi/knowledge-categories`
- И так далее...

**Результат:** При клике на любую ссылку в sidebar → 404 ошибка

---

## Анализ кода

### 1. `components/layout/Sidebar.tsx`

Функция `getNavigation` правильно генерирует href с tenant-id:
```typescript
const getNavigation = (tNav: any, tenantId?: string): NavSection[] => {
  const basePath = tenantId ? `/manage/${tenantId}` : ''
  
  return [
    {
      items: [
        {
          label: 'Агенты ИИ',
          href: tenantId ? `${basePath}/ai-agents` : '/ai-agents',
          // ...
        }
      ]
    }
  ]
}
```

И вызов правильный:
```typescript
const navigation = getNavigation(() => {}, tenantId)
```

### 2. `components/layout/HeaderWithSidebar.tsx`

tenantId передается в Sidebar:
```typescript
<Sidebar
  organizations={organizations}
  activeOrganizationId={activeOrganization?.id ?? session.user.orgId}
  tenantId={tenantId}  // ✅ Передается
  isOpen={isOpen}
/>
```

### 3. `app/manage/[tenantId]/layout.tsx`

tenantId передается в HeaderWithSidebar:
```typescript
<HeaderWithSidebar
  session={session}
  organizations={organizations}
  activeOrganization={activeOrganization}
  tenantId={tenantId}  // ✅ Передается
>
```

---

## Возможные причины

1. **Проблема с деплоем** - изменения не применены на продакшене
2. **Проблема с кэшированием** - браузер кэширует старую версию
3. **Проблема с SSR/CSR** - tenantId не доступен на клиенте

---

## Решение

### Проверить:
1. ✅ Убедиться, что `tenantId` правильно извлекается из `params` в layout.tsx
2. ✅ Убедиться, что `tenantId` передается через все компоненты
3. ✅ Проверить, что на продакшене применены последние изменения

### Если проблема в деплое:
- Деплой изменений на продакшен
- Проверка, что все файлы обновлены

### Если проблема в кэшировании:
- Очистить кэш браузера
- Проверить в режиме инкогнито

---

## Текущий статус

- ✅ Код исправлен правильно
- ❌ На продакшене не работает (возможно, не задеплоено)
- 🔴 Все ссылки в sidebar ведут на 404

---

**Требуется:** Деплой изменений + повторное тестирование

