# 🔧 Настройка BACKEND_API_URL

**Дата:** 2025-01-26  
**Приоритет:** 🔴 КРИТИЧНО

---

## 📋 Проблема

`BACKEND_API_URL` используется в `lib/backend/client.ts` для вызова Worker API. Если не настроен, синхронизация Kommo не работает.

---

## ✅ Решение

### Шаг 1: Проверить текущее значение в Vercel

1. Открыть: https://vercel.com/dashboard
2. Выбрать проект: `gpt-agent-kwid`
3. Settings → Environment Variables
4. Проверить есть ли `BACKEND_API_URL`

### Шаг 2: Установить правильное значение

**Значение должно быть:**
```
BACKEND_API_URL=https://gpt-agent-platform-production.up.railway.app
```

**Это URL Worker сервиса на Railway**, где обрабатываются фоновые задачи.

### Шаг 3: Добавить во все окружения

- ✅ Production
- ✅ Preview  
- ✅ Development

---

## 🔍 Проверка

После настройки можно проверить:

```bash
# Health check Worker
curl https://gpt-agent-platform-production.up.railway.app/health

# Должен вернуть:
# {"status":"ok","service":"worker",...}
```

---

## ⚠️ Важно

**НЕ путать с NEXTAUTH_URL!**

- `NEXTAUTH_URL` = URL Frontend (Vercel)
- `BACKEND_API_URL` = URL Worker (Railway)

---

**Последнее обновление:** 2025-01-26

