# ✅ Полная инструкция: Деплой Worker на Railway

> Все готово для деплоя! Следуйте этим шагам.

## 🚀 Быстрый путь (через Dashboard - 5 минут)

### Шаг 1: Откройте Railway Dashboard

1. Перейдите: https://railway.app
2. Войдите через GitHub (если еще не вошли)
3. Нажмите **"New Project"**

### Шаг 2: Подключите репозиторий

1. Выберите **"Deploy from GitHub repo"**
2. Найдите репозиторий: `worldwideservice/gpt-agent-platform`
3. Нажмите **"Deploy Now"**

### Шаг 3: Добавьте Worker сервис

Railway может автоматически обнаружить несколько сервисов. Если worker не добавился автоматически:

1. Нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите тот же репозиторий
3. В настройках сервиса:
   - **Name:** `worker`
   - **Root Directory:** `services/worker` ⚠️ **ВАЖНО!**
   - Railway автоматически обнаружит `railway.json` и `Dockerfile`

### Шаг 4: Настройте переменные окружения

1. Откройте сервис `worker`
2. **Settings** → **Variables**
3. Добавьте каждую переменную:

#### Переменные для добавления:

```bash
REDIS_URL=redis://default:AYcU...MDU= (замаскировано)@usw1-merry-term-40416.upstash.io:6379

SUPABASE_URL=https://rpzchsgutabxeabbnwas.supabase.co

SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...bx2I (замаскировано)

ENCRYPTION_KEY=HxXQ...wE= (замаскировано)

OPENROUTER_API_KEY=sk-or-v1-...80d7 (замаскировано)

JOB_QUEUE_NAME=agent-jobs

JOB_CONCURRENCY=5

PORT=3001
```

**Как добавить:**
1. Нажмите **"+ New Variable"**
2. Введите **Name** (например: `REDIS_URL`)
3. Введите **Value** (скопируйте из списка выше)
4. Нажмите **"Add"**
5. Повторите для каждой переменной

### Шаг 5: Деплой запустится автоматически

После добавления переменных Railway:
1. Автоматически запустит сборку
2. Соберет Docker образ
3. Задеплоит сервис

**Время деплоя:** ~3-5 минут

### Шаг 6: Проверка деплоя

1. Дождитесь завершения деплоя (зеленая галочка ✅)
2. Откройте **Settings** → **Networking**
3. Нажмите **"Generate Domain"**
4. Скопируйте URL (например: `worker-production-xxxx.up.railway.app`)

#### Проверьте health check:

```bash
curl https://your-worker-url.up.railway.app/health
```

**Ожидаемый ответ:**
```json
{
  "status": "ok",
  "service": "worker",
  "timestamp": "2025-01-XX..."
}
```

#### Проверьте логи:

В Railway Dashboard → **Logs** должны быть:
```
[worker] Started processing jobs from queue: agent-jobs
[worker] Concurrency: 5
[worker-health] Health check server listening on port 3001
```

---

## 🔍 Troubleshooting

### ❌ Ошибка: "Redis connection failed"

**Причина:** Неправильный формат REDIS_URL

**Решение:**
1. Проверьте что REDIS_URL начинается с `redis://`
2. Формат: `redis://default:TOKEN@HOST:6379`
3. Убедитесь что TOKEN правильный (из Upstash)

### ❌ Ошибка: "ENCRYPTION_KEY too short"

**Причина:** ENCRYPTION_KEY меньше 32 символов

**Решение:**
Используйте: `HxXQ...wE= (замаскировано)`

### ❌ Ошибка: "Build failed"

**Причина:** Проблемы с Dockerfile или зависимостями

**Решение:**
1. Проверьте логи сборки в Railway
2. Убедитесь что `services/worker/Dockerfile` существует
3. Проверьте что `package.json` в правильной директории

### ❌ Ошибка: "Worker not processing jobs"

**Причина:** Проблемы с подключением к Redis или очередь пуста

**Решение:**
1. Проверьте REDIS_URL
2. Проверьте логи worker
3. Убедитесь что задачи добавляются в очередь (проверьте в основном API)

---

## ✅ Чеклист деплоя

- [ ] Railway проект создан
- [ ] Репозиторий подключен
- [ ] Сервис `worker` добавлен
- [ ] Root Directory установлен: `services/worker`
- [ ] Все 7 переменных окружения добавлены
- [ ] REDIS_URL правильного формата
- [ ] ENCRYPTION_KEY минимум 32 символа
- [ ] Деплой завершился успешно (зеленая галочка)
- [ ] Health check возвращает `{"status":"ok"}`
- [ ] Логи показывают "Started processing jobs"

---

## 📊 После успешного деплоя

Worker будет автоматически:
- ✅ Обрабатывать файлы для базы знаний
- ✅ Генерировать embeddings
- ✅ Синхронизировать Kommo воронки
- ✅ Обрабатывать webhooks
- ✅ Отправлять сообщения в Kommo

---

## 🔗 Полезные ссылки

- Railway Dashboard: https://railway.app
- Railway Documentation: https://docs.railway.app
- Worker Health Check: `https://your-worker-url.up.railway.app/health`

---

**Railway Token:** `b2d3...cf26 (замаскировано)` ✅  
**Последнее обновление:** 2025-01-XX  
**Статус:** ✅ Готово к деплою

