# 🔍 Анализ проблемы с Railway Token

## ✅ НОВЫЙ ТОКЕН СОЗДАН

**Старый токен:** `5cd06a89-c580-450d-958c-6b1553bb1428` ❌ (невалидный)  
**Новый токен:** `b2d35fc1-afcf-4589-8b24-da667437cf26` ✅ (работает!)

**Дата создания:** 2025-01-26  
**Статус:** ✅ Валидный и протестирован

### Проверка через API:

```bash
curl -X POST https://backboard.railway.com/graphql/v2 \
  -H "Authorization: Bearer b2d35fc1-afcf-4589-8b24-da667437cf26" \
  -H "Content-Type: application/json" \
  -d '{"query":"query { me { name email } }"}'
```

**Ответ (успех):**
```json
{"data":{"me":{"name":"Maksym","email":"admin@worldwideservice.eu"}}}
```

---

## ❌ Старый токен (для справки)

**Токен:** `5cd06a89-c580-450d-958c-6b1553bb1428`  
**Статус:** Невалидный  
**Ошибка API:** `"Not Authorized"`  

### Тест через Railway API:

```bash
curl -X POST https://backboard.railway.com/graphql/v2 \
  -H "Authorization: Bearer 5cd06a89-c580-450d-958c-6b1553bb1428" \
  -H "Content-Type: application/json" \
  -d '{"query":"query { me { name email } }"}'
```

**Ответ:**
```json
{
  "errors": [{
    "message": "Not Authorized",
    "extensions": {
      "code": "INTERNAL_SERVER_ERROR"
    }
  }]
}
```

---

## 🔍 Почему токен невалидный?

Согласно [Railway Public API Documentation](https://docs.railway.com/reference/public-api):

### 1. **Неверный тип токена** ❌
Токен `5cd06a89-c580-450d-958c-6b1553bb1428` может быть:
- **Не API Token** - возможно это ID проекта, сервиса или другой идентификатор
- **Старый формат** - Railway мог изменить формат токенов
- **CLI Session Token** - не подходит для Public API

### 2. **Токен истек или отозван** ⏰
- API токены Railway могут иметь срок действия
- Если токен был удален в настройках, он больше не работает

### 3. **Неправильный формат использования** 🔐
Railway API требует:
- **Endpoint:** `https://backboard.railway.com/graphql/v2` (GraphQL)
- **Header:** `Authorization: Bearer <API_TOKEN>`
- **Content-Type:** `application/json`

### 4. **Требуется правильный тип токена** 📝

Railway предоставляет **3 типа токенов**:

#### A. **Team Token** (Командный токен)
- Привязан к команде
- Доступ ко всем ресурсам команды
- Не может получить доступ к личным ресурсам
- Создается в: **Account Settings → Tokens → Team Token**

#### B. **Personal Token** (Личный токен)
- Привязан к аккаунту Railway
- Доступ ко всем ресурсам пользователя
- Нельзя делиться с другими
- Создается в: **Account Settings → Tokens → Personal Token**

#### C. **Project Token** (Проектный токен)
- Привязан к конкретному проекту и окружению
- Только для этого проекта
- Используется заголовок: `Project-Access-Token: <TOKEN>`
- Создается в: **Project Settings → Tokens**

---

## ✅ РЕШЕНИЕ

### Шаг 1: Создать правильный API Token

1. **Откройте:** https://railway.app/account/tokens
2. **Выберите тип токена:**
   - Для автоматизации команды: **Team Token**
   - Для личных ресурсов: **Personal Token**
   - Для конкретного проекта: **Project Token**

3. **Нажмите "Create Token"**
4. **Скопируйте токен сразу** (показывается один раз!)

### Шаг 2: Проверить токен

```bash
# Тест через GraphQL API
curl -X POST https://backboard.railway.com/graphql/v2 \
  -H "Authorization: Bearer <ВАШ_НОВЫЙ_ТОКЕН>" \
  -H "Content-Type: application/json" \
  -d '{"query":"query { me { name email } }"}'
```

**Ожидаемый ответ (успех):**
```json
{
  "data": {
    "me": {
      "name": "Your Name",
      "email": "your@email.com"
    }
  }
}
```

---

## 🔧 Альтернативный способ: Railway CLI

Railway CLI требует **интерактивную авторизацию**:

```bash
# Установка CLI
npm install -g @railway/cli

# Интерактивная авторизация (откроет браузер)
railway login
```

**⚠️ Важно:** Railway CLI не поддерживает прямую авторизацию через токен в неинтерактивном режиме.

---

## 🎯 Рекомендация

### Для автоматизации через API:
1. ✅ Создать **Team Token** или **Personal Token** через Dashboard
2. ✅ Использовать в GraphQL запросах с заголовком `Authorization: Bearer <TOKEN>`
3. ✅ Endpoint: `https://backboard.railway.com/graphql/v2`

### Для быстрого деплоя:
1. ✅ Использовать **Railway Dashboard** (проще и быстрее)
2. ✅ Новый проект → Deploy from GitHub repo
3. ✅ Root Directory: `services/worker`

---

## 📚 Документация

- **Railway Public API:** https://docs.railway.com/reference/public-api
- **GraphQL Playground:** https://backboard.railway.com/graphql/v2
- **Account Tokens:** https://railway.app/account/tokens

---

## 🔍 Формат валидного токена

Railway API токены обычно имеют формат:
- Длинная строка из букв и цифр
- Примерно 40-60 символов
- Не UUID формат (не `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`)

Токен `5cd06a89-c580-450d-958c-6b1553bb1428` выглядит как **UUID**, что указывает на то, что это **не API Token**, а скорее всего:
- ID проекта
- ID сервиса
- ID деплоя
- Или другой идентификатор Railway

---

**Последнее обновление:** 2025-01-26  
**Статус:** Требуется создание нового API Token

