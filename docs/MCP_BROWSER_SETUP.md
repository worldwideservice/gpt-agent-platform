# 🔧 Настройка MCP Browser Tools в Cursor IDE

## ❌ Проблема: "Browser is not connected"

Если вы видите ошибку `"error":"Not connected"` при использовании браузерных инструментов, значит MCP Browser сервер не настроен или не запущен.

---

## ✅ Решение: Настройка MCP Browser в Cursor IDE

### Шаг 1: Откройте настройки Cursor

1. **Mac:** `Cmd + ,` (или `Cursor` → `Settings`)
2. **Windows/Linux:** `Ctrl + ,`

### Шаг 2: Найдите настройки MCP

1. В поиске настроек введите: `MCP` или `Model Context Protocol`
2. Найдите раздел **"MCP Servers"** или **"Model Context Protocol"**

### Шаг 3: Добавьте/проверьте MCP Browser сервер

Убедитесь, что добавлен сервер **"cursor-ide-browser"** или **"browser"**:

```json
{
  "mcpServers": {
    "cursor-ide-browser": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-browser"
      ]
    }
  }
}
```

Или если используется другой формат:

```json
{
  "mcp": {
    "servers": {
      "browser": {
        "command": "npx",
        "args": [
          "-y",
          "@modelcontextprotocol/server-browser"
        ]
      }
    }
  }
}
```

### Шаг 4: Перезапустите Cursor

1. Закройте Cursor полностью
2. Откройте снова
3. MCP сервер должен запуститься автоматически

---

## 🔍 Проверка подключения

После настройки попробуйте:

1. **Через AI:** Попросите AI открыть браузер:
   ```
   Открой браузер и перейди на https://railway.app
   ```

2. **Через команду:** AI должен использовать `browser_navigate`

---

## ⚠️ Альтернативные решения

### Если MCP Browser не работает:

1. **Проверьте версию Cursor**
   - Убедитесь, что используется последняя версия Cursor
   - Обновите Cursor, если нужно

2. **Проверьте установку Node.js**
   - MCP Browser требует Node.js
   - Проверьте: `node --version`
   - Должна быть версия 18+ или 20+

3. **Проверьте интернет-соединение**
   - MCP Browser загружает зависимости через npm
   - Убедитесь, что есть доступ в интернет

4. **Проверьте логи Cursor**
   - `Cursor` → `Help` → `Toggle Developer Tools`
   - Проверьте консоль на ошибки

---

## 📝 Ручная проверка логов

Если MCP Browser не работает, вы можете проверить логи Worker вручную:

1. Откройте Railway Dashboard: https://railway.app
2. Перейдите в проект → Worker service → Deployments
3. Откройте последний failed deployment
4. Проверьте Build Logs и Runtime Logs
5. Скопируйте ошибку и отправьте AI

---

## 🔧 Быстрая настройка через файл конфигурации

Если в Cursor есть файл конфигурации MCP (обычно `~/.cursor/mcp.json` или в настройках проекта):

```json
{
  "mcpServers": {
    "cursor-ide-browser": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-browser"
      ],
      "env": {}
    }
  }
}
```

---

**Дата:** 2025-01-26  
**Статус:** Требуется настройка MCP Browser в Cursor IDE

