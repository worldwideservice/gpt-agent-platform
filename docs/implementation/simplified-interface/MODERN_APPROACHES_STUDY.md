# Изучение современных подходов для UI компонентов

**Дата:** 2025-01-26  
**Цель:** Изучить современные подходы для реализации упрощенного интерфейса

---

## 📚 Изученные источники

### 1. shadcn/ui

**Компоненты для multi-select:**
- `Combobox` - базовый компонент для выбора с поиском
- `Command` - компонент для командной палитры и поиска
- `Popover` - для выпадающих списков
- `Badge` - для отображения тегов

**Подход:**
- Использование `Popover` + `Command` для создания выпадающего списка с поиском
- `CommandInput` для поиска
- `CommandItem` для элементов списка
- `CommandGroup` для группировки элементов

**Пример структуры:**
```tsx
<Popover>
  <PopoverTrigger>
    <Button>Выберите...</Button>
  </PopoverTrigger>
  <PopoverContent>
    <Command>
      <CommandInput placeholder="Поиск..." />
      <CommandList>
        <CommandEmpty>Ничего не найдено</CommandEmpty>
        <CommandGroup>
          <CommandItem>Элемент 1</CommandItem>
          <CommandItem>Элемент 2</CommandItem>
        </CommandGroup>
      </CommandList>
    </Command>
  </PopoverContent>
</Popover>
```

### 2. Существующие компоненты в проекте

**`components/ui/combobox.tsx`:**
- Уже реализован базовый Combobox
- Поддерживает поиск
- Использует `Command` и `Popover`
- Нужно расширить для multi-select с тегами

**`components/ui/command.tsx`:**
- Полностью реализован Command компонент
- Поддерживает поиск, группировку, пустые состояния
- Можно использовать для multi-select

---

## 🎨 Современные паттерны (2024-2025)

### Multi-select с тегами

**Паттерн:**
1. Выпадающий список с поиском (Combobox/Command)
2. Выбранные элементы отображаются как теги (Badge)
3. Теги можно удалять (крестик на теге)
4. Группировка тегов по типам
5. Цветовая кодировка

**Структура:**
```tsx
<div>
  {/* Выбранные теги */}
  <div className="flex flex-wrap gap-2">
    {selectedItems.map(item => (
      <Badge key={item.id} variant="secondary">
        {item.label}
        <button onClick={() => removeItem(item.id)}>×</button>
      </Badge>
    ))}
  </div>
  
  {/* Выпадающий список */}
  <Popover>
    <PopoverTrigger>
      <Button>Добавить...</Button>
    </PopoverTrigger>
    <PopoverContent>
      <Command>
        <CommandInput />
        <CommandList>
          <CommandGroup heading="Воронки">
            {pipelines.map(p => (
              <CommandItem key={p.id}>
                {p.name}
              </CommandItem>
            ))}
          </CommandGroup>
        </CommandList>
      </Command>
    </PopoverContent>
  </Popover>
</div>
```

### Текстовое поле с инструкциями

**Паттерн:**
1. Большое текстовое поле (Textarea)
2. Инструкция с примером вверху
3. Плейсхолдер с примером
4. Кнопка голосового ввода (🎤)
5. Автоматическое изменение размера (auto-resize)

**Структура:**
```tsx
<div>
  <div className="text-sm text-muted-foreground mb-2">
    📝 Опишите, как должен работать агент:
    <br />
    <span className="text-xs italic">
      Пример: "Агент должен быть дружелюбным..."
    </span>
  </div>
  <div className="relative">
    <Textarea
      placeholder="Введите инструкцию или нажмите 🎤 для голосового ввода"
      className="min-h-[200px] resize-none"
    />
    <Button
      className="absolute bottom-2 right-2"
      size="icon"
      variant="ghost"
    >
      🎤
    </Button>
  </div>
</div>
```

### AI-ассистент предпросмотр

**Паттерн:**
1. Карточка с предпросмотром (Card)
2. Отображение структурированных данных
3. Статус валидации
4. Кнопки "Применить" и "Редактировать"

**Структура:**
```tsx
<Card>
  <CardHeader>
    <CardTitle>Предпросмотр настроек</CardTitle>
  </CardHeader>
  <CardContent>
    <div className="space-y-4">
      <div>
        <Label>Тип:</Label>
        <p>Триггер</p>
      </div>
      <div>
        <Label>Условие:</Label>
        <p>Когда сделка попадает в этап "Квалификация"</p>
      </div>
      <div>
        <Label>Действия:</Label>
        <ul>
          <li>1. Создать задачу менеджеру</li>
          <li>2. Отправить сообщение клиенту</li>
        </ul>
      </div>
    </div>
  </CardContent>
  <CardFooter>
    <Button>Применить</Button>
    <Button variant="outline">Редактировать</Button>
  </CardFooter>
</Card>
```

---

## 🔧 Технические детали

### Голосовой ввод

**Web Speech API:**
- `SpeechRecognition` для записи
- Поддержка русского и английского
- Обработка ошибок (микрофон недоступен)

**Whisper через OpenRouter:**
- Отправка аудио на транскрипцию
- Получение текста
- Обновление текстового поля

### Синхронизация Kommo

**Логика:**
1. Пользователь нажимает "🔄 Синхронизировать"
2. Показывается индикатор загрузки
3. Отправляется запрос на `/api/kommo/sync`
4. Данные сохраняются в `kommo_sync_cache`
5. Обновляется статус синхронизации
6. Выпадающие списки обновляются из кэша

**Статус синхронизации:**
- ✅ Синхронизировано (время последней синхронизации)
- ⚠️ Требуется синхронизация
- 🔄 Синхронизация...

---

## 📝 Рекомендации

1. **Использовать существующие компоненты:**
   - `Combobox` как базу для multi-select
   - `Command` для поиска
   - `Badge` для тегов
   - `Card` для предпросмотра

2. **Расширить Combobox:**
   - Добавить поддержку множественного выбора
   - Добавить отображение выбранных элементов как теги
   - Добавить удаление тегов

3. **Создать новые компоненты:**
   - `MultiSelectDropdown` - на основе Combobox
   - `TagGroup` - для группировки тегов
   - `InstructionTextarea` - текстовое поле с инструкцией
   - `VoiceInput` - голосовой ввод
   - `AIAssistantPreview` - предпросмотр от AI

---

**Последнее обновление:** 2025-01-26

