# Анализ проекта для реализации упрощенного интерфейса

**Дата:** 2025-01-26  
**Ветка:** `feature/simplified-agent-interface`  
**Статус:** Анализ завершен

---

## 📊 Текущая архитектура проекта

### Технологический стек

- **Frontend:** Next.js 14 (App Router), React 18, TypeScript
- **UI:** Tailwind CSS, Radix UI, shadcn/ui
- **Backend:** Next.js API Routes, Node.js
- **База данных:** Supabase (PostgreSQL 17 + pgvector)
- **Очереди:** BullMQ + Redis (Upstash)
- **AI:** OpenRouter API
- **CRM:** Kommo (OAuth 2.0, REST API, Webhooks)
- **Хостинг:** Vercel (Frontend), Railway (Worker)

### Структура базы данных

#### Существующие таблицы для агентов:

1. **`agents`** - основная таблица агентов
   - `id`, `org_id`, `connection_id`
   - `name`, `status` (active/inactive/draft)
   - `default_model`, `instructions`, `system_prompt`
   - `temperature`, `max_tokens`, `response_delay_seconds`
   - `settings` (JSONB) - для хранения настроек

2. **`agent_pipelines`** - привязка агента к воронкам
   - `agent_id`, `pipeline_external_id`, `name`
   - `is_active`, `metadata` (JSONB)

3. **`agent_pipeline_stages`** - этапы воронок
   - `pipeline_id`, `stage_external_id`, `name`
   - `order_index`, `metadata` (JSONB)

4. **`agent_stage_policies`** - политики для этапов
   - `agent_id`, `stage_id`
   - `instructions`, `auto_advance`, `followup_delay`, `followup_template`

5. **`agent_triggers`** - триггеры автоматизации
   - `agent_id`, `name`, `description`, `is_active`

6. **`agent_trigger_conditions`** - условия триггеров
   - `trigger_id`, `condition_type`, `payload` (JSONB), `ordering`

7. **`agent_trigger_actions`** - действия триггеров
   - `trigger_id`, `action_type`, `payload` (JSONB), `ordering`
   - **Важно:** Можно создать несколько действий для одного триггера

8. **`agent_sequences`** - цепочки действий
   - `agent_id`, `name`, `description`, `is_active`
   - `settings` (JSONB)

9. **`agent_sequence_steps`** - шаги цепочек
   - `sequence_id`, `step_index`, `wait_interval`
   - `channel`, `template`, `metadata` (JSONB)

10. **`agent_channels`** - каналы связи
    - `agent_id`, `channel_key`, `is_enabled`
    - `settings` (JSONB)

11. **`crm_connections`** - подключения к CRM
    - `org_id`, `provider` (kommo), `base_domain`
    - `access_token`, `refresh_token`, `expires_at`

12. **`crm_pipelines`** - синхронизированные воронки из CRM
    - `connection_id`, `external_id`, `name`, `is_active`

13. **`crm_pipeline_stages`** - синхронизированные этапы
    - `pipeline_id`, `external_id`, `name`, `sort_order`

### Существующие сервисы

1. **`lib/lib/services/agent-actions.ts`** - действия агента
   - `AgentActionsService` - анализ и предложение действий
   - Интеграция с KommoActionsService

2. **`lib/lib/services/rule-engine.ts`** - движок правил
   - `createRule`, `getRules`, `executeRule`
   - Поддержка условий и действий

3. **`lib/lib/crm/kommo.ts`** - клиент Kommo API
   - `KommoAPI` класс
   - Методы для работы с воронками, этапами, сделками, контактами
   - `parseWebhook` - парсинг webhook событий

4. **`lib/services/webhook-processor.ts`** - обработка webhooks
   - Автоматическая обработка всех типов событий
   - Автоматический запуск Rule Engine и Sequences

### Существующие API endpoints

1. **`/api/crm/webhook`** - webhook от Kommo
   - Автоматическая обработка событий
   - Не требует синхронизации

2. **`/api/agents/*`** - CRUD агентов
   - Создание, обновление, удаление агентов

3. **`/api/integrations/kommo/*`** - интеграция Kommo
   - OAuth, синхронизация

### Текущая структура компонентов

- `components/ui/` - базовые UI компоненты (shadcn/ui)
- `components/refine/` - компоненты Refine
- `components/layout/` - Header, Sidebar (удалены, нужно создать новые)

---

## 🎯 Что нужно реализовать

### Новые компоненты

1. **AgentSettings/** - основной контейнер настроек агента
2. **SyncButton** - кнопка синхронизации с Kommo
3. **SyncStatus** - статус синхронизации
4. **MultiSelectDropdown** - выпадающий список с множественным выбором
5. **TagGroup** - группа тегов
6. **Tag** - отдельный тег
7. **InstructionTextarea** - текстовое поле с инструкцией
8. **VoiceInput** - голосовой ввод
9. **AIAssistantPreview** - предпросмотр от AI-ассистента
10. **ActionList** - список действий (для триггеров/цепочек)

### Новые API endpoints

1. **Синхронизация Kommo:**
   - `GET /api/kommo/sync/status` - статус синхронизации
   - `POST /api/kommo/sync` - запустить синхронизацию
   - `GET /api/kommo/pipelines` - получить воронки (из кэша)
   - `GET /api/kommo/stages` - получить этапы (из кэша)
   - `GET /api/kommo/deal-fields` - получить поля сделок (из кэша)
   - `GET /api/kommo/contact-fields` - получить поля контактов (из кэша)
   - `GET /api/kommo/channels` - получить каналы (из кэша)
   - `GET /api/kommo/actions` - получить действия (из кэша)

2. **AI-ассистент:**
   - `POST /api/agents/parse-instruction` - обработать инструкцию

3. **OpenRouter:**
   - `GET /api/openrouter/models` - получить список моделей
   - `POST /api/openrouter/transcribe` - транскрипция через Whisper

4. **Настройки агента:**
   - `PUT /api/agents/[id]/settings/main` - сохранить основные настройки
   - `PUT /api/agents/[id]/settings/deals-contacts` - сохранить настройки сделок и контактов
   - `PUT /api/agents/[id]/triggers` - сохранить триггеры
   - `PUT /api/agents/[id]/chains` - сохранить цепочки
   - `PUT /api/agents/[id]/settings/additional` - сохранить дополнительные настройки

### Новые таблицы БД

1. **`kommo_sync_cache`** - кэш синхронизированных данных
   - `org_id`, `data_type` (pipelines, stages, deal_fields, etc.)
   - `data` (JSONB), `synced_at`, `status`, `error_message`

### Новые сервисы

1. **`lib/services/kommo-sync.ts`** - сервис синхронизации Kommo
   - Функции синхронизации для каждого типа данных
   - Кэширование в БД

2. **`lib/services/ai-assistant.ts`** - сервис AI-ассистента
   - Парсинг инструкций
   - Валидация данных из Kommo
   - Генерация предпросмотра

3. **`lib/services/voice-transcription.ts`** - сервис транскрипции
   - Запись аудио
   - Отправка на Whisper через OpenRouter
   - Обработка ответа

### Новые хуки

1. **`hooks/useKommoSync.ts`** - хук для синхронизации
2. **`hooks/useVoiceInput.ts`** - хук для голосового ввода
3. **`hooks/useAIAssistant.ts`** - хук для AI-ассистента
4. **`hooks/useTagGroups.ts`** - хук для группировки тегов

---

## 🔄 Логика работы

### Синхронизация Kommo

1. Пользователь нажимает "🔄 Синхронизировать"
2. Система обращается к Kommo API:
   - `/api/v4/leads/pipelines` - воронки
   - `/api/v4/leads/pipelines/{id}` - этапы для каждой воронки
   - `/api/v4/leads/custom_fields` - поля сделок
   - `/api/v4/contacts/custom_fields` - поля контактов
   - И другие endpoints
3. Данные сохраняются в `kommo_sync_cache`
4. Обновляется статус синхронизации
5. Выпадающие списки обновляются из кэша

### Webhooks (автоматические)

1. Kommo отправляет webhook на `/api/crm/webhook`
2. Система обрабатывает событие автоматически
3. Запускается Rule Engine и Sequences
4. **Не требует синхронизации** - работает независимо

### AI-ассистент

1. Пользователь вводит инструкцию в текстовое поле
2. Отправляется на `/api/agents/parse-instruction`
3. AI (GPT-4o Mini) парсит инструкцию:
   - Определяет тип настройки
   - Извлекает условия, действия, параметры
   - Валидирует данные из Kommo
4. Возвращается структурированный предпросмотр
5. Пользователь подтверждает или редактирует
6. Настройки применяются и сохраняются в БД

### Голосовой ввод

1. Пользователь нажимает 🎤
2. Запрашивается разрешение на микрофон
3. Начинается запись (Web Audio API)
4. Пользователь говорит
5. Останавливает запись
6. Аудио отправляется на Whisper через OpenRouter
7. Транскрибированный текст появляется в поле

---

## 📝 Важные детали

### Множественные действия

- В таблице `agent_trigger_actions` можно создать несколько записей для одного `trigger_id`
- Каждое действие имеет свой `ordering` для порядка выполнения
- AI-ассистент понимает множественные действия из одной инструкции:
  ```
  "Когда сделка попадает в этап 'Квалификация':
  1. Создать задачу менеджеру
  2. Отправить клиенту сообщение
  3. Обновить поле 'Статус'"
  ```

### Группировка тегов

- Теги группируются по типам данных
- Каждая группа имеет свой цвет
- Группы отображаются последовательно

### Предпросмотр AI-ассистента

- Всегда показывается перед применением
- Визуальная карточка с деталями
- Возможность редактирования/уточнения

---

## 🚀 Следующие шаги

1. ✅ Создана ветка `feature/simplified-agent-interface`
2. ✅ Создан план реализации
3. ✅ Проведен анализ проекта
4. ⏳ Начать реализацию с Этапа 1: Базовая инфраструктура

---

**Последнее обновление:** 2025-01-26

