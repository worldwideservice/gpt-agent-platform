# План реализации упрощенного интерфейса настройки агентов

**Дата создания:** 2025-01-26  
**Версия:** 1.0  
**Статус:** Готов к реализации  
**Ветка:** `feature/simplified-agent-interface`

---

## 🎯 Цель

Создать максимально простой, но функциональный интерфейс для настройки AI-агентов, где пользователь:
1. Выбирает из списка (данные из Kommo CRM) → отображаются как теги
2. Пишет/диктует инструкцию в текстовом поле
3. AI-ассистент обрабатывает и показывает предпросмотр
4. Пользователь подтверждает → настройка применяется

---

## 📋 Ключевые принципы

### 1. Синхронизация данных
- **Синхронизация воронок/этапов/полей:** Только по кнопке "🔄 Синхронизировать"
- **Webhooks для сообщений:** Работают автоматически (не требуют синхронизации)
  - Webhooks обрабатываются через `/api/crm/webhook`
  - Автоматически запускают Rule Engine и Sequences
  - Не требуют ручной синхронизации
- **Статус синхронизации:** Показывается всегда (✅ Синхронизировано / ⚠️ Требуется синхронизация)

### 2. Множественные действия
- Можно создавать несколько действий (2-3) на этап или воронку
- Действия отображаются в списке и могут быть отредактированы/удалены
- Каждое действие может быть настроено отдельно через текстовое поле
- AI-ассистент понимает множественные действия из одной инструкции

### 3. Голосовой ввод
- OpenAI Whisper через OpenRouter
- Поддержка русского и английского
- Автоматическая транскрипция

### 4. AI-ассистент
- Всегда показывает предпросмотр перед применением
- Визуальная карточка с деталями
- Возможность редактирования/уточнения

### 5. Теги
- Неограниченное количество
- Группировка по типам (Воронки, Поля сделок, Каналы и т.д.)
- Цветовая кодировка

---

## 🏗️ Архитектура реализации

### Структура компонентов

```
components/
├── agents/
│   ├── AgentSettings/
│   │   ├── AgentSettingsTabs.tsx          # Контейнер табов
│   │   ├── tabs/
│   │   │   ├── MainTab.tsx                 # Таб "Основные"
│   │   │   ├── DealsContactsTab.tsx       # Таб "Сделки и контакты"
│   │   │   ├── TriggersTab.tsx             # Таб "Триггеры"
│   │   │   ├── ChainsTab.tsx               # Таб "Цепочки"
│   │   │   ├── IntegrationsTab.tsx         # Таб "Интеграции"
│   │   │   └── AdditionalTab.tsx           # Таб "Дополнительно"
│   │   ├── components/
│   │   │   ├── SyncButton.tsx              # Кнопка синхронизации CRM
│   │   │   ├── SyncStatus.tsx              # Статус синхронизации
│   │   │   ├── MultiSelectDropdown.tsx     # Выпадающий список с тегами
│   │   │   ├── TagGroup.tsx                # Группа тегов
│   │   │   ├── Tag.tsx                     # Отдельный тег
│   │   │   ├── InstructionTextarea.tsx     # Текстовое поле с инструкцией
│   │   │   ├── VoiceInput.tsx             # Голосовой ввод
│   │   │   ├── AIAssistantPreview.tsx     # Предпросмотр от AI-ассистента
│   │   │   └── ActionList.tsx               # Список действий (для триггеров/цепочек)
│   │   └── hooks/
│   │       ├── useKommoSync.ts              # Хук для синхронизации Kommo
│   │       ├── useVoiceInput.ts              # Хук для голосового ввода
│   │       ├── useAIAssistant.ts             # Хук для AI-ассистента
│   │       └── useTagGroups.ts               # Хук для группировки тегов
│   └── AgentList/
│       └── AgentList.tsx                    # Список агентов
```

---

## 📦 Этапы реализации

### Этап 1: Базовая инфраструктура (1-2 дня)

#### 1.1. Создание ветки и структуры
- [x] Создать ветку `feature/simplified-agent-interface`
- [ ] Создать структуру папок для компонентов
- [ ] Настроить TypeScript типы

#### 1.2. Базовые компоненты
- [ ] `SyncButton` - кнопка синхронизации с индикатором загрузки
- [ ] `SyncStatus` - отображение статуса синхронизации
- [ ] `Tag` - компонент тега с удалением
- [ ] `TagGroup` - группа тегов с заголовком
- [ ] `MultiSelectDropdown` - выпадающий список с множественным выбором

#### 1.3. API endpoints
- [ ] `GET /api/kommo/sync/status` - получить статус синхронизации
- [ ] `POST /api/kommo/sync` - запустить синхронизацию
- [ ] `GET /api/kommo/pipelines` - получить воронки (из кэша)
- [ ] `GET /api/kommo/stages` - получить этапы (из кэша)
- [ ] `GET /api/kommo/fields` - получить поля (из кэша)
- [ ] `GET /api/kommo/channels` - получить каналы (из кэша)
- [ ] `GET /api/kommo/actions` - получить действия (из кэша)

---

### Этап 2: Синхронизация Kommo (2-3 дня)

#### 2.1. Сервис синхронизации
- [ ] Обновить `lib/services/kommo-sync.ts`:
  - Функция синхронизации воронок
  - Функция синхронизации этапов
  - Функция синхронизации полей сделок
  - Функция синхронизации полей контактов
  - Функция синхронизации каналов
  - Функция синхронизации действий
  - Кэширование данных в БД

#### 2.2. Хранение синхронизированных данных
- [ ] Таблица `kommo_sync_cache`:
  - `org_id` - ID организации
  - `data_type` - тип данных (pipelines, stages, fields, etc.)
  - `data` - JSON с данными
  - `synced_at` - время последней синхронизации
  - `status` - статус (success, error, pending)

#### 2.3. UI компоненты синхронизации
- [ ] `SyncButton` - кнопка с состояниями (обычное, загрузка, успех, ошибка)
- [ ] `SyncStatus` - отображение статуса с временем последней синхронизации
- [ ] Интеграция в каждый таб, где нужны данные из Kommo

---

### Этап 3: Компоненты выбора и теги (2-3 дня)

#### 3.1. MultiSelectDropdown
- [ ] Выпадающий список с поиском
- [ ] Множественный выбор
- [ ] Отображение выбранных опций как теги
- [ ] Группировка тегов по типам
- [ ] Фильтры (опционально)

#### 3.2. TagGroup
- [ ] Компонент группы тегов с заголовком
- [ ] Цветовая кодировка по типам
- [ ] Удаление тегов
- [ ] Адаптивность (перенос на новую строку)

#### 3.3. Интеграция с данными Kommo
- [ ] Загрузка данных из кэша
- [ ] Обновление при синхронизации
- [ ] Обработка ошибок (нет данных, ошибка загрузки)

---

### Этап 4: Текстовое поле с инструкциями (1-2 дня)

#### 4.1. InstructionTextarea
- [ ] Большое текстовое поле (textarea)
- [ ] Инструкция с примером вверху
- [ ] Плейсхолдер с примером
- [ ] Адаптивная высота (auto-resize)
- [ ] Валидация (опционально)

#### 4.2. Интеграция голосового ввода
- [ ] Кнопка 🎤 в правом нижнем углу
- [ ] Индикатор записи
- [ ] Интеграция с OpenAI Whisper через OpenRouter
- [ ] Обработка ошибок

---

### Этап 5: Голосовой ввод (2-3 дня)

#### 5.1. Сервис транскрипции
- [ ] `lib/services/voice-transcription.ts`:
  - Функция записи аудио (Web Audio API)
  - Отправка на Whisper через OpenRouter
  - Обработка ответа
  - Обработка ошибок

#### 5.2. Компонент VoiceInput
- [ ] Кнопка микрофона
- [ ] Индикатор записи (анимация волн)
- [ ] Таймер записи
- [ ] Индикатор уровня звука
- [ ] Индикатор обработки
- [ ] Обработка ошибок (микрофон недоступен, ошибка транскрипции)

#### 5.3. Хук useVoiceInput
- [ ] Управление состоянием записи
- [ ] Запрос разрешения на микрофон
- [ ] Запись аудио
- [ ] Отправка на транскрипцию
- [ ] Обновление текстового поля

---

### Этап 6: AI-ассистент обработки (3-4 дня)

#### 6.1. Сервис AI-ассистента
- [ ] `lib/services/ai-assistant.ts`:
  - Функция парсинга инструкции
  - Валидация данных из Kommo
  - Структурирование настроек
  - Генерация предпросмотра

#### 6.2. API endpoint
- [ ] `POST /api/agents/parse-instruction`:
  - Принимает текст инструкции
  - Принимает выбранные теги (опционально)
  - Принимает тип настройки (триггер, цепочка, инструкция)
  - Возвращает структурированные настройки
  - Возвращает предпросмотр

#### 6.3. Компонент AIAssistantPreview
- [ ] Карточка с предпросмотром
- [ ] Отображение типа настройки
- [ ] Отображение условий (если есть)
- [ ] Отображение действий (если есть)
- [ ] Статус валидации
- [ ] Кнопки "Применить" и "Редактировать"

#### 6.4. Хук useAIAssistant
- [ ] Отправка инструкции на обработку
- [ ] Управление состоянием (loading, success, error)
- [ ] Обработка ответа
- [ ] Применение настроек

---

### Этап 7: Реализация табов (5-7 дней)

#### 7.1. Таб "Основные" (MainTab)
- [ ] Профиль агента (текстовое поле)
- [ ] Общие инструкции (текстовое поле)
- [ ] Настройка воронок (выпадающий список + текстовое поле)
- [ ] Настройка каналов (выпадающий список + текстовое поле)
- [ ] База знаний (выпадающий список + текстовое поле)
- [ ] Кнопки "Сохранить" и "Отменить"

#### 7.2. Таб "Сделки и контакты" (DealsContactsTab)
- [ ] Доступ к данным сделок (выпадающий список + текстовое поле)
- [ ] Доступ к данным контактов (выпадающий список + текстовое поле)
- [ ] Автоматическое обновление полей (выпадающий список + текстовое поле)
- [ ] Кнопки "Сохранить" и "Отменить"

#### 7.3. Таб "Триггеры" (TriggersTab)
- [ ] Список существующих триггеров (таблица)
- [ ] Создание/редактирование триггера:
  - Выпадающий список действий
  - Текстовое поле для описания
  - AI-ассистент для обработки
  - Предпросмотр
- [ ] Множественные действия (2-3 действия на триггер)

#### 7.4. Таб "Цепочки" (ChainsTab)
- [ ] Список существующих цепочек (таблица)
- [ ] Создание/редактирование цепочки:
  - Выпадающий список действий
  - Текстовое поле для описания
  - AI-ассистент для обработки
  - Предпросмотр
- [ ] Множественные действия в шагах

#### 7.5. Таб "Интеграции" (IntegrationsTab)
- [ ] Список интеграций (таблица)
- [ ] Настройка Kommo (текстовое поле)
- [ ] Настройка Google Calendar (текстовое поле)
- [ ] Кнопки "Сохранить" и "Отменить"

#### 7.6. Таб "Дополнительно" (AdditionalTab)
- [ ] Модель ИИ (выпадающий список из OpenRouter + текстовое поле)
- [ ] Язык (выпадающий список + текстовое поле)
- [ ] Креативность (выпадающий список + текстовое поле)
- [ ] Задержка ответа (текстовое поле)
- [ ] Кнопки "Сохранить" и "Отменить"

---

### Этап 8: Интеграция с OpenRouter (2-3 дня)

#### 8.1. Получение списка моделей
- [ ] `GET /api/openrouter/models` - получить список моделей из OpenRouter
- [ ] Кэширование списка моделей
- [ ] Фильтрация моделей (бесплатные/платные)

#### 8.2. Выбор модели для агента
- [ ] Выпадающий список с моделями
- [ ] Поиск по названию
- [ ] Рекомендации для задач (продажи, поддержка, квалификация)
- [ ] Сохранение выбранной модели

#### 8.3. Whisper для транскрипции
- [ ] Интеграция Whisper через OpenRouter
- [ ] Обработка аудио
- [ ] Обработка ошибок

---

### Этап 9: Сохранение и применение настроек (2-3 дня)

#### 9.1. API endpoints для сохранения
- [ ] `PUT /api/agents/[id]/settings/main` - сохранить основные настройки
- [ ] `PUT /api/agents/[id]/settings/deals-contacts` - сохранить настройки сделок и контактов
- [ ] `PUT /api/agents/[id]/triggers` - сохранить триггеры
- [ ] `PUT /api/agents/[id]/chains` - сохранить цепочки
- [ ] `PUT /api/agents/[id]/settings/additional` - сохранить дополнительные настройки

#### 9.2. Валидация и применение
- [ ] Валидация структурированных настроек
- [ ] Сохранение в БД
- [ ] Применение к агенту
- [ ] Уведомление об успехе/ошибке

---

### Этап 10: Тестирование и полировка (3-4 дня)

#### 10.1. Unit тесты
- [ ] Тесты компонентов
- [ ] Тесты хуков
- [ ] Тесты сервисов

#### 10.2. E2E тесты
- [ ] Тест создания агента
- [ ] Тест настройки агента через текстовое поле
- [ ] Тест голосового ввода
- [ ] Тест синхронизации Kommo
- [ ] Тест AI-ассистента

#### 10.3. UX полировка
- [ ] Анимации переходов
- [ ] Обработка ошибок
- [ ] Загрузочные состояния
- [ ] Адаптивность (мобильные устройства)

---

## 🔧 Технические детали

### Компоненты на основе shadcn/ui

Используем компоненты из shadcn/ui:
- `Form`, `FormField`, `FormItem`, `FormLabel`, `FormDescription`, `FormMessage`
- `Input`, `Textarea`
- `Button`
- `Select`, `MultiSelect` (кастомный на основе Select)
- `Badge` (для тегов)
- `Card` (для предпросмотра)
- `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`
- `Dialog` (для модальных окон)
- `Toast` (для уведомлений)

### Стилизация

- Tailwind CSS для стилей
- Цветовая схема из `FINAL_INTERFACE_CONCEPT_V2.md`
- Анимации через `framer-motion` (уже в проекте)

### State Management

- React Hook Form для форм
- TanStack Query для данных из API
- Zustand (опционально) для глобального состояния

---

## 📊 База данных

### Новые таблицы

#### `kommo_sync_cache`
```sql
CREATE TABLE kommo_sync_cache (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  org_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
  data_type TEXT NOT NULL, -- 'pipelines', 'stages', 'deal_fields', 'contact_fields', 'channels', 'actions'
  data JSONB NOT NULL,
  synced_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  status TEXT NOT NULL DEFAULT 'success', -- 'success', 'error', 'pending'
  error_message TEXT,
  UNIQUE(org_id, data_type)
);

CREATE INDEX idx_kommo_sync_cache_org ON kommo_sync_cache(org_id);
CREATE INDEX idx_kommo_sync_cache_type ON kommo_sync_cache(data_type);
```

### Обновление существующих таблиц

#### `agents`
- Добавить поле `settings_json` (JSONB) для хранения структурированных настроек
- Добавить поле `ai_model_id` (TEXT) для выбранной модели из OpenRouter

---

## 🔌 API Endpoints

### Синхронизация Kommo

```
GET  /api/kommo/sync/status          # Статус синхронизации
POST /api/kommo/sync                 # Запустить синхронизацию
GET  /api/kommo/pipelines            # Получить воронки (из кэша)
GET  /api/kommo/stages               # Получить этапы (из кэша)
GET  /api/kommo/deal-fields         # Получить поля сделок (из кэша)
GET  /api/kommo/contact-fields      # Получить поля контактов (из кэша)
GET  /api/kommo/channels            # Получить каналы (из кэша)
GET  /api/kommo/actions             # Получить действия (из кэша)
```

### AI-ассистент

```
POST /api/agents/parse-instruction  # Обработать инструкцию через AI
```

### Настройки агента

```
PUT  /api/agents/[id]/settings/main              # Сохранить основные настройки
PUT  /api/agents/[id]/settings/deals-contacts    # Сохранить настройки сделок и контактов
PUT  /api/agents/[id]/triggers                   # Сохранить триггеры
PUT  /api/agents/[id]/chains                    # Сохранить цепочки
PUT  /api/agents/[id]/settings/additional       # Сохранить дополнительные настройки
```

### OpenRouter

```
GET  /api/openrouter/models         # Получить список моделей
POST /api/openrouter/transcribe     # Транскрипция через Whisper
```

---

## 🎨 UI/UX Детали

### Анимации

- Плавное появление предпросмотра AI-ассистента
- Анимация волн при голосовом вводе
- Плавное появление/исчезновение тегов
- Индикатор загрузки при синхронизации

### Обработка ошибок

- Понятные сообщения об ошибках
- Retry для синхронизации
- Fallback для голосового ввода (если Whisper недоступен)
- Валидация перед сохранением

### Адаптивность

- Мобильная версия всех компонентов
- Адаптивные выпадающие списки (модальные окна на мобильных)
- Адаптивные теги (перенос на новую строку)

---

## ✅ Чеклист готовности

### Перед началом
- [x] Создана ветка `feature/simplified-agent-interface`
- [ ] Изучена документация Kommo API
- [ ] Изучены современные подходы (shadcn/ui, Context7)
- [ ] Создан план реализации

### После реализации
- [ ] Все компоненты реализованы
- [ ] Все API endpoints работают
- [ ] Unit тесты написаны
- [ ] E2E тесты написаны
- [ ] Документация обновлена
- [ ] Code review пройден
- [ ] Тестирование на staging
- [ ] Merge в main

---

## 📝 Примечания

1. **Webhooks:** Webhooks для сообщений работают автоматически (не требуют синхронизации). Синхронизация нужна только для данных (воронки, этапы, поля).

2. **Множественные действия:** Можно создавать несколько действий (2-3) на этап или воронку. Действия отображаются в списке.

3. **Голосовой ввод:** Используется OpenAI Whisper через OpenRouter. Поддержка только русского и английского.

4. **AI-ассистент:** Всегда показывает предпросмотр перед применением. Модель: GPT-4o Mini (фиксированная для всех пользователей).

5. **Теги:** Неограниченное количество, группируются по типам, цветовая кодировка.

---

## 🚀 Следующие шаги

1. Начать с Этапа 1: Базовая инфраструктура
2. Постепенно реализовывать каждый этап
3. Тестировать после каждого этапа
4. Получить обратную связь от пользователя
5. Итеративно улучшать

---

**Последнее обновление:** 2025-01-26

