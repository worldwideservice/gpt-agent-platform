# 🔄 Инструкция по ротации секретов

> ⚠️ **КРИТИЧЕСКИ ВАЖНО:** Этот документ содержит пошаговые инструкции по ротации всех компрометированных ключей

## 📋 Список ключей требующих ротации

После обнаружения секретов в git истории, **ВСЕ** следующие ключи должны быть ротированы:

1. ✅ `SUPABASE_SERVICE_ROLE_KEY`
2. ✅ `OPENROUTER_API_KEY`
3. ✅ `UPSTASH_REDIS_REST_TOKEN`
4. ✅ `NEXTAUTH_SECRET`
5. ✅ `ENCRYPTION_KEY` (если использовался)
6. ✅ `JWT_SECRET` (если использовался)

---

## 🔐 Шаг 1: Ротация Supabase Service Role Key

### 1.1 Генерация нового ключа

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите проект: `rpzchsgutabxeabbnwas`
3. Перейдите в **Settings** → **API**
4. Найдите секцию **Project API keys**
5. Найдите **`service_role`** ключ
6. Нажмите **Regenerate** (или создайте новый)

⚠️ **ВНИМАНИЕ:** Старый ключ будет недействителен немедленно!

### 1.2 Обновление в Vercel

```bash
# Добавить новый ключ в Vercel Secrets
vercel env add SUPABASE_SERVICE_ROLE_KEY production

# Или через Vercel Dashboard:
# Settings → Environment Variables → Add New
# Name: SUPABASE_SERVICE_ROLE_KEY
# Value: [новый ключ]
# Environment: Production
```

### 1.3 Проверка

После обновления проверьте:
```bash
curl https://your-app.vercel.app/api/health
```

---

## 🤖 Шаг 2: Ротация OpenRouter API Key

### 2.1 Создание нового ключа

1. Откройте [OpenRouter Dashboard](https://openrouter.ai/keys)
2. Войдите в аккаунт
3. Перейдите в **Keys**
4. Нажмите **Create Key**
5. Укажите имя: `Production - New (2025-01)`
6. Скопируйте новый ключ (формат: `sk-or-v1-...`)

### 2.2 Удаление старого ключа

1. Найдите старый ключ в списке
2. Нажмите **Delete** или **Revoke**
3. ⚠️ Убедитесь что удалили правильный ключ!

### 2.3 Обновление в Vercel

```bash
vercel env add OPENROUTER_API_KEY production
```

### 2.4 Проверка

После обновления проверьте что API работает:
- Запустите тестовый запрос к OpenRouter
- Проверьте логи приложения

---

## 🔴 Шаг 3: Ротация Upstash Redis

### 3.1 Создание нового Redis instance

**Вариант A: Создать новый database**

1. Откройте [Upstash Dashboard](https://console.upstash.com)
2. Перейдите в **Redis**
3. Нажмите **Create Database**
4. Выберите регион (рекомендуется тот же: `usw1`)
5. Выберите тип: **Regional** или **Global**
6. Создайте database

**Вариант B: Ротировать токен (если возможно)**

1. Откройте существующий database
2. Перейдите в **REST API**
3. Если доступна опция "Regenerate Token" - используйте её
4. Иначе создайте новый database

### 3.2 Получение новых credentials

После создания нового database:
1. Скопируйте **REST URL** (формат: `https://xxx.upstash.io`)
2. Скопируйте **REST TOKEN** (формат: `Axxx...`)

### 3.3 Обновление в Vercel

```bash
vercel env add UPSTASH_REDIS_REST_URL production
vercel env add UPSTASH_REDIS_REST_TOKEN production
```

### 3.4 Миграция данных (если нужно)

Если в Redis есть важные данные:
1. Экспорт данных из старого Redis
2. Импорт в новый Redis
3. Или подождите пока данные пересоздадутся естественным образом

### 3.5 Удаление старого Redis

⚠️ **Только после проверки что новый работает!**

1. Убедитесь что новый Redis работает
2. Проверьте health checks приложения
3. Удалите старый Redis database

---

## 🔑 Шаг 4: Ротация NextAuth Secret

### 4.1 Генерация нового secret

```bash
# Локально
openssl rand -base64 32

# Или через Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 4.2 Обновление в Vercel

```bash
vercel env add NEXTAUTH_SECRET production
```

⚠️ **ВАЖНО:** После ротации **ВСЕ пользователи будут разлогинены!**

Это нормально и ожидаемо при ротации секрета.

### 4.3 Проверка

После обновления:
1. Проверьте что логин работает
2. Проверьте создание новых сессий

---

## 🗝️ Шаг 5: Ротация ENCRYPTION_KEY

### 5.1 Генерация нового ключа

```bash
openssl rand -base64 32
```

Или через Node.js:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64').substring(0, 32))"
```

### 5.2 Обновление в Vercel

```bash
vercel env add ENCRYPTION_KEY production
```

⚠️ **КРИТИЧНО:** После ротации все зашифрованные данные (например, CRM токены) **НЕ МОГУТ** быть расшифрованы!

**Решение:**
1. Если есть зашифрованные CRM токены в БД - нужно будет переподключить все интеграции
2. Или использовать миграцию для перешифровки (сложно, требует временного хранения старого ключа)

### 5.3 Проверка

После обновления:
1. Проверьте работу CRM интеграций
2. Если нужно - переподключите интеграции

---

## 🔒 Шаг 6: Ротация JWT_SECRET (если используется)

### 6.1 Генерация

```bash
openssl rand -base64 32
```

### 6.2 Обновление в Vercel

```bash
vercel env add JWT_SECRET production
```

---

## ✅ Финальная проверка

После ротации всех ключей проверьте:

### 1. Health Check
```bash
curl https://your-app.vercel.app/api/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "database": "ok",
  "redis": "ok"
}
```

### 2. Проверка функций
- [ ] Авторизация работает
- [ ] База данных доступна
- [ ] Redis работает
- [ ] OpenRouter API работает
- [ ] CRM интеграции работают (если применимо)

### 3. Проверка логов
- Проверьте Vercel Logs на наличие ошибок
- Проверьте Sentry на новые ошибки

---

## 📝 Чеклист ротации

Используйте этот чеклист чтобы не пропустить шаги:

- [ ] Ротирован `SUPABASE_SERVICE_ROLE_KEY`
- [ ] Ротирован `OPENROUTER_API_KEY`
- [ ] Ротирован `UPSTASH_REDIS_REST_TOKEN` (и URL)
- [ ] Ротирован `NEXTAUTH_SECRET`
- [ ] Ротирован `ENCRYPTION_KEY` (если использовался)
- [ ] Ротирован `JWT_SECRET` (если использовался)
- [ ] Все новые ключи добавлены в Vercel Secrets
- [ ] Health check проходит
- [ ] Основные функции работают
- [ ] Логи проверены на ошибки

---

## 🚨 Если что-то пошло не так

### Проблема: Приложение не запускается

1. Проверьте Vercel Logs
2. Убедитесь что все ключи добавлены правильно
3. Проверьте формат ключей (нет ли лишних пробелов)
4. Откатитесь к старому ключу временно для диагностики

### Проблема: База данных недоступна

1. Проверьте `SUPABASE_SERVICE_ROLE_KEY`
2. Проверьте что ключ имеет правильные права
3. Проверьте что проект Supabase активен

### Проблема: Redis не работает

1. Проверьте `UPSTASH_REDIS_REST_URL` и `UPSTASH_REDIS_REST_TOKEN`
2. Проверьте что database активен в Upstash
3. Проверьте лимиты использования

---

## 📞 Контакты для помощи

Если возникли проблемы:
1. Проверьте документацию в `docs/SECRETS.md`
2. Проверьте логи в Vercel Dashboard
3. Обратитесь к команде DevOps

---

**Важно:** После ротации всех ключей, старые ключи **НЕ ДОЛЖНЫ** использоваться. Удалите их из всех систем и хранилищ.

---

**Последнее обновление:** 2025-01-XX  
**Статус ротации:** 🚧 В процессе

