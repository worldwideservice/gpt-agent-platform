# 🔄 Инструкция по ротации секретов

**Статус:** 🔴 КРИТИЧНО - Требуется немедленная ротация всех секретов

## 📋 Найденные секреты в репозитории

См. `SECURITY_AUDIT_REPORT.md` для полного списка.

---

## 🎯 План ротации (по приоритету)

### 1. Railway Token (ВЫСОКИЙ ПРИОРИТЕТ)

**Текущий токен:** `b2d35fc1-afcf-4589-8b24-da667437cf26` (компрометирован)

**Действия:**

1. **Создать новый токен:**
   - Откройте: https://railway.app/account/tokens
   - Создайте новый API token
   - Скопируйте новый токен

2. **Удалить старый токен:**
   - В том же разделе удалите старый токен

3. **Обновить переменные окружения:**
   ```bash
   export RAILWAY_TOKEN="новый_токен"
   ```

4. **Обновить скрипты:**
   - Все скрипты теперь используют `${RAILWAY_TOKEN:?RAILWAY_TOKEN is required}`
   - Убедитесь, что переменная установлена перед запуском скриптов

---

### 2. Sentry Tokens (ВЫСОКИЙ ПРИОРИТЕТ)

**Найденные токены:**
- `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`
- `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`
- `82a4d7aaaf2d11f092a62ea79c10f815`

**Действия:**

1. **Создать новые токены:**
   - Откройте: https://sentry.io/settings/account/api/auth-tokens/
   - Создайте новые API tokens с нужными правами
   - Скопируйте новые токены

2. **Удалить старые токены:**
   - В том же разделе удалите все найденные токены

3. **Обновить переменные окружения:**
   ```bash
   export SENTRY_TOKEN="новый_токен"
   ```

4. **Обновить скрипты:**
   - Все скрипты теперь используют `${SENTRY_TOKEN:?SENTRY_TOKEN is required}`

---

### 3. Vercel Token (ВЫСОКИЙ ПРИОРИТЕТ)

**Текущий токен:** `g5wBHt7TxDknUEIHchTJUHEK` (компрометирован)

**Действия:**

1. **Создать новый токен:**
   - Откройте: https://vercel.com/account/tokens
   - Создайте новый API token
   - Скопируйте новый токен

2. **Удалить старый токен:**
   - В том же разделе удалите старый токен

3. **Обновить переменные окружения:**
   ```bash
   export VERCEL_TOKEN="новый_токен"
   ```

---

### 4. Upstash Redis Token (ВЫСОКИЙ ПРИОРИТЕТ)

**Текущий токен:** `AYcUASQgZjI2MTM5NzYtYzU2ZS00YjFkLTk3MmQtMWIyODAzYjY3ODg5OGE3ODAzNDUwMzQ5NGE0Yjk5NzEwZDFiNWE4ZTg0MDU=`

**Действия:**

1. **Создать новый токен:**
   - Откройте: https://console.upstash.com/redis
   - Выберите вашу Redis базу данных
   - Перейдите в раздел "REST API"
   - Создайте новый REST Token
   - Скопируйте новый токен

2. **Обновить в Railway:**
   - Откройте Railway Dashboard → Ваш проект → Variables
   - Обновите `UPSTASH_REDIS_REST_TOKEN` новым значением

3. **Удалить старый токен:**
   - В Upstash Dashboard удалите старый токен

---

### 5. Supabase Service Role Key (ВЫСОКИЙ ПРИОРИТЕТ)

**Текущий ключ:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` (компрометирован)

**Действия:**

1. **Создать новый ключ:**
   - Откройте: https://supabase.com/dashboard/project/rpzchsgutabxeabbnwas/settings/api
   - В разделе "Project API keys" найдите "service_role" key
   - Нажмите "Reset" для создания нового ключа
   - Скопируйте новый ключ

2. **Обновить в Vercel:**
   - Откройте Vercel Dashboard → Ваш проект → Settings → Environment Variables
   - Обновите `SUPABASE_SERVICE_ROLE_KEY` новым значением

3. **Обновить в Railway:**
   - Откройте Railway Dashboard → Ваш проект → Variables
   - Обновите `SUPABASE_SERVICE_ROLE_KEY` новым значением

4. **⚠️ ВАЖНО:** После обновления перезапустите все сервисы

---

### 6. Encryption Key (КРИТИЧНО!)

**Текущий ключ:** `HxXQ5WCMJ3TrFZehEHJUyMVgVX5fdGsSWy/2rixkVwE=` (компрометирован)

**⚠️ ВАЖНО:** Ротация этого ключа требует перешифровки всех данных!

**Действия:**

1. **Создать новый ключ:**
   ```bash
   # Генерация нового 32-байтового ключа в base64
   openssl rand -base64 32
   ```

2. **Сохранить старый ключ** (для расшифровки существующих данных)

3. **Перешифровать все данные:**
   - Создайте скрипт для миграции данных
   - Расшифруйте все данные старым ключом
   - Зашифруйте новым ключом

4. **Обновить в Vercel и Railway:**
   - Обновите `ENCRYPTION_KEY` новым значением

5. **Удалить старый ключ** после успешной миграции

---

### 7. OpenRouter API Key (СРЕДНИЙ ПРИОРИТЕТ)

**Текущий ключ:** `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`

**Действия:**

1. **Создать новый ключ:**
   - Откройте: https://openrouter.ai/keys
   - Создайте новый API key
   - Скопируйте новый ключ

2. **Обновить в Vercel и Railway:**
   - Обновите `OPENROUTER_API_KEY` новым значением

3. **Удалить старый ключ:**
   - В OpenRouter Dashboard удалите старый ключ

---

### 8. Sentry Password (КРИТИЧНО!)

**Текущий пароль:** `l1tmw6u977c9!Q` (компрометирован)

**Действия:**

1. **Изменить пароль:**
   - Откройте: https://sentry.io/settings/account/security/
   - Измените пароль на новый надежный пароль
   - Используйте менеджер паролей для генерации

2. **Удалить из скриптов:**
   - Убедитесь, что пароль не хранится в коде

---

## 🔧 После ротации всех секретов

### 1. Очистка Git истории

**⚠️ ВАЖНО:** После ротации всех ключей удалите их из Git истории!

```bash
# Использовать BFG Repo-Cleaner (рекомендуется)
# Или git filter-branch для каждого файла со секретами

# Пример для scripts/update-railway-variables.sh:
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch scripts/update-railway-variables.sh" \
  --prune-empty --tag-name-filter cat -- --all
```

### 2. Обновление скриптов

Все скрипты должны использовать переменные окружения:
```bash
# ❌ ПЛОХО
RAILWAY_TOKEN="b2d35fc1-afcf-4589-8b24-da667437cf26"

# ✅ ХОРОШО
RAILWAY_TOKEN="${RAILWAY_TOKEN:?RAILWAY_TOKEN is required}"
```

### 3. Тестирование

После ротации:
- [ ] Проверить работу всех сервисов
- [ ] Проверить логи на ошибки аутентификации
- [ ] Протестировать деплой
- [ ] Проверить мониторинг

---

## 📚 Дополнительные меры безопасности

### 1. Настройка Git Hooks

Создать `.git/hooks/pre-commit`:
```bash
#!/bin/bash
# Проверка на секреты перед коммитом
if git diff --cached | grep -E "(password|secret|key|token).*=.*[\"'][^\"']+[\"']"; then
  echo "❌ Ошибка: Обнаружены потенциальные секреты в коде!"
  exit 1
fi
```

### 2. Secret Scanning

- Включить GitHub Secret Scanning
- Настроить GitGuardian или аналогичный сервис
- Настроить CI/CD проверки на секреты

---

**Статус:** 🔴 КРИТИЧНО - Выполнить немедленно!
