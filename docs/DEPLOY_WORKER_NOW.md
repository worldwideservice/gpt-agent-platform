# 🚀 Деплой Worker с исправлениями Redis

**Дата:** 2025-01-26  
**Приоритет:** 🔴 КРИТИЧНО

---

## 📋 Что нужно сделать

Worker был исправлен для улучшения подключения к Redis. Нужно задеплоить изменения на Railway.

---

## 🔧 Способы деплоя

### Вариант 1: Автоматический деплой через Git (рекомендуется)

Если Railway подключен к GitHub репозиторию:

1. **Изменения уже в main ветке:**
   ```bash
   # Все изменения уже закоммичены и запушены
   # Railway автоматически задеплоит при push в main
   ```

2. **Проверить деплой:**
   - Открыть: https://railway.app/dashboard
   - Выбрать проект: `gpt-agent-platform-production`
   - Выбрать сервис: `Worker`
   - Проверить вкладку "Deployments" - должен быть новый деплой

---

### Вариант 2: Ручной деплой через Railway CLI

Если автоматический деплой не сработал:

```bash
# 1. Установить Railway CLI (если не установлен)
npm i -g @railway/cli

# 2. Войти в Railway
railway login

# 3. Перейти в директорию Worker
cd services/worker

# 4. Задеплоить
railway up
```

---

## ✅ Проверка после деплоя

### 1. Проверить Health Check

```bash
curl https://gpt-agent-platform-production.up.railway.app/health
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "redis": {
    "connected": true
  }
}
```

### 2. Проверить Metrics

```bash
curl https://gpt-agent-platform-production.up.railway.app/metrics
```

**Ожидаемый результат:**
```json
{
  "redis": {
    "connected": true,
    "lastError": null
  }
}
```

### 3. Проверить логи Worker

1. Открыть: https://railway.app/dashboard
2. Выбрать проект → Worker → Logs
3. Найти строки:
   - `Redis connection ready`
   - `Redis URL constructed`
   - Нет ошибок `ERR DB connection timed-out`

---

## 🔍 Что было исправлено

**Файл:** `services/worker/src/index.ts`

**Изменения:**
- ✅ TLS настройки: `rejectUnauthorized: false`
- ✅ Таймаут: увеличен до 30 секунд
- ✅ IPv4: принудительно используется `family: 4`
- ✅ Обработка ошибок: улучшена для DNS, auth, timeout
- ✅ Логирование: добавлено детальное логирование

**Коммит:** `6bdcd911`

---

## ⚠️ Если деплой не помог

Если после деплоя Redis все еще не подключается:

1. **Проверить переменные окружения в Railway:**
   - `UPSTASH_REDIS_REST_URL` - должен быть валидный URL
   - `UPSTASH_REDIS_REST_TOKEN` - должен быть валидный токен

2. **Проверить логи:**
   - Найти ошибки подключения
   - Проверить формат URL

3. **Проверить Upstash Dashboard:**
   - Убедиться что Redis инстанс активен
   - Проверить что токен валиден

---

## 📞 Следующие шаги

После успешного деплоя:
1. ✅ Проверить health check
2. ✅ Протестировать синхронизацию Kommo
3. ✅ Проверить обработку jobs

---

**Последнее обновление:** 2025-01-26

