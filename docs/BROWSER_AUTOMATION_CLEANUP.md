# 🧹 Автоматическое управление браузером

## Проблема

MCP Browser Tools оставляет браузер открытым между сессиями, что вызывает ошибку "Browser is already in use" при следующем использовании.

## ✅ Решение

Создан автоматизированный процесс для управления жизненным циклом браузера:

### 1. Скрипт очистки браузера

**Файл:** `scripts/cleanup-browser.sh`

**Назначение:** Автоматически закрывает все процессы браузера Playwright/MCP

**Использование:**
```bash
./scripts/cleanup-browser.sh
```

**Что делает:**
- Закрывает процессы `mcp-chrome`
- Закрывает процессы `playwright`
- Закрывает процессы `chromium` связанные с MCP
- Принудительно завершает процессы, если они не закрываются

### 2. Обновленные правила Cursor IDE

**Файл:** `.cursorrules`

**Добавлено:**
- Автоматическая проверка браузера перед началом работы
- Автоматическое закрытие браузера после завершения задач
- Инструкции по правильной последовательности действий

### 3. Процесс работы AI с браузером

**ПЕРЕД началом:**
1. Проверяет, не занят ли браузер
2. При необходимости выполняет `scripts/cleanup-browser.sh`

**ВО ВРЕМЯ работы:**
1. Использует браузер для автоматизации
2. Выполняет все необходимые действия

**ПОСЛЕ завершения:**
1. Ждет 2 секунды (`browser_wait_for`)
2. Автоматически запускает `scripts/cleanup-browser.sh`
3. Проверяет, что браузер закрыт

## 📋 Использование

### Автоматически (рекомендуется)

AI автоматически использует скрипт очистки перед и после работы с браузером. Никаких дополнительных действий не требуется.

### Вручную

Если нужно закрыть браузер вручную:

```bash
./scripts/cleanup-browser.sh
```

Или через терминал:

```bash
pkill -f "mcp-chrome"
pkill -f "playwright"
```

## 🔍 Проверка состояния браузера

Проверить, открыт ли браузер:

```bash
ps aux | grep -E "mcp-chrome|playwright.*chrome" | grep -v grep
```

Если команда ничего не выводит - браузер закрыт.

## ⚙️ Настройка

Скрипт можно настроить для других процессов браузера, изменив `scripts/cleanup-browser.sh`.

---

**Создано:** 2025-01-26  
**Версия:** 1.0

