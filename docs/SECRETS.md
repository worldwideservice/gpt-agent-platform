# 🔐 Управление секретами

> Документ описывает как правильно управлять секретами в проекте TON 18 Platform

**Обновлено:** 2025-01-26  
**Версия:** 2.0

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**НИКОГДА не коммитьте реальные секреты в Git!**

Все файлы с секретами должны быть в `.gitignore`:
- `.env`
- `.env.local`
- `.env.production`
- `*.env`
- Любые файлы с реальными ключами

---

## 📋 Структура переменных окружения

### 🔐 Обязательные переменные (Критично для работы)

#### 1. Аутентификация

##### `NEXTAUTH_SECRET` ⚠️ СЕКРЕТНО
- **Описание:** Секрет для NextAuth JWT токенов
- **Генерация:**
  ```bash
  openssl rand -base64 32
  ```
- **Где хранится:** Vercel Secrets
- **Длина:** Минимум 32 символа (рекомендуется)
- **Обязательность:** ✅ Обязательно

##### `NEXTAUTH_URL`
- **Описание:** URL приложения
- **Пример:** `https://your-app.vercel.app` (production) или `http://localhost:3000` (development)
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ✅ Обязательно

##### `JWT_SECRET` ⚠️ СЕКРЕТНО (Опционально, но рекомендуется)
- **Описание:** Дополнительный секрет для JWT токенов
- **Генерация:**
  ```bash
  openssl rand -base64 32
  ```
- **Где хранится:** Vercel Secrets
- **Обязательность:** ⚠️ Опционально

---

#### 2. База данных (Supabase)

##### `SUPABASE_URL`
- **Описание:** URL вашего Supabase проекта
- **Пример:** `https://xxxxx.supabase.co`
- **Где хранится:** Vercel Environment Variables
- **Публичный:** Да (можно коммитить в `.env.example`)
- **Обязательность:** ✅ Обязательно

##### `SUPABASE_ANON_KEY`
- **Описание:** Публичный анонимный ключ Supabase (для серверных операций)
- **Где хранится:** Vercel Environment Variables
- **Публичный:** Да (можно коммитить в `.env.example`)
- **⚠️ Ограничения:** Только с RLS политиками
- **Обязательность:** ✅ Обязательно

##### `SUPABASE_SERVICE_ROLE_KEY` ⚠️ СЕКРЕТНО
- **Описание:** Приватный сервисный ключ Supabase
- **Где хранится:** Vercel Secrets (НЕ в Environment Variables!)
- **Публичный:** ❌ НЕТ! Никогда не коммитьте!
- **⚠️ Доступ:** Обходит RLS, использовать только на сервере
- **Обязательность:** ✅ Обязательно

##### `NEXT_PUBLIC_SUPABASE_URL`
- **Описание:** Публичный URL Supabase (для клиентских операций)
- **Пример:** `https://xxxxx.supabase.co`
- **Где хранится:** Vercel Environment Variables
- **Публичный:** Да (доступен в браузере)
- **Обязательность:** ✅ Обязательно

##### `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- **Описание:** Публичный анонимный ключ Supabase (для клиентских операций)
- **Где хранится:** Vercel Environment Variables
- **Публичный:** Да (доступен в браузере)
- **⚠️ Ограничения:** Только с RLS политиками
- **Обязательность:** ✅ Обязательно

##### `SUPABASE_DEFAULT_ORGANIZATION_ID` (Опционально)
- **Описание:** UUID организации по умолчанию (для demo-режима)
- **Формат:** UUID (например: `00000000-0000-4000-8000-000000000001`)
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ⚠️ Опционально

---

#### 3. Redis (Upstash)

##### `UPSTASH_REDIS_REST_URL`
- **Описание:** REST URL для Upstash Redis
- **Пример:** `https://xxxxx.upstash.io`
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ✅ Обязательно (для production)

##### `UPSTASH_REDIS_REST_TOKEN` ⚠️ СЕКРЕТНО
- **Описание:** Токен доступа к Upstash Redis
- **Где хранится:** Vercel Secrets
- **Публичный:** ❌ НЕТ!
- **Обязательность:** ✅ Обязательно (для production)

##### `REDIS_URL` (Опционально, для локальной разработки)
- **Описание:** Прямое подключение к Redis
- **Пример:** `redis://username:password@host:port` или `redis://localhost:6379`
- **Где хранится:** Vercel Secrets (если используется)
- **Обязательность:** ⚠️ Опционально

---

#### 4. AI API (OpenRouter)

##### `OPENROUTER_API_KEY` ⚠️ СЕКРЕТНО
- **Описание:** API ключ для OpenRouter
- **Формат:** `sk-or-v1-...`
- **Где хранится:** Vercel Secrets
- **Публичный:** ❌ НЕТ!
- **Получение:** https://openrouter.ai/keys
- **Обязательность:** ⚠️ Опционально (но требуется для работы AI)

##### `OPENROUTER_EMBEDDING_MODEL` (Опционально)
- **Описание:** Модель для embeddings
- **По умолчанию:** `text-embedding-ada-002`
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ⚠️ Опционально

---

#### 5. Шифрование

##### `ENCRYPTION_KEY` ⚠️ СЕКРЕТНО
- **Описание:** Ключ для шифрования CRM токенов и чувствительных данных (AES-256)
- **Генерация:**
  ```bash
  openssl rand -base64 32
  ```
- **Длина:** 32 байта (base64)
- **Где хранится:** Vercel Secrets
- **Публичный:** ❌ НЕТ!
- **Обязательность:** ✅ Обязательно

---

### 🔧 Опциональные переменные (Используются при наличии)

#### 6. Backend API

##### `BACKEND_API_URL`
- **Описание:** URL Fastify API сервиса
- **Пример:** `https://your-app.vercel.app` (обычно совпадает с NEXTAUTH_URL)
- **Для локальной разработки:** `http://localhost:4000`
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ⚠️ Опционально

---

#### 7. Kommo CRM Integration

##### `KOMMO_OAUTH_REDIRECT_BASE`
- **Описание:** Base URL для OAuth callback Kommo
- **Пример:** `https://your-app.vercel.app/integrations/kommo/oauth/callback`
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ⚠️ Опционально (требуется для Kommo интеграции)

##### `KOMMO_WEBHOOK_SECRET` ⚠️ СЕКРЕТНО
- **Описание:** Секрет для проверки подписи Kommo webhooks
- **Генерация:** Произвольная строка (минимум 10 символов)
- **Где хранится:** Vercel Secrets
- **Обязательность:** ⚠️ Опционально (требуется для Kommo webhooks)

---

#### 8. Email Service (SMTP)

##### `SMTP_HOST`
- **Описание:** SMTP сервер
- **Примеры:** `smtp.gmail.com`, `smtp.sendgrid.net`
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ⚠️ Опционально (требуется для отправки email)

##### `SMTP_PORT`
- **Описание:** SMTP порт
- **Примеры:** `587` (TLS), `465` (SSL)
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ⚠️ Опционально (требуется для отправки email)

##### `SMTP_USER`
- **Описание:** SMTP username
- **Пример:** `apikey` (для SendGrid) или `your-email@gmail.com`
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ⚠️ Опционально (требуется для отправки email)

##### `SMTP_PASS` ⚠️ СЕКРЕТНО
- **Описание:** SMTP password
- **Где хранится:** Vercel Secrets
- **Обязательность:** ⚠️ Опционально (требуется для отправки email)

##### `FROM_EMAIL`
- **Описание:** Email адрес отправителя по умолчанию
- **Пример:** `noreply@yourdomain.com`
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ⚠️ Опционально (требуется для отправки email)

---

#### 9. Cron Jobs

##### `CRON_SECRET`
- **Описание:** Секрет для защиты cron endpoints (Vercel Cron)
- **Генерация:** Произвольная строка (минимум 10 символов)
- **Где хранится:** Vercel Secrets
- **Обязательность:** ⚠️ Опционально (рекомендуется для production)

---

#### 10. Мониторинг (Sentry)

##### `SENTRY_DSN`
- **Описание:** DSN для Sentry error tracking (серверная сторона)
- **Формат:** `https://xxx@sentry.io/project-id`
- **Где хранится:** Vercel Environment Variables
- **Публичный:** Можно (DSN не критичен, но лучше хранить как Secret)
- **Обязательность:** ⚠️ Опционально

##### `NEXT_PUBLIC_SENTRY_DSN`
- **Описание:** Публичный DSN для client-side Sentry
- **Где хранится:** Vercel Environment Variables
- **Публичный:** Да (доступен в браузере)
- **Обязательность:** ⚠️ Опционально

---

#### 11. WebSocket

##### `FRONTEND_URL`
- **Описание:** URL фронтенда для WebSocket CORS
- **Пример:** `https://your-app.vercel.app`
- **По умолчанию:** Используется `NEXTAUTH_URL`
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ⚠️ Опционально

##### `WEBSOCKET_URL`
- **Описание:** URL WebSocket сервера
- **По умолчанию:** Используется `window.location.origin` в production
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ⚠️ Опционально

---

#### 12. Admin Access

##### `ADMIN_USERS`
- **Описание:** Список email адресов администраторов (через запятую)
- **Пример:** `admin@yourcompany.com,manager@yourcompany.com`
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ⚠️ Опционально

---

#### 13. Feature Flags

##### `DEMO_MODE`
- **Описание:** Демо-режим с ограниченной функциональностью
- **Значения:** `true`, `false`, `1`, `0`
- **В production:** Должно быть `false` или не устанавливать
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ⚠️ Опционально (только для разработки)

##### `E2E_ONBOARDING_FAKE`
- **Описание:** Фейковый onboarding для E2E тестов
- **Значения:** `1`, `0`
- **В production:** НЕ устанавливать
- **Где хранится:** Vercel Environment Variables
- **Обязательность:** ⚠️ Опционально (только для тестирования)

---

#### 14. Environment

##### `NODE_ENV`
- **Описание:** Режим окружения
- **Значения:** `development`, `production`, `test`
- **По умолчанию:** `development` (локально), `production` (Vercel автоматически)
- **Где хранится:** Vercel Environment Variables (автоматически)
- **Обязательность:** ⚠️ Опционально (устанавливается автоматически)

---

### 🧪 Только для разработки (Development Only)

#### 15. Kommo Testing

##### `KOMMO_TEST_ENABLED`
- **Описание:** Включить тестовые endpoints Kommo API
- **Значения:** `0` (выключено), `1` (включено)
- **Где хранится:** Только локально (`.env.local`)
- **Обязательность:** ⚠️ Опционально (только для разработки)

##### `KOMMO_TEST_DOMAIN`, `KOMMO_TEST_CLIENT_ID`, `KOMMO_TEST_CLIENT_SECRET`, `KOMMO_TEST_REDIRECT_URI`, `KOMMO_TEST_ACCESS_TOKEN`, `KOMMO_TEST_REFRESH_TOKEN`
- **Описание:** Настройки Kommo для ручного тестирования
- **Где хранится:** Только локально (`.env.local`)
- **Обязательность:** ⚠️ Опционально (только для разработки)

---

## 🔧 Настройка секретов в Vercel

### Через Vercel Dashboard

1. Откройте ваш проект в Vercel Dashboard
2. Перейдите в **Settings → Environment Variables**
3. Добавьте переменные:
   - Для **секретов** (токены, ключи): используйте **Secrets** (скрытые значения)
   - Для **публичных** переменных: используйте **Environment Variables** (видимые значения)

### Через Vercel CLI

```bash
# Добавить секрет (интерактивно, значение скрыто)
vercel env add SUPABASE_SERVICE_ROLE_KEY production

# Добавить публичную переменную
vercel env add NEXT_PUBLIC_SUPABASE_URL production
```

---

## 🚨 Что делать если секрет скомпрометирован?

### Немедленные действия:

1. **Ротировать ключ:**
   - Сгенерировать новый ключ
   - Заменить в Vercel Secrets
   - Удалить старый ключ

2. **Проверить логи:**
   - Проверить использование старого ключа
   - Заблокировать подозрительную активность

3. **Обновить зависимости:**
   - Перезапустить все сервисы
   - Проверить health checks

### Ключи для ротации:

#### Supabase Service Role Key
1. Откройте Supabase Dashboard
2. Settings → API
3. Сгенерируйте новый Service Role Key
4. Обновите в Vercel Secrets

#### OpenRouter API Key
1. Откройте https://openrouter.ai/keys
2. Удалите старый ключ
3. Создайте новый ключ
4. Обновите в Vercel Secrets

#### Upstash Redis Token
1. Откройте Upstash Dashboard
2. Создайте новый Redis database
3. Скопируйте новый REST URL и Token
4. Обновите в Vercel Secrets

#### NextAuth Secret
1. Сгенерируйте новый:
   ```bash
   openssl rand -base64 32
   ```
2. Обновите в Vercel Secrets
3. **⚠️ Все пользователи будут разлогинены!**

---

## 📝 Проверка секретов

### Локально

```bash
# Проверить наличие всех переменных
npm run check:env

# Проверить и верифицировать
npm run verify:env
```

### В Production

1. Проверить Vercel Dashboard → Settings → Environment Variables
2. Убедиться что все секреты добавлены
3. Проверить health check: `/api/health`

---

## 🔍 Проверка что секреты не в Git

### Проверить git history:

```bash
# Проверить есть ли секреты в истории
git log --all --full-history --source -- "*env.production*"

# Искать секреты в коде
git grep -i "supabase_service_role_key"
git grep -i "openrouter_api_key"
```

### Если секреты найдены в истории:

```bash
# Удалить файл из git истории (осторожно!)
git rm --cached env.production
git commit -m "security: remove secrets from repository"

# Если нужно удалить из всей истории (используйте git filter-branch или BFG Repo-Cleaner)
```

---

## 📚 Шаблоны файлов

### `.env.example`
Содержит примеры переменных БЕЗ реальных значений. Используется как шаблон для разработчиков.

### `.env.local` (локальная разработка)
Содержит реальные значения для локальной разработки.
**НЕ коммитится в Git!**

### `env.production.example`
Шаблон для production переменных. **НЕ содержит реальных секретов!**
Реальные секреты хранятся в Vercel Environment Variables.

---

## ✅ Best Practices

1. ✅ Используйте Vercel Secrets для всех токенов и ключей
2. ✅ Используйте Environment Variables только для публичных значений
3. ✅ Ротируйте ключи регулярно (каждые 3-6 месяцев)
4. ✅ Используйте разные ключи для dev/staging/production
5. ✅ Ограничивайте доступ к секретам (принцип наименьших привилегий)
6. ✅ Логируйте использование секретов (но не сами секреты!)
7. ✅ Регулярно проверяйте что секреты не скомпрометированы
8. ✅ Используйте переменные с префиксом `NEXT_PUBLIC_` только для публичных значений

---

## 🚫 Что НЕ делать

1. ❌ НЕ коммитьте секреты в Git
2. ❌ НЕ отправляйте секреты в сообщения/email
3. ❌ НЕ храните секреты в комментариях кода
4. ❌ НЕ используйте один ключ для всех окружений
5. ❌ НЕ передавайте секреты через URL параметры
6. ❌ НЕ логируйте секреты в консоль/файлы
7. ❌ НЕ используйте переменные с префиксом `NEXT_PUBLIC_` для секретов

---

**Последнее обновление:** 2025-01-26  
**Версия:** 2.0
