# 🚨 Runbook - Руководство по инцидентам

**Дата создания:** 2025-01-26  
**Версия:** 1.0  
**Статус:** ✅ Активный

---

## 📋 Обзор

Этот документ содержит пошаговые инструкции для разрешения типичных инцидентов в production системе GPT Agent Platform.

### Компоненты системы

1. **Frontend (Vercel)** - Next.js приложение
2. **Worker (Railway)** - Background job processing
3. **Database (Supabase)** - PostgreSQL database
4. **Redis (Upstash)** - Job queue
5. **Monitoring (Sentry)** - Error tracking

---

## 🔥 Инцидент 1: Worker не обрабатывает jobs

### Симптомы

- Jobs накапливаются в очереди (проверить через `scripts/monitor-redis-queue.sh`)
- Worker health check возвращает ошибку
- Логи Worker показывают ошибки подключения к Redis

### Диагностика

**Шаг 1: Проверить статус Worker**
```bash
# Health check
curl https://gpt-agent-platform-production.up.railway.app/health

# Метрики
curl https://gpt-agent-platform-production.up.railway.app/metrics
```

**Шаг 2: Проверить логи Worker**
- Открыть: https://railway.app → Worker Service → Logs
- Искать ошибки: "Redis connection", "ENOTFOUND", "connection refused"

**Шаг 3: Проверить глубину очереди**
```bash
./scripts/monitor-redis-queue.sh
```

### Решение

**Если проблема с Redis:**

1. **Проверить переменные окружения в Railway:**
   - `UPSTASH_REDIS_REST_URL`
   - `UPSTASH_REDIS_REST_TOKEN`
   - Убедиться, что значения корректны

2. **Проверить статус Upstash:**
   - Открыть: https://console.upstash.com
   - Проверить статус Redis instance

3. **Перезапустить Worker:**
   - Railway Dashboard → Worker Service → Settings → Redeploy

**Если проблема с обработкой jobs:**

1. **Проверить логи на ошибки обработки:**
   - Railway Dashboard → Logs
   - Sentry Dashboard → Issues

2. **Увеличить concurrency (если jobs накапливаются):**
   - Railway Dashboard → Variables
   - Изменить `JOB_CONCURRENCY` с 25 на 50

3. **Проверить доступность внешних API:**
   - OpenRouter API
   - Supabase

### Эскалация

Если проблема не решена в течение 15 минут:
1. Проверить статус всех сервисов (Vercel, Railway, Upstash, Supabase)
2. Создать инцидент в Sentry
3. Уведомить команду разработки

---

## 🔥 Инцидент 2: Redis очередь переполнена

### Симптомы

- Скрипт мониторинга показывает > 1000 jobs в очереди
- Worker не успевает обрабатывать jobs
- Latency увеличивается

### Диагностика

```bash
# Проверить глубину очереди
./scripts/monitor-redis-queue.sh 1000 500

# Проверить метрики Worker
curl https://gpt-agent-platform-production.up.railway.app/metrics | jq '.jobs'
```

### Решение

**Немедленные действия:**

1. **Увеличить concurrency:**
   ```bash
   # В Railway Dashboard → Variables
   JOB_CONCURRENCY=50  # Увеличить с 25 до 50
   ```
   - Railway автоматически перезапустит Worker

2. **Проверить, нет ли застрявших jobs:**
   - Railway Dashboard → Logs
   - Искать "stalled" или "failed" jobs

3. **Масштабировать Worker:**
   - Railway Dashboard → Settings → Scaling
   - Увеличить `numReplicas` с 2 до 3-4

**Долгосрочные действия:**

1. Оптимизировать медленные jobs
2. Добавить rate limiting для внешних API
3. Рассмотреть использование priority queues

---

## 🔥 Инцидент 3: Frontend недоступен (503/502)

### Симптомы

- Сайт возвращает 503 или 502
- Health check endpoint не отвечает
- Vercel Dashboard показывает ошибки деплоя

### Диагностика

**Шаг 1: Проверить статус Vercel**
```bash
curl https://gpt-agent-kwid.vercel.app/api/health
```

**Шаг 2: Проверить Vercel Dashboard**
- Открыть: https://vercel.com/dashboard
- Проверить последний деплой
- Проверить логи деплоя

**Шаг 3: Проверить переменные окружения**
- Vercel Dashboard → Settings → Environment Variables
- Убедиться, что все обязательные переменные установлены

### Решение

**Если проблема с деплоем:**

1. **Проверить ошибки билда:**
   - Vercel Dashboard → Deployments → Последний деплой → Build Logs
   - Исправить ошибки компиляции

2. **Ручной редеплой:**
   - Vercel Dashboard → Deployments
   - Выбрать последний успешный деплой
   - Нажать "Redeploy"

**Если проблема с переменными окружения:**

1. Проверить наличие всех обязательных переменных
2. Убедиться, что значения корректны
3. Перезапустить деплой

**Если проблема с базой данных:**

1. Проверить подключение к Supabase:
   ```bash
   curl https://gpt-agent-kwid.vercel.app/api/health | jq '.database'
   ```

2. Проверить статус Supabase:
   - Открыть: https://supabase.com/dashboard
   - Проверить статус проекта

---

## 🔥 Инцидент 4: База данных недоступна

### Симптомы

- Health check показывает `database: error`
- Frontend не может загрузить данные
- Worker не может обработать jobs, требующие БД

### Диагностика

**Шаг 1: Проверить health check**
```bash
curl https://gpt-agent-kwid.vercel.app/api/health | jq '.database'
```

**Шаг 2: Проверить статус Supabase**
- Открыть: https://supabase.com/dashboard/project/rpzchsgutabxeabbnwas
- Проверить статус проекта
- Проверить использование ресурсов

**Шаг 3: Проверить логи Supabase**
- Supabase Dashboard → Logs
- Искать ошибки подключения

### Решение

**Если проблема с подключением:**

1. **Проверить переменные окружения:**
   - `SUPABASE_URL`
   - `SUPABASE_ANON_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY`

2. **Проверить Connection Pooler:**
   - Supabase Dashboard → Settings → Database
   - Убедиться, что Pooler включен

**Если проблема с лимитами:**

1. **Проверить использование:**
   - Supabase Dashboard → Usage
   - Проверить лимиты базы данных

2. **Если лимит превышен:**
   - Рассмотреть upgrade плана
   - Оптимизировать запросы

**Если проблема с производительностью:**

1. Проверить медленные запросы в Supabase Dashboard
2. Оптимизировать индексы
3. Рассмотреть использование read replicas

---

## 🔥 Инцидент 5: Высокий error rate в Sentry

### Симптомы

- Sentry Dashboard показывает всплеск ошибок
- Error rate > 1% от общего количества запросов
- Критичные ошибки появляются часто

### Диагностика

**Шаг 1: Проверить Sentry Dashboard**
- Открыть: https://world-wide-services.sentry.io
- Проверить Issues → All
- Отсортировать по частоте

**Шаг 2: Анализ ошибок**
- Открыть детали ошибки
- Проверить stack trace
- Проверить контекст (tags, extra data)

**Шаг 3: Проверить метрики**
- Sentry Dashboard → Performance
- Проверить error rate по времени

### Решение

**Если ошибка в Worker:**

1. **Проверить логи Worker:**
   - Railway Dashboard → Logs
   - Сопоставить с временем ошибок в Sentry

2. **Исправить код:**
   - Создать hotfix branch
   - Исправить ошибку
   - Задеплоить в production

**Если ошибка во Frontend:**

1. **Проверить логи Vercel:**
   - Vercel Dashboard → Logs
   - Сопоставить с ошибками в Sentry

2. **Проверить пользовательский контекст:**
   - Sentry → Error Details → User Context
   - Определить, влияет ли на всех или на конкретных пользователей

3. **Исправить и задеплоить:**
   - Создать hotfix
   - Задеплоить через Vercel

**Если ошибка временная (network, timeout):**

1. Добавить retry logic
2. Улучшить error handling
3. Мониторить external API status

---

## 📊 Метрики для мониторинга

### Критичные метрики

1. **Worker:**
   - Jobs processed per minute
   - Failed jobs rate
   - Average processing time
   - Queue depth

2. **Frontend:**
   - Error rate
   - Response time (p50, p95, p99)
   - API endpoint availability

3. **Database:**
   - Connection pool usage
   - Query performance
   - Storage usage

4. **Redis:**
   - Connection status
   - Queue depth
   - Memory usage

### Пороги для алертов

- **Error rate:** > 1% → Warning, > 5% → Critical
- **Queue depth:** > 500 → Warning, > 1000 → Critical
- **Response time (p95):** > 2s → Warning, > 5s → Critical
- **Worker downtime:** > 1 minute → Critical

---

## 🔗 Полезные ссылки

- **Vercel Dashboard:** https://vercel.com/dashboard
- **Railway Dashboard:** https://railway.app
- **Supabase Dashboard:** https://supabase.com/dashboard
- **Sentry Dashboard:** https://world-wide-services.sentry.io
- **Upstash Console:** https://console.upstash.com

---

## 📞 Контакты для эскалации

- **Vercel Support:** https://vercel.com/support
- **Railway Support:** support@railway.app
- **Supabase Support:** support@supabase.com
- **Sentry Support:** support@sentry.io

---

**Последнее обновление:** 2025-01-26

