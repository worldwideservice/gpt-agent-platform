# Быстрый старт

Полное руководство по развертыванию GPT Agent Platform за 30 минут.

## Требования

Перед началом убедитесь, что у вас установлено:

- **Node.js** 18.17 или новее
- **npm** или **yarn**
- **Git** для клонирования репозитория
- **Supabase** аккаунт (бесплатный план подойдет)
- **OpenAI API Key** для GPT моделей

## Шаг 1: Клонирование репозитория

```bash
# Клонируйте репозиторий
git clone https://github.com/worldwideservice/gpt-agent-platform.git
cd gpt-agent-platform

# Установите зависимости
npm install
```

## Шаг 2: Настройка Supabase

### 2.1 Создайте проект в Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Нажмите "New Project"
3. Выберите организацию и регион
4. Задайте Database Password (сохраните его!)
5. Дождитесь создания проекта (~2 минуты)

### 2.2 Получите учетные данные

В настройках проекта (`Settings > API`) найдите:
- **Project URL** - URL вашего Supabase проекта
- **Anon/Public Key** - публичный ключ
- **Service Role Key** - секретный ключ (⚠️ не публикуйте!)

### 2.3 Примените миграции базы данных

```bash
# Установите Supabase CLI
npm install -g supabase

# Войдите в Supabase
supabase login

# Свяжите с проектом
supabase link --project-ref your-project-ref

# Примените миграции
supabase db push
```

Или вручную через SQL Editor в Supabase Dashboard:
1. Откройте `SQL Editor` в Supabase Dashboard
2. Скопируйте содержимое файлов из `supabase/migrations/`
3. Выполните каждую миграцию по порядку

## Шаг 3: Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```bash
# Скопируйте пример
cp .env.example .env.local
```

Заполните переменные:

```bash
# === Supabase ===
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# === OpenAI ===
OPENAI_API_KEY=sk-...

# === Authentication ===
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=$(openssl rand -base64 32)

# === Encryption (для токенов) ===
ENCRYPTION_KEY=$(node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")

# === Redis (опционально для rate limiting) ===
# Для development можно пропустить - будет использован memory store
REDIS_HOST=localhost
REDIS_PORT=6379

# Или используйте Upstash для production
# UPSTASH_REDIS_REST_URL=https://...
# UPSTASH_REDIS_REST_TOKEN=...

# === Logging (опционально) ===
LOG_LEVEL=info
LOG_TO_FILE=false

# === Sentry (опционально) ===
# SENTRY_DSN=https://...
```

### Генерация секретных ключей

```bash
# Генерация NEXTAUTH_SECRET
openssl rand -base64 32

# Генерация ENCRYPTION_KEY
npm run crypto:generate-key
```

## Шаг 4: Запуск в development режиме

```bash
# Запустите dev сервер
npm run dev

# Или с Docker (включает Redis)
npm run docker:dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Шаг 5: Регистрация первого пользователя

1. Перейдите на [http://localhost:3000/auth/register](http://localhost:3000/auth/register)
2. Заполните форму регистрации:
   - Email
   - Пароль (минимум 8 символов)
   - Имя организации
3. Нажмите "Создать аккаунт"
4. Вы будете перенаправлены в Dashboard

## Шаг 6: Создайте первого AI-агента

После регистрации вы попадете в Dashboard. Создайте первого агента:

1. Нажмите **"Создать агента"**
2. Заполните параметры:
   - **Название**: "Помощник поддержки"
   - **Описание**: "Отвечает на вопросы клиентов"
   - **Промпт**: "Ты - вежливый помощник службы поддержки. Отвечай кратко и по делу."
   - **Модель**: GPT-4 или GPT-3.5-Turbo
   - **Температура**: 0.7
3. Нажмите **"Сохранить"**

Подробнее: [Создание первого агента](/docs/ru/getting-started/first-agent)

## Шаг 7: Протестируйте агента

1. Перейдите в раздел **"Тестовый чат"**
2. Выберите созданного агента
3. Начните разговор:
   ```
   Вы: Привет! Как вы работаете?
   Агент: Здравствуйте! Я AI-помощник службы поддержки...
   ```

Подробнее: [Использование тестового чата](/docs/ru/getting-started/test-chat)

## Шаг 8: Подключите CRM (опционально)

Если у вас есть Kommo (amoCRM):

1. Перейдите в **Настройки → Интеграции**
2. Нажмите **"Подключить Kommo"**
3. Заполните OAuth данные:
   - Client ID
   - Client Secret
   - Redirect URI
4. Пройдите OAuth авторизацию
5. Настройте вебхуки для синхронизации

Подробнее: [Интеграция с Kommo](/docs/ru/integrations/kommo)

## Проверка установки

Убедитесь что все работает:

```bash
# Проверка типов TypeScript
npm run type-check

# Запуск линтера
npm run lint

# Запуск тестов (если настроены)
npm test

# Проверка миграций
npm run db:migrate
```

## Troubleshooting

### Ошибка подключения к Supabase

```
Error: Invalid Supabase URL or Anon Key
```

**Решение**: Проверьте что `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY` указаны правильно в `.env.local`

### Ошибка OpenAI API

```
Error: OpenAI API key not configured
```

**Решение**:
1. Получите API key на [platform.openai.com](https://platform.openai.com/api-keys)
2. Добавьте в `.env.local`: `OPENAI_API_KEY=sk-...`
3. Убедитесь что у вас есть кредиты на аккаунте

### База данных не создается

```
Error: relation "organizations" does not exist
```

**Решение**: Примените миграции:
```bash
supabase db push
# или вручную через SQL Editor в Supabase Dashboard
```

### Redis подключение не работает

```
Warning: Redis unavailable, using memory store
```

**Решение**: Это не критично! В development режиме можно работать без Redis. Для production:
1. Создайте бесплатный аккаунт на [upstash.com](https://upstash.com)
2. Создайте Redis базу
3. Добавьте `UPSTASH_REDIS_REST_URL` и `UPSTASH_REDIS_REST_TOKEN` в `.env.local`

### Port 3000 уже занят

```
Error: Port 3000 is already in use
```

**Решение**:
```bash
# Используйте другой порт
PORT=3001 npm run dev

# Или остановите процесс на порту 3000
lsof -ti:3000 | xargs kill
```

## Production Deployment

Для развертывания в production:

### Vercel (рекомендуется)

1. Создайте аккаунт на [vercel.com](https://vercel.com)
2. Подключите GitHub репозиторий
3. Добавьте Environment Variables из `.env.local`
4. Deploy!

```bash
# Или через CLI
npm install -g vercel
vercel
```

### Docker

```bash
# Соберите образ
docker build -t gpt-agent-platform .

# Запустите контейнер
docker run -p 3000:3000 --env-file .env.local gpt-agent-platform
```

### Railway / Render

1. Подключите GitHub
2. Выберите репозиторий
3. Настройте Environment Variables
4. Deploy

## Следующие шаги

Теперь когда платформа запущена:

- 📚 [Создайте базу знаний](/docs/ru/features/knowledge-base) для вашего агента
- 🔌 [Настройте интеграции](/docs/ru/features/integrations) с CRM
- 📊 [Изучите аналитику](/docs/ru/features/analytics) для мониторинга
- 🤖 [Создайте несколько агентов](/docs/ru/getting-started/first-agent) для разных задач

## Полезные команды

```bash
# Development
npm run dev              # Запуск dev сервера
npm run build            # Production build
npm run start            # Запуск production сервера

# Database
npm run db:migrate       # Применить миграции
npm run db:reset         # Сброс БД (осторожно!)

# Code Quality
npm run lint             # ESLint проверка
npm run type-check       # TypeScript проверка
npm test                 # Запуск тестов

# Security
npm run crypto:generate-key        # Генерация ключа шифрования
npm run crypto:migrate-tokens      # Шифрование существующих токенов
npm run crypto:migrate-tokens:dry-run  # Dry run миграции

# Docker
npm run docker:dev       # Dev окружение с Redis
npm run docker:build     # Production build
```

## Получить помощь

- 📖 [Полная документация](/docs/ru)
- ❓ [FAQ](/docs/ru/help/faq)
- 💬 [GitHub Discussions](https://github.com/worldwideservice/gpt-agent-platform/discussions)
- 🐛 [Сообщить о баге](https://github.com/worldwideservice/gpt-agent-platform/issues)

Удачи с GPT Agent Platform! 🚀
