# Интеграция с Kommo (amoCRM)

Полное руководство по подключению и использованию Kommo CRM с GPT Agent Platform.

## Обзор

Kommo (ранее amoCRM) - популярная CRM система для малого и среднего бизнеса. Интеграция позволяет:

✅ **Автоматизировать работу с лидами**
- Автоматические ответы на новые обращения
- Квалификация лидов через AI-агента
- Обогащение данных о клиентах

✅ **Синхронизировать данные**
- Двусторонняя синхронизация контактов
- Создание и обновление сделок
- История всех взаимодействий

✅ **Получать события в реальном времени**
- Webhook уведомления о новых лидах
- События изменения статусов
- Новые сообщения в чатах

## Требования

Перед началом вам понадобится:
- ✅ Аккаунт Kommo (любой тариф)
- ✅ Права администратора
- ✅ GPT Agent Platform аккаунт

## Шаг 1: Создание приложения в Kommo

### 1.1 Войдите в настройки

1. Откройте ваш аккаунт Kommo
2. Перейдите в **Настройки** → **Интеграции** → **API**
3. Нажмите **"Создать интеграцию"**

### 1.2 Заполните данные приложения

**Название:** `GPT Agent Platform`

**Redirect URI:**
```
https://your-domain.com/api/crm/kommo/callback
```

**Права доступа (scopes):**
```
✅ crm.read      - Чтение данных CRM
✅ crm.write     - Создание/обновление данных
✅ crm.leads     - Работа с лидами
✅ crm.contacts  - Работа с контактами
✅ crm.tasks     - Создание задач
✅ notifications - Отправка уведомлений
```

**Webhook URL:**
```
https://your-domain.com/api/crm/kommo/webhook
```

### 1.3 Сохраните credentials

После создания интеграции сохраните:
```
Client ID:     abc123...
Client Secret: xyz789...
Domain:        yourcompany.kommo.com
```

⚠️ **Важно:** Client Secret показывается только один раз!

## Шаг 2: Подключение в GPT Platform

### 2.1 Откройте настройки интеграций

1. Войдите в GPT Agent Platform
2. Перейдите в **Настройки** → **Интеграции**
3. Найдите **Kommo** и нажмите **"Подключить"**

### 2.2 Введите credentials

```
Client ID:       [вставьте из Kommo]
Client Secret:   [вставьте из Kommo]
Redirect URI:    https://your-domain.com/api/crm/kommo/callback
Base Domain:     yourcompany.kommo.com
```

Нажмите **"Продолжить"**

### 2.3 Авторизация OAuth

1. Вы будете перенаправлены на Kommo
2. Подтвердите доступ к данным
3. После успешной авторизации вернетесь в платформу

✅ Готово! Интеграция подключена.

## Шаг 3: Настройка Webhook

Webhook позволяют получать события из Kommo в реальном времени.

### 3.1 Включите webhook в Kommo

1. В Kommo: **Настройки** → **API** → Ваше приложение
2. **Webhook** → **Включить**
3. **URL:** `https://your-domain.com/api/crm/kommo/webhook`

### 3.2 Выберите события

```
✅ Создание лида         (leads.created)
✅ Изменение лида        (leads.updated)
✅ Создание контакта     (contacts.created)
✅ Новое сообщение       (messages.created)
✅ Изменение статуса     (leads.status_changed)
```

### 3.3 Проверьте подключение

```bash
# Отправьте тестовый webhook
curl -X POST https://your-domain.com/api/crm/kommo/webhook \
  -H "Content-Type: application/json" \
  -d '{"test": true}'

# Ответ должен быть:
{"status": "ok"}
```

## Использование интеграции

### Автоматический ответ на новые лиды

**Сценарий:** Когда появляется новый лид в Kommo, AI-агент автоматически отправляет приветствие.

#### Настройка

1. Создайте агента (или используйте существующего)
2. Включите интеграцию Kommo для агента
3. Настройте триггер:

```yaml
Триггер: Новый лид в Kommo
Действие: Отправить сообщение агентом

Промпт агента:
  "Когда приходит новый лид, поприветствуй его
   и спроси чем можем помочь.

   У тебя есть доступ к данным лида:
   - Имя
   - Email
   - Телефон
   - Источник

   Персонализируй приветствие используя имя."
```

#### Пример работы

```
Новый лид в Kommo:
  Имя: Иван Петров
  Email: [email protected]
  Источник: Сайт
          ↓
    [AI Agent обрабатывает]
          ↓
Отправка в Kommo:
  "Здравствуйте, Иван! 👋

   Спасибо за интерес к нашему продукту.
   Я AI-помощник. Чем могу помочь?"
```

### Квалификация лидов

**Сценарий:** AI-агент задает вопросы и квалифицирует лид.

#### Настройка агента

```yaml
Название: Квалификатор лидов
Модель: GPT-4
Температура: 0.6

Промпт:
  Ты квалифицируешь лиды для отдела продаж.

  ЗАДАЧИ:
  1. Узнать название компании и сферу
  2. Размер компании (кол-во сотрудников)
  3. Бюджет и сроки внедрения
  4. Кто принимает решение

  ПРОЦЕСС:
  - Задавай вопросы последовательно
  - Будь вежливым но настойчивым
  - Записывай ответы в поля CRM

  КВАЛИФИКАЦИЯ:
  Если компания >10 человек И бюджет >$500/мес:
    → Создай задачу менеджеру "Позвонить клиенту"
    → Установи статус "Квалифицированный"
  Иначе:
    → Статус "Не подходит"

  У тебя есть функции:
  - updateLead(id, data) - обновить данные лида
  - createTask(leadId, text) - создать задачу
```

#### API функции

```typescript
// Агент может вызывать:

// 1. Обновление лида
await updateLead(leadId, {
  budget: 1000,
  companySize: 50,
  decisionMaker: 'CTO',
  status: 'qualified'
})

// 2. Создание задачи
await createTask(leadId, {
  text: 'Позвонить клиенту - квалифицированный лид',
  responsible: managerId,
  deadline: tomorrow
})

// 3. Добавление примечания
await addNote(leadId, {
  text: 'Клиент интересуется интеграцией с 1С'
})
```

### Обогащение данных

**Сценарий:** AI дополняет информацию о клиенте из разговора.

```typescript
// До обогащения:
Lead {
  name: "Иван",
  phone: "+7..."
}

// Разговор с AI:
User: "Я из компании TechCorp, у нас 25 сотрудников..."

// После обогащения:
Lead {
  name: "Иван",
  phone: "+7...",
  company: "TechCorp",       // ← AI извлек
  companySize: 25,            // ← AI извлек
  industry: "tech"            // ← AI определил
}
```

**Настройка:**

```yaml
Промпт:
  Во время разговора извлекай информацию:
  - Название компании → поле company
  - Количество сотрудников → поле company_size
  - Сфера деятельности → поле industry
  - Текущее решение → поле current_solution

  После извлечения автоматически обновляй лид:
  updateLead(leadId, extractedData)
```

## Доступные API методы

### Получение данных

#### Получить лид

```typescript
const lead = await kommo.getLead(leadId)

// Response:
{
  id: 12345,
  name: "Иван Петров",
  status_id: 123,
  responsible_user_id: 456,
  price: 1000,
  custom_fields: [
    { id: 1, name: "Email", value: "[email protected]" },
    { id: 2, name: "Phone", value: "+7..." }
  ]
}
```

#### Получить контакт

```typescript
const contact = await kommo.getContact(contactId)

// Response:
{
  id: 67890,
  name: "Иван Петров",
  email: "[email protected]",
  phone: "+7...",
  company: "TechCorp"
}
```

#### Поиск

```typescript
// По email
const leads = await kommo.searchLeads({ email: "[email protected]" })

// По телефону
const contacts = await kommo.searchContacts({ phone: "+79991234567" })

// По имени
const results = await kommo.search({ query: "Иван" })
```

### Создание и обновление

#### Создать лид

```typescript
const newLead = await kommo.createLead({
  name: "Новый лид из чата",
  price: 1000,
  contact_id: contactId,
  custom_fields: [
    { id: 1, value: "[email protected]" }
  ]
})
```

#### Обновить лид

```typescript
await kommo.updateLead(leadId, {
  status_id: 142, // Квалифицированный
  price: 2000,
  custom_fields: [
    { id: 5, value: "50" } // Размер компании
  ]
})
```

#### Создать задачу

```typescript
await kommo.createTask({
  lead_id: leadId,
  text: "Позвонить клиенту",
  complete_till: Math.floor(Date.now() / 1000) + 86400, // Завтра
  task_type_id: 1, // Звонок
  responsible_user_id: managerId
})
```

#### Добавить примечание

```typescript
await kommo.addNote(leadId, {
  note_type: 'common', // Обычное примечание
  text: 'Клиент интересуется тарифом Enterprise'
})
```

### Отправка сообщений

```typescript
await kommo.sendMessage(leadId, {
  text: "Здравствуйте! Это автоматическое сообщение от нашего AI-помощника."
})
```

## Webhook события

### Структура события

```json
{
  "event": "lead.created",
  "timestamp": 1705392000,
  "account": {
    "id": "12345",
    "subdomain": "yourcompany"
  },
  "data": {
    "lead": {
      "id": 67890,
      "name": "Новый лид",
      "status_id": 123,
      "price": 1000,
      "contacts": [
        {
          "id": 111,
          "name": "Иван Петров"
        }
      ]
    }
  }
}
```

### Обработка событий

```typescript
// app/api/crm/kommo/webhook/route.ts

export async function POST(request: Request) {
  const event = await request.json()

  switch (event.event) {
    case 'lead.created':
      await handleNewLead(event.data.lead)
      break

    case 'lead.status_changed':
      await handleStatusChange(event.data.lead)
      break

    case 'message.created':
      await handleNewMessage(event.data.message)
      break
  }

  return Response.json({ status: 'ok' })
}
```

## Настройка полей CRM

### Стандартные поля

```typescript
{
  name: "Название лида",
  price: "Бюджет",
  status_id: "Статус",
  responsible_user_id: "Ответственный",
  pipeline_id: "Воронка"
}
```

### Кастомные поля

```typescript
// Получить список полей
const fields = await kommo.getCustomFields()

// Пример custom fields:
{
  id: 123456,
  name: "Размер компании",
  type: "text",
  value: "50"
}

{
  id: 123457,
  name: "Источник лида",
  type: "select",
  enums: [
    { id: 1, value: "Сайт" },
    { id: 2, value: "Реклама" },
    { id: 3, value: "Рекомендация" }
  ]
}
```

### Маппинг полей

Настройте соответствие полей AI-агента и Kommo:

```yaml
Маппинг:
  agent.companyName   → kommo.custom_field[123]  # Название компании
  agent.companySize   → kommo.custom_field[456]  # Размер компании
  agent.budget        → kommo.price              # Бюджет
  agent.industry      → kommo.custom_field[789]  # Отрасль
```

## Воронки и статусы

### Получить воронки

```typescript
const pipelines = await kommo.getPipelines()

// Response:
[
  {
    id: 1,
    name: "Продажи",
    statuses: [
      { id: 142, name: "Новый" },
      { id: 143, name: "Квалификация" },
      { id: 144, name: "Переговоры" },
      { id: 145, name: "Закрыто успешно" }
    ]
  }
]
```

### Перемещение по воронке

```typescript
// AI-агент может автоматически менять статус:

// Лид квалифицирован → Переговоры
await kommo.updateLead(leadId, {
  status_id: 144 // Переговоры
})

// Клиент отказался → Отказ
await kommo.updateLead(leadId, {
  status_id: 143, // Отказ
  custom_fields: [
    { id: 999, value: "Не подходит бюджет" } // Причина отказа
  ]
})
```

## Мониторинг интеграции

### Статистика

```
Сегодня:
  Новых лидов:           45
  Обновлено:             89
  Отправлено сообщений:  156
  Созданных задач:       23

Статус:
  ✅ Подключено
  Последняя синхронизация: 2 минуты назад
```

### Логи

```
[10:30:15] INFO  Webhook: lead.created
  leadId: 12345
  name: "Иван Петров"

[10:30:16] INFO  AI Agent: Processing lead
  agentId: "qualifier"
  action: "send_welcome"

[10:30:17] SUCCESS Message sent
  messageId: "msg_123"
  text: "Здравствуйте, Иван!..."

[10:30:18] INFO  Lead updated
  status: 142 → 143 (Квалификация)
```

### Ошибки

```
[10:45:23] ERROR Kommo API error
  endpoint: /leads/12345
  status: 429 (Rate limit exceeded)
  action: Retry in 60s

[10:46:30] SUCCESS Retry successful
```

## Лимиты API

### Kommo ограничения

```
Запросы:   1000/час
Webhook:   Unlimited
Tokens:    Обновление каждые 24 часа
```

**При превышении:**
- HTTP 429: Too Many Requests
- Retry-After header указывает время ожидания

**Оптимизация:**
- Используйте batch запросы где возможно
- Кэшируйте данные (5-10 минут)
- Используйте webhook вместо polling

## Troubleshooting

### Проблема: OAuth авторизация не работает

**Ошибка:**
```
Error: redirect_uri_mismatch
```

**Решение:**
1. Проверьте что Redirect URI совпадает точно:
   - В Kommo: `https://domain.com/api/crm/kommo/callback`
   - В настройках: `https://domain.com/api/crm/kommo/callback`
2. Нет trailing slash `/`
3. Используется HTTPS (не HTTP)

### Проблема: Webhook не приходят

**Чек-лист:**
1. ✅ URL доступен публично (не localhost)
2. ✅ Endpoint возвращает 200 OK
3. ✅ HTTPS enabled
4. ✅ Webhook включены в Kommo
5. ✅ Выбраны нужные события

**Тест:**
```bash
# Отправьте тестовый webhook из Kommo
# В Kommo: Настройки → API → Webhook → Отправить тест
```

### Проблема: Rate limit превышен

**Решение:**
```typescript
// Используйте retry с exponential backoff
async function callKommoAPI(endpoint, retries = 3) {
  for (let i = 0; i < retries; i++) {
    try {
      return await kommo.api.get(endpoint)
    } catch (error) {
      if (error.status === 429) {
        const delay = error.headers['retry-after'] || 2 ** i * 1000
        await sleep(delay)
        continue
      }
      throw error
    }
  }
}
```

### Проблема: Токен устарел

```
Error: Unauthorized (401)
```

**Решение:**
Токен должен обновляться автоматически. Если нет:
1. Проверьте что `refresh_token` сохранен
2. Проверьте логи обновления
3. Переподключите интеграцию если проблема сохраняется

## Best Practices

### Безопасность

✅ **DO:**
- Храните Client Secret в переменных окружения
- Используйте HTTPS для всех запросов
- Проверяйте webhook signatures
- Логируйте все операции

❌ **DON'T:**
- Не коммитьте секреты в Git
- Не передавайте токены в URL
- Не игнорируйте ошибки авторизации

### Производительность

✅ **DO:**
- Кэшируйте справочники (статусы, поля)
- Используйте batch операции
- Обрабатывайте webhook асинхронно

❌ **DON'T:**
- Не делайте синхронные вызовы в цикле
- Не запрашивайте одни данные повторно
- Не игнорируйте rate limits

### UX

✅ **DO:**
- Используйте персонализацию (имя клиента)
- Быстро отвечайте на новые лиды (<2 минут)
- Обогащайте данные автоматически

## Примеры использования

### Автоответчик

```
Новый лид → AI приветствие (30 сек)
          → Квалификация (2-3 вопроса)
          → Создание задачи менеджеру
```

**ROI:** 70% лидов получают ответ моментально vs 4+ часа раньше

### Классификация лидов

```
AI анализирует сообщение
  → Определяет intent (продажи/поддержка/вопрос)
  → Назначает ответственного
  → Устанавливает приоритет
```

**ROI:** +25% к конверсии благодаря правильному распределению

### Реактивация

```
Лид в статусе "Холодный" >30 дней
  → AI отправляет персональное предложение
  → Отслеживает реакцию
  → Переводит в активные при ответе
```

**ROI:** 15% холодных лидов реактивируются

## Получить помощь

- 📖 [Kommo API Documentation](https://www.amocrm.ru/developers/content/crm_platform/platform-api)
- 💬 [Обсудить в сообществе](https://github.com/worldwideservice/gpt-agent-platform/discussions)
- 🐛 [Сообщить о проблеме](https://github.com/worldwideservice/gpt-agent-platform/issues)
