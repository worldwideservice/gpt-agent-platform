# 📋 Инструкция по применению миграций

**Дата:** 2025-01-26  
**Статус:** Критично для работы системы

---

## 🔴 Критичные миграции для применения

### 1. `add_crm_tasks_and_calls.sql` - КРИТИЧНО

**Что создает:**
- Таблица `crm_tasks` - для хранения задач из CRM
- Таблица `crm_calls` - для хранения звонков из CRM

**Почему критично:**
- Эти таблицы используются в `lib/services/webhook-processor.ts`
- Без них код будет падать с ошибкой "table doesn't exist"

**Как применить:**

#### Способ 1: Через Supabase Dashboard (Рекомендуется)

1. Откройте https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Скопируйте содержимое файла `supabase/migrations/add_crm_tasks_and_calls.sql`
5. Вставьте в редактор
6. Нажмите **Run**
7. Проверьте результат

#### Способ 2: Через Supabase CLI

```bash
# Если у вас настроен Supabase CLI
supabase db push

# Или напрямую
supabase db execute --file supabase/migrations/add_crm_tasks_and_calls.sql
```

#### Способ 3: Через psql

```bash
psql "postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres" \
  -f supabase/migrations/add_crm_tasks_and_calls.sql
```

**Проверка:**
```sql
-- Проверить что таблицы созданы
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
  AND table_name IN ('crm_tasks', 'crm_calls');
```

---

### 2. `add_activity_logs.sql` - ВАЖНО

**Что создает:**
- Таблица `activity_logs` - для Dashboard Recent Updates

**Почему важно:**
- Используется в `/api/dashboard/updates`
- Без нее Dashboard будет показывать fallback данные

**Как применить:**
Аналогично миграции выше, используйте файл `supabase/migrations/add_activity_logs.sql`

---

## ✅ Проверка после применения

### Проверить все таблицы:

```sql
-- Список всех таблиц проекта
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;
```

### Должны быть созданы:
- ✅ `crm_tasks`
- ✅ `crm_calls`
- ✅ `activity_logs`

### Проверить RLS политики:

```sql
-- Проверить что RLS включен
SELECT tablename, rowsecurity 
FROM pg_tables 
WHERE schemaname = 'public' 
  AND tablename IN ('crm_tasks', 'crm_calls', 'activity_logs');
```

Все должны иметь `rowsecurity = true`

---

## 🚨 Если что-то пошло не так

### Ошибка: "table already exists"
- Это нормально, миграция использует `CREATE TABLE IF NOT EXISTS`
- Продолжайте выполнение

### Ошибка: "permission denied"
- Убедитесь что используете Service Role Key
- Или выполните через Supabase Dashboard (использует admin права)

### Ошибка: "relation does not exist" (для organizations)
- Убедитесь что базовая таблица `organizations` существует
- Примените базовые миграции сначала

---

## 📝 После применения миграций

1. ✅ Проверить что таблицы созданы
2. ✅ Проверить что индексы созданы
3. ✅ Проверить что RLS политики работают
4. ✅ Протестировать webhook обработку
5. ✅ Протестировать Dashboard Recent Updates

---

**Статус:** Требует применения в Supabase  
**Приоритет:** 🔴 КРИТИЧНО

