# ==============================================================================
# TON 18 PLATFORM - ПРОДАКШЕН КОНФИГУРАЦИЯ
# ==============================================================================
# 🔐 СКОПИРУЙТЕ ЭТИ ЗНАЧЕНИЯ В VERCEL DASHBOARD
# ⚠️ НЕ КОММИТЬТЕ env.production С СЕКРЕТАМИ В GIT!
# ✅ Перед деплоем выполните `npm run verify:env`, чтобы проверить заполнение всех обязательных полей.
#
# Все переменные должны быть добавлены в Vercel Project Settings → Environment Variables
# Секретные переменные должны быть добавлены как Secrets (не Environment Variables)
# ==============================================================================

# ==============================================================================
# 🔐 ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ (Критично для работы)
# ==============================================================================

# === АУТЕНТИФИКАЦИЯ ===
# Секрет для NextAuth JWT токенов (минимум 32 символа)
# Генерация: openssl rand -base64 32
# ⚠️ СЕКРЕТНО - Добавьте в Vercel Secrets
NEXTAUTH_SECRET=your-nextauth-secret-here

# URL приложения в production
NEXTAUTH_URL=https://your-app.vercel.app

# Дополнительный секрет для JWT токенов
# Генерация: openssl rand -base64 32
# ⚠️ СЕКРЕТНО - Добавьте в Vercel Secrets
JWT_SECRET=your-jwt-secret-here

# === SUPABASE БАЗА ДАННЫХ ===
# URL вашего Supabase проекта
SUPABASE_URL=https://your-project.supabase.co

# Анонимный ключ Supabase (для серверных операций)
SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Сервисный ключ Supabase (ОБХОДИТ RLS, используйте только на сервере!)
# ⚠️ СЕКРЕТНО - Добавьте в Vercel Secrets
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key-here

# Публичный URL Supabase (для клиентских операций)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Публичный анонимный ключ Supabase (для клиентских операций)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# UUID организации по умолчанию (опционально, для demo-режима)
SUPABASE_DEFAULT_ORGANIZATION_ID=00000000-0000-4000-8000-000000000001

# === REDIS ДЛЯ BACKGROUND JOBS И RATE LIMITING ===
# PRODUCTION: Используйте Upstash Redis (serverless)
# REST URL для Upstash Redis
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io

# REST токен для Upstash Redis
# ⚠️ СЕКРЕТНО - Добавьте в Vercel Secrets
UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-rest-token-here

# DEVELOPMENT/STAGING: Локальный Redis (optional, fallback if Upstash not available)
# URL формат: redis://[:password@]host[:port][/db-number]
# Пример: redis://:mypassword@localhost:6379
REDIS_URL=redis://localhost:6379

# Или используйте отдельные параметры:
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=change-me-in-production

# === AI API (OPENROUTER) ===
# API ключ для OpenRouter (формат: sk-or-v1-...)
# Получение: https://openrouter.ai/keys
# ⚠️ СЕКРЕТНО - Добавьте в Vercel Secrets
OPENROUTER_API_KEY=your-openrouter-api-key-here

# === ДОПОЛНИТЕЛЬНЫЕ СЕКРЕТЫ ===
# Ключ шифрования для токенов и sensitive данных (32 байта в hex = 64 символа)
# Генерация: npx tsx scripts/generate-encryption-key.ts
# ИЛИ: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ⚠️ СЕКРЕТНО - Добавьте в Vercel Secrets
# ⚠️ КРИТИЧНО - Без этого ключа невозможно расшифровать существующие токены!
ENCRYPTION_KEY=your-64-char-hex-encryption-key-here

# ==============================================================================
# 🔧 ОПЦИОНАЛЬНЫЕ ПЕРЕМЕННЫЕ (Используются при наличии)
# ==============================================================================

# === BACKEND API ===
# URL Fastify API сервиса (обычно совпадает с NEXTAUTH_URL)
BACKEND_API_URL=https://your-app.vercel.app

# === KOMMO CRM INTEGRATION ===
# Базовый URL для OAuth редиректа Kommo
KOMMO_OAUTH_REDIRECT_BASE=https://your-app.vercel.app/integrations/kommo/oauth/callback

# Секрет для проверки подписи Kommo Webhook
# ⚠️ СЕКРЕТНО - Добавьте в Vercel Secrets
KOMMO_WEBHOOK_SECRET=change-me-in-production

# === EMAIL SERVICE (SMTP) ===
# SMTP настройки для отправки email (опционально)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
FROM_EMAIL=noreply@yourdomain.com

# === CRON JOBS ===
# Секрет для защиты cron endpoints (Vercel Cron)
CRON_SECRET=your-cron-secret-here

# === МОНИТОРИНГ (SENTRY) ===
# Sentry DSN для отслеживания ошибок (опционально)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# === ПРОДУКТОВАЯ АНАЛИТИКА ===
NEXT_PUBLIC_SEGMENT_WRITE_KEY=
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# === WEBSOCKET ===
# URL фронтенда для WebSocket CORS (опционально, по умолчанию используется NEXTAUTH_URL)
FRONTEND_URL=https://your-app.vercel.app

# === АДМИН ДОСТУП ===
# Список email адресов администраторов (через запятую)
ADMIN_USERS=admin@yourcompany.com,manager@yourcompany.com

# === FEATURE FLAGS ===
# Демо-режим (в production должно быть false или не устанавливать)
DEMO_MODE=false

# Фейковый onboarding для E2E тестов (в production НЕ устанавливать)
E2E_ONBOARDING_FAKE=false

# === ENVIRONMENT ===
# Режим окружения
NODE_ENV=production

# ==============================================================================
# 📝 ПРИМЕЧАНИЯ
# ==============================================================================
# 
# 1. Все переменные с префиксом NEXT_PUBLIC_ доступны в браузере
# 2. Секретные переменные (отмечены как ⚠️ СЕКРЕТНО) должны быть добавлены в Vercel Secrets
# 3. Для Railway Worker используйте те же переменные, кроме NEXT_PUBLIC_*
# 4. Проверьте наличие всех обязательных переменных перед деплоем
# 5. Документация: docs/SECRETS.md
# 
# ==============================================================================
